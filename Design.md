# OTW Schedule Design Guide

## 공개 페이지 AEO

기존 일정·프로필·공지·피드·영상·권리 안내의 화면 구성과 스크롤을 유지한다.
AEO를 위한 요약 패널, 오류 안내, 추가 버튼을 React 화면에 넣지 않는다.
서버 HTML에는 JavaScript 없이도 읽을 수 있는 공개 본문을 제공하고, 앱 실행 시
기존 UI가 이를 대체한다. 초기 데이터·메타데이터·JSON-LD와 날짜 이동 시 갱신은
화면 요소 없이 유지한다. 멀티뷰는 제외하며 회원 전용 Play 콘텐츠는 노출하지 않는다.

일반 접속의 최초 화면은 `불러오는 중…` 텍스트만 중앙에 표시한다. 저장된
라이트·다크·시스템 테마를 첫 화면부터 적용하고, 앱이 준비되면 즉시 제거한다.
JavaScript 미실행 시에는 로딩 표시 없이 서버 본문을 읽을 수 있다. 실행 실패나
15초 지연 시 서버 본문으로 복귀하며, 뒤늦게 앱이 준비되면 기존 UI로 전환한다.

## OTW Play 회원 제안의 가수 검색

원곡 가수·외부 참여자는 이름과 별칭으로 기존 가수를 먼저 검색한다. 정확히 일치하는 이름·별칭이 있으면 기존 가수를 선택하며, 부분 일치 후보만 있거나 결과가 없으면 `새 가수로 추가`를 함께 제공한다. 검색 중이거나 조회에 실패하면 새 가수 추가를 보류하고 재시도를 안내한다.

## OTW Play 사용자 요청과 검수 목록

관리자 OTW Play의 `사용자 곡 요청` 메뉴에서 대기 중인 회원 제안을 바로 확인하고 기존 영상·가창 정보 검수 후 승인·거절한다. `가져오기/검수` 목록은 하단 접근 시 다음 커서 페이지를 자동으로 불러온다. 조회 중에는 중복 요청하지 않고, 오류가 나면 이미 불러온 항목을 유지하며 명시적 재시도를 제공한다. 필터 변경은 새 목록에서 시작하고, 검수 편집 중이거나 화면을 떠나면 자동 추가 조회를 멈춘다. 키보드 사용자를 위한 `더 보기`도 유지한다.

## OTW Play 노래방송 직접 등록

카탈로그의 `새 영상 등록`에서 `노래방송`을 선택하면 입력한 영상과 구간을 유지한 채 노래 클립 등록으로 이어진다. 기존 곡 검색·새 곡 입력, 가창 참여자, 방송 정보로 비공개 등록하며 승인된 Play 노래 클립 채널 조건을 적용한다. 채널 관리는 새 탭으로 열어 입력을 보존한다. 저장 후 카탈로그 필터는 등록 종류를 표시한다. 구간을 지정했다면 `같은 영상의 다음 곡 추가`로 이어서 등록할 수 있으며 방송일·근거·원본 링크·참여자는 유지하고 곡과 완곡/일부 선택은 초기화한다.

## OTW Play 플레이큐 비우기

데스크톱·모바일 플레이큐 헤더의 `비우기`는 현재 곡을 포함한 전체 큐를 제거하고 재생을 중지한다. 빈 큐에서는 버튼을 비활성화한다. 세션에 저장된 큐도 초기화하며 완료 안내를 제공한다.

## OTW Play 검수 게시 미리보기

미리보기의 `지정 구간 재생`은 저장 전 시작·종료 위치를 실제 YouTube 플레이어에 전달한다. 일시정지·재생 계속·구간 처음부터·종료 5초 전부터를 제공하고 종료점에서 정지한다. 종료 위치가 없으면 영상 끝까지 재생한다. 잘못된 구간은 실행을 막으며 구간/대상 변경·미리보기 닫기·검수 화면 이탈 시 플레이어를 폐기한다. 오류와 자동 재생 차단은 플레이어 아래에 안내한다.

공식 영상·노래 클립 검수의 제목 아래에 `OTW Play 게시 미리보기` 접기/펼치기를 제공한다. 저장 전 폼 상태를 곡 상세의 썸네일, 곡·원곡 정보, 가창 역할, 분류 및 방송 정보로 표시한다. 공개 화면의 `play-glass.css`, 분류 배지와 방송 정보 컴포넌트를 공유하고 미리보기 너비에 따라 한 열·두 열로 배치한다. 기존 곡은 카탈로그의 공통 정보를 사용한다. 미입력·잘못된 구간은 확인 필요로 표시하고 검수 메모는 포함하지 않는다. 미리보기는 게시 동작이 아니며 입력값과 별개로 열림 상태만 관리한다.

이 문서는 현재 구현된 OTW Schedule의 화면, 컴포넌트, 색상, 접근성 기준을
정리한 디자인 가이드입니다. 새 화면을 만들거나 기존 화면을 개선할 때는 이
문서를 먼저 확인하고, 변경 후 문서도 함께 갱신합니다.

## 기준 파일

| 영역 | 파일 |
| --- | --- |
| 전역 토큰 | `src/index.css` |
| 공개 앱 프레임 | `src/app/layout/app-shell.tsx` |
| 공개 내비게이션 | `src/app/layout/app-navigation.ts` |
| 콘텐츠 페이지 헤더 | `src/shared/ui/content-page-shell.tsx` |
| 사이트 푸터 | `src/app/layout/footer.tsx` |
| UI primitive | `src/shared/ui/*` |
| 일정 입력 | `src/features/schedules/ui/schedule-dialog.tsx` |
| 공지 배너 | `src/features/notices/ui/notice-banner.tsx` |
| 관리자 레이아웃 | `src/app/admin/admin-layout.tsx` |

## 공통 UI 선택 기준과 사용처

공통 UI는 표시·접근성·상호작용을 소유한다. API 호출, 권한, 서버 상태,
업무별 성공·오류·최신성 판정은 각 기능이 소유한다. 기능 간에는 해당 기능의
`index.ts` 공개 인터페이스를 사용한다. 전체 화면과 참조 목록은
[UI 통합 검증 기록](docs/archive/ui-system-audit.md)과
[컴포넌트 인벤토리](docs/archive/ui-component-inventory.md)에서 확인한다.

| 필요 | 기본 선택 / 지원 변형 | 실제 사용 예 |
| --- | --- | --- |
| 일반 동작 / 링크 모양 버튼 | `Button`: variant, size, disabled, asChild. 처리 중에는 기능이 disabled와 동작 문구를 전달 | `RootRouteError`, `RootNotFound`, `PlayShell` |
| 문자열·숫자·날짜 / 긴 입력 | `Input`, `Textarea` + `LabeledField` 또는 `FieldLabel htmlFor` | `NoticeFormDialog`, `DDayFormDialog`, `ScheduleDialog` |
| 단일 옵션 선택 | `Select` / `SelectField`: 기본·sm, label 또는 aria-label | 공지·D-Day 정렬, 일정 수집 설정 |
| 그룹 중 하나 선택 | `ChoiceGroup`: pills / cards, cardColumns 2 / 3. native radio와 legend 사용 | Play 회원 곡 연결 방식, 관리자 가져오기 |
| 켜짐·꺼짐 필터 | `FilterChip`: wrap / vertical, selected → aria-pressed, native button 속성 | Play 곡 검색의 다중 멤버 선택 |
| 멤버 단일 선택 | members의 `MemberFilter`: 멤버 색상·이모지, wrap / vertical, deselectOnRepeat | VOD, 치지직 클립 |
| 로딩·조회 실패·정상 빈 결과 | `QueryState`: loading / error / empty, headingLevel, action | YouTube / Shorts, 공지, Play 조회 |
| URL 이동 메뉴 | `SectionNavigation` + router `Link`, `aria-current="page"` | Admin Center 업무 선택, Play 발견·곡 검색 |
| 같은 화면의 내부 전환 | `TabsList`: 제어형 value / onValueChange, id / panelId | 작업 이력 보기, 독립형 일정 설정·Play 관리자 |
| 날짜 이동 | `DateNavigation`: 이전 / 오늘 / 다음, compact, isCurrent | 오늘·주간 스케쥴 |
| 확인 / 미저장 보호 | `useConfirmation`, `useUnsavedChanges`, `ConfirmActionDialog` | 운영 작업, Play 제안, 공지·D-Day·일정 편집 |
| 일반 콘텐츠 프레임 | `ContentPageShell`, 앱의 `PublicAppShell` | 공지·VOD·피드 |

### 입력과 오류 연결

단일 입력은 render child에 전달된 id·설명·오류 속성을 실제 컨트롤에 펼친다.
단순히 입력 위에 글자를 놓는 것으로 레이블 연결을 대신하지 않는다.

```tsx
<LabeledField label="내용" error={errors.content?.message}>
  {(control) => <Textarea {...control} {...register("content")} />}
</LabeledField>
```

설명과 오류가 함께 있으면 `aria-describedby`는 두 ID를 연결한다. `hideLabel`은
시각적으로만 레이블을 숨긴다. 여러 입력을 묶는 기존 카탈로그 편집기는 node
children을 사용해 이름 있는 group으로 유지하고, 내부 개별 입력의 이름도 제공한다.
파일·색상·범위·시간 입력, DayPicker의 월·연도 선택, 복합 검색 combobox는
브라우저·도메인 동작에 맞는 컨트롤을 유지한다. 기본 HTML 요소를 일괄 금지하지 않는다.

### 필터와 조회 상태의 의미

`MemberFilter`는 현재 VOD·클립·피드의 단일 선택 표시용이다. 기본값은 같은 멤버를
다시 누르면 전체로 돌아간다. 게시글은 `deselectOnRepeat={false}`로 같은 선택을
유지한다. Play의 다중 선택은 `FilterChip`을 조합하고 기능이 배열 및 URL을 갱신한다.
공통 UI에서 단일·다중 선택 의미를 임의로 바꾸지 않는다.

```tsx
<MemberFilter members={members} selectedUids={selectedMemberUids}
  onChange={setSelectedMemberUids} layout="vertical" deselectOnRepeat={false} />
<QueryState state="error" title={errorMessage}
  action={{ onClick: () => void refetch(), pending: isFetching }} />
```

`QueryState`에 query 객체를 넘기지 않는다. 기능이 최초 실패·정상 0건·수집 중을
구분한다. Shorts는 최초 실패 때 빈 결과를 함께 렌더링하지 않으며, 갱신 실패 때는
기존 목록과 최신성 안내·재시도를 함께 유지한다. 일반 YouTube의 기존 데이터 유지,
Play의 409 일관성 오류·503 공개 설정 정책은 기존 기능 계층에 남는다.
`action.icon`의 기본값은 새로고침이며, 조건 초기화처럼 다른 동작은 `icon: null`을 쓴다.

### 탭, URL, 확인창

`TabsList`는 좌우 방향키·Home·End로 활성 탭과 포커스를 함께 이동하고 disabled 항목을
건너뛴다. 소비자는 `role="tabpanel"`, `id={panelId}`, `aria-labelledby={activeTabId}`를
연결한다. 외부 업무 메뉴가 전환하는 콘텐츠는 이름 있는 `region`을 사용하고, 화면에
존재하지 않는 내부 탭 ID를 참조하지 않는다. URL 메뉴는 탭 키보드 모델을 덧씌우지 않는다.

```tsx
const confirm = useConfirmation();
if (!await confirm({ title: "작업 확인", description: "선택한 작업을 실행합니다." })) return;
await executeOperation(); // 기능이 권한, mutation, 재조회 및 오류를 소유

const canDiscard = useUnsavedChanges(isDirty);
const closeEditor = async () => { if (await canDiscard()) onOpenChange(false); };
```

루트 `InteractionProvider` 하나가 확인 요청과 dirty 등록을 처리한다. 같은 확인에서
중복 작업을 승인하지 않으며 취소 시 입력과 시작 버튼 포커스를 보존한다. 페이지 이동은
루트 router blocker에 맡기고, 편집기 닫기만 `canDiscard`를 호출한다. 두 곳에서 동일한
이동을 다시 확인하지 않는다. 새로고침·탭 닫기는 `beforeunload`를 유지한다.
단독 컨트롤형 확인창도 shared `ConfirmActionDialog`를 사용한다. 직접 `window.confirm`
및 폐기 경로 import는 `pnpm architecture:check`가 거부한다.

### 테마와 기능 전용 화면

루트 `UiScopeContext`의 public / admin / play 값이 공통 portal 콘텐츠로 전달된다.
Dialog·AlertDialog·Select·Popover·DropdownMenu·Sheet·Tooltip에 페이지의 테마를 적용하며,
body 전체에 관리자/Play 클래스를 붙여 다른 화면을 오염시키지 않는다. 짧은 화면에서
대화상자 본문을 스크롤할 수 있고 동작 줄이기 설정에서는 공통 오버레이 애니메이션을 제거한다.

프로필의 배경·사인·채널 배치, 스냅샷의 내보내기 폭, Mul.Live iframe, Play glass와
플레이어·대기열은 기능 전용으로 유지한다. 특히 플레이어는 검색·상세의 하위 콘텐츠
전환과 무관한 기존 소유 위치에 둔다. 화면 전체를 일반 콘텐츠 카드로 바꾸거나 route
필터 변경을 이유로 iframe에 새 key를 부여하지 않는다.

## 제품 톤

- 공개 화면은 팬 사이트다운 친근함과 빠른 스캔성을 함께 가져간다.
- 관리자 화면은 장식보다 밀도, 정렬, 상태 확인을 우선한다.
- 멤버 색상은 감성 요소가 아니라 정보 accent로 쓴다.
- 플랫폼 고유색은 아이콘, 작은 배지, 상태 강조에만 제한한다.
- 다크모드에서는 강한 블루 선택색을 쓰지 않는다. 사이드바 활성 항목은 흰색
  배경과 어두운 텍스트를 사용한다.

- 자동 수집 일정 검토의 상단 상태 요약은 작은 아이콘과 라벨, 수치·보조 정보를 압축 배치한다. 데스크톱은 네 열, 모바일은 두 열로 표시하고 긴 설명은 줄바꿈한다.

- 거부 제외 검색 및 결과 카드는 공통 카드의 세로 패딩만 사용하며, 헤더 없는 `CardContent`에 상단 패딩을 중복 적용하지 않는다.

- 관리자 감사 기록은 필터와 단일 로그 표로 구성한다. 결과·오류를 함께 표시하고, 미등록 이벤트 코드·대상 ID를 보존한다. 라이브/수동 일정 수집의 확인·반영 건수는 성공률로 해석하지 않으며 미기록 건수는 0으로 대체하지 않는다. 발생 시각은 KST, 날짜 필터는 UTC임을 명시한다.

- X·네이버 카페 운영 패널은 바깥 카드의 세로 패딩·간격을 제거하고 헤더 8px, 본문 12px 패딩을 사용한다. 내부 상태 카드와 섹션 간격은 12px을 기준으로 한다. 카탈로그 상단 필터는 별도 카드 테두리 없이 본문에 직접 배치한다.

## 앱 프레임

- 관리자 좌측 브랜드 영역과 상단 업무 탭은 공통 56px 높이를 사용한다. 탭 하단
  구분선은 본문 좌우 끝까지 이어지고, 탭 버튼은 본문 패딩에 맞춰 세로 중앙에 배치한다.
  탭은 본문 스크롤 시 상단에 고정되며, 좁은 화면에서는 한 줄 가로 스크롤을 사용한다.
- 일반 공개 화면은 `PublicAppShell`을 사용한다.
- `lg`에서는 접힌 64px 사이드바, `xl`에서는 256px 사이드바를 사용한다.
- `/multiview`는 iframe 폭이 breakpoint에서 역으로 줄어들지 않도록 `lg` 이상에서도
  64px 접힌 사이드바를 유지한다.
- 모바일은 56px 상단 헤더와 `Sheet` 메뉴를 사용한다.
- 사이드바 상단 brand 영역과 콘텐츠 헤더는 데스크톱에서 64px 높이로 라인을
  맞춘다.
- 사이드바 푸터는 펼친 상태와 접힌 상태 모두 56px로 사이트 푸터와 정렬한다.
  사용자 메뉴 버튼 하나로 계정과 라이트·다크·시스템 테마 선택을 제공하며,
  모바일 상단 헤더에서도 동일한 사용자 메뉴를 사용한다. 펼친 사이드바에는 사용자 닉네임을 표시하고, 메뉴는 아바타·닉네임, 가로 테마 선택, 계정 관리·로그아웃 순으로 구분한다.
- 관리자 화면에는 화면 테마 선택 컨트롤을 제공하지 않는다.
- 사이트 푸터는 56px 기준의 보조 정보 영역이다.

## 콘텐츠 페이지

공지사항, VOD & 클립, 멤버 게시글처럼 콘텐츠를 탐색하는 화면은
`ContentPageShell`을 사용한다.

- 헤더 divider는 좌우 여백 없이 전체 폭을 채운다.
- 헤더는 스크롤 컨테이너 밖에 고정되어 스크롤 위치에 흔들리지 않는다.
- 헤더 내부 콘텐츠는 `max-w-screen-2xl`과 동일한 좌우 패딩 체계를 따른다.
- 페이지 본문은 `px-3 sm:px-5 lg:px-7 xl:px-8`, `gap-5`, `pb-10` 흐름을
  기본으로 한다.

## 셀렉트 입력

- 일반 선택 입력은 공통 `Select`를 사용한다. 단순한 문자열 옵션·필터는 같은 primitive를
  조합한 `SelectField`를 사용해 트리거뿐 아니라 펼친 메뉴·선택 표시·키보드 동작도 통일한다.
- 드롭다운 화살표는 우측 테두리에서 16px 안쪽에 두고, 선택값과 최소 12px 간격을 둔다.
  높이는 기본 36px, 조밀한 화면에서는 `size="sm"`의 32px를 사용하며 개별 높이 스타일로 덮어쓰지 않는다.
- 달력의 월·연도 선택은 작은 캡션 크기를 유지하면서 화살표 우측 여백을 16px로 맞춘다.
- `SelectField`는 빈 문자열을 포함한 실제 필터 값을 보존한다. 달력의 월·연도 선택은
  날짜 탐색과 결합된 DayPicker 컨트롤을 유지한다.

## 화면별 패턴

### OTW Play 관리자 정보 밀도

- 새 영상 등록은 기본적으로 전체 영상을 사용한다. `구간 선택` 체크박스를 켠 경우에만 시작·종료 시간 입력을 표시하며, 해제하면 전체 영상으로 되돌리고 영상 확인을 다시 진행한다. 메들리의 다음 커버 추가에서는 구간 선택을 자동 활성화한다.

- `채널 관리`에서 수집 감시와 승인 채널·외부 주체 관리를 함께 제공한다.
  감시 설정과 채널 승인 절차는 독립적으로 유지하며, 기존 채널 감시 링크도 통합 화면으로 연결한다.

- `재생·공개 관리`에서 공개 설정, 소스별 재생 점검, 서비스 관측을 함께 제공한다.
  별도의 소스 요약·이동 카드를 반복하지 않으며, 기존 재생 상태 링크도 이 화면으로 연결한다.

- 5개 업무 탭은 한 줄에서 가로로 탐색하며, 본문 폭이 좁아져도 화면 밖으로 밀리지 않는다.
- 제목 아래 조회 시각과 카탈로그 건수를 한 행에 모으고, 같은 의미의 제목·건수·설명을 반복하지 않는다.
- 카드 간격과 폼 그룹은 12px, 카드 내부 간격은 8px, 일반 내용 패딩은 12~14px를 기준으로 한다.
  모바일 조작부의 최소 44px 터치 영역과 긴 내용의 줄바꿈을 유지한다.
- 자동 영상 후보와 사용자 제안은 `영상 검토`에서 함께 확인하고 전체·자동 수집·사용자 제안으로 필터링한다.
  각 항목에 수집 출처를 표시하며, 선택한 사용자 제안과 자동 후보의 URL 상태는 별도로 보존한다.
  수집 방식 안내는 표시하지 않는다. 오류, 저장 영향, 승인·공개 확인은 계속 표시한다.
- 가져오기 이력의 신규 작업 링크는 제목 행에 배치하고, 결과 집계는 발견 건수와 0이 아닌 항목에 집중한다.
- 가져오기 결과 요약은 후보 검토 카드에 통합하며, 카탈로그의 일괄 게시 영역은 미게시 가창이 있을 때 표시한다.
- 카탈로그 버전 표시 영역은 제공하지 않으며, 공개 데이터 불일치에 대한 경고와 편집 보호는 유지한다.

### 공지사항 & 이벤트

- 대표 안내와 목록 카드는 `유형 → 제목 → 진행 기간·관련 멤버 → 관련 링크`
  순서와 같은 속성 라벨 정렬을 공유한다. 별도의 게시자 텍스트는 표시하지 않고,
  관련 멤버가 없으면 해당 행을 생략한다.
- 카드 배경은 neutral surface를 사용하고, 공지·이벤트 색은 작은 배지에만 적용한다.
  대표 제목은 최대 `text-2xl`, 목록 제목은 `text-lg`로 제한하며 전문은 줄여 숨기지 않는다.
- 데스크톱은 좌측 이미지와 우측 정보를 나란히 놓고, 모바일은 제목 다음에
  이미지·속성·링크를 세로로 배치한다. 긴 제목·멤버명·링크는 카드 안에서 줄바꿈한다.
- 썸네일은 `object-cover`로 영역을 빈 여백 없이 채운다. 하단 목록의 가로형 카드는
  썸네일 영역을 카드 내부 전체 높이에 맞춘다. 모바일 세로형 카드는 4:3 비율을 유지한다. 여러 이미지는
  이미지 아래에 이전·현재 위치·다음 조작부를 두고 키보드로도 이동할 수 있게 한다.
- 링크 버튼은 속성 아래에 모으고 별도의 구분선·제목·개수는 표시하지 않는다. 모든 링크는 같은 outline 스타일과
  최소 44px 높이를 사용하고, 모바일에서는 전체 폭으로 배치한다. 링크가 없으면
  준비 중인 기능처럼 보이는 빈 조작 영역을 만들지 않는다.
- 시작일 또는 종료일만 있으면 각각 `부터`·`까지`로 표시한다. 대표 안내를 제외한
  목록의 제목과 건수는 `함께 확인할 안내`로 표현한다.
- 참고: [GOV.UK Summary list](https://design-system.service.gov.uk/components/summary-list/),
  [Carbon Tile](https://carbondesignsystem.com/components/tile/usage/),
  [WAI Carousel controls](https://www.w3.org/WAI/tutorials/carousels/controls/).

### 관리자 X 수집 운영

- 멤버 게시글 관리의 X 탭은 게시물 수집과 인용 원문 보강 상태를 상단의
  두 카드로 비교한다. 넓은 화면에서는 나란히, 좁은 화면에서는 세로로 배치한다.
- 실행 이력은 설정보다 먼저 표시하고 전체 결과, 게시물 수집, 원문 보강을
  구분한다. 행을 펼치면 계정 묶음별 저장 결과와 보강 오류·재시도 시각을 보여준다.
  작업 묶음 진행률은 원문 보강 완료율로 해석하지 않는다.
- 현재 전체 대기량은 특정 실행의 처리량과 구분한다. 원문·작성자 및 검토·연결·
  이월 건수는 중복되거나 집계 단위가 다르므로 합산 비율을 만들지 않는다.
- 전체 X 예산과 그 안에 포함되는 원문 보강 한도를 사용·예약·잔여 금액 및
  UTC 기준일과 함께 표시한다. 예산 대기는 게시물 수집 실패와 구별한다.
- 공개·피드, 게시물 수집, 원문 보강 설정은 하단의 접이식 영역으로 제공한다.
  카드의 설정 버튼은 해당 영역을 펼치고 summary에 키보드 포커스를 이동한다.
- 답글은 추가 X 조회 없이 저장된 미리보기 또는 관계와 링크를 표시한다. 인용의
  원문·작성자 대기·오류와 분리해 미리보기 있음 / 관계 표시 / 접근 불가 건수를 표시한다.
  새 실행의 인용 보강과 과거 답글·인용 보강 이력은 실행에 저장된 범위로 구분한다.
- 조회 실패·오래된 결과·과거 기록 누락은 명시한다. 미확인 값을 0이나 정상으로
  표시하지 않으며, 다음 처리 가능 시각을 실행 예약 시각으로 표현하지 않는다.

### 오늘의 스케쥴

- 멤버 카드가 홈 화면의 핵심이다. 아바타, 멤버명, 그룹칩, 라이브 상태가 먼저
  읽혀야 한다.
- 그룹명 칩은 카드 우측 상단에 배치해 카드 높이를 절약한다.
- 라이브 표시는 아바타와 통합하고, hover 시 시청자 수를 보여준다.
- schedule card는 좌측 flag divider 없이 상태칩, 시간, 제목만으로 읽히게 한다.
- 카드 텍스트는 WCAG 대비와 모바일 줄바꿈을 기준으로 조정한다.

### 주간 스케쥴표

- 주간 화면은 테이블형 정보 탐색이 우선이다.
- sticky 요일 헤더와 멤버 열을 유지한다.
- schedule item은 시간, 상태, 제목 순서로 읽히게 하고 과한 장식은 피한다.
- 다크모드에서는 미묘한 실선이나 겹친 배경이 보이지 않도록 투명도 중첩을
  줄인다.

### VOD & 클립

- 다섯 미디어 탭은 동일한 24px 아이콘 영역을 사용하고 20px 플랫폼 로고를 영역 중앙(상단·좌측 2px)에 배치해 텍스트와 세로 중심을 맞춘다. 우측 하단 가위·카메라는 12px 원형 배경 안에 10px SVG·선 굵기 2.25로 표시하고, 아이콘 영역의 우측·하단으로 각각 2px 옮겨 기본 로고를 가리는 면적을 줄인다. 로고는 원래 불투명도를 유지하고, 보조 SVG는 선택 여부와 관계없이 유튜브 계열은 빨강(text-red-600), 치지직 계열은 초록(text-emerald-500)의 시그니처 색상을 유지한다. 배지 배경은 탭의 선택·호버 표면에 맞추며, SVG는 `size-2.5`로 명시해 공통 버튼의 기본 크기와 충돌하지 않게 한다.
- 플랫폼별 콘텐츠는 같은 섹션 헤더, 날짜 그룹, 카드 밀도를 공유한다.
- 미디어 카드는 썸네일 비중을 크게 두고 제목은 2줄 안에서 끊는다.
- 더보기/접기 버튼은 outline surface, 충분한 높이, 명확한 텍스트 대비를
  유지한다.

### 멤버 게시글

- `/feed`는 날짜별 최신순 카드 피드다. 화면 크기에 관계없이 1열이며 카드 간격은 16px이다. 읽기 영역은 최대 648px로 제한한다. 데스크톱 전체 프레임은 1064px(좌우 패딩 포함)이며 피드 왼쪽은 헤더 아이콘 왼쪽에, 오른쪽은 헤더 오른쪽에 맞춘다. X·카페의 기존 카드 표현을 재사용해 둥근 테두리, 카드 배경, 왼쪽 멤버 시그니처 색상 선으로 구분한다. 기존 밝은/어두운 테마를 유지한다.
- 1024px 이상에서는 가변 너비의 왼쪽 필터와 최대 648px 피드를 16px 간격으로 배치한다.
  헤더와 콘텐츠 바깥 여백은 16px로 맞추고 같은 중앙 프레임을 사용한다. 출처 필터는 제거하고
  카드 작성자 옆의 X·네이버 카페 16px 아이콘과 접근성 이름으로 출처를 구분한다.
  `멤버` 제목은 표시하지 않고 접근성 그룹 이름만 유지한다. 멤버 칩은 PC에서 세로로
  전체 너비에 배치하고 작은 화면에서는 피드 위에 줄바꿈한다. 별도 선택 Dialog는 두지 않는다.
  초기화 버튼은 두지 않고 `전체 멤버` 또는 선택한 칩을 다시 눌러 선택을 해제한다. 칩은 오시마크·멤버 이름·대표색
  테두리를 사용하고 선택하면 해당 색상을 채우되 명도에 맞는 글자색을 적용한다. 모두 펼쳐
  보이며 선택 칩을 다시 누르면 전체로 돌아간다. 모바일 터치 영역은 44px를 유지한다.
- 왼쪽 필터는 데스크톱에서 스크롤 중에도 상단에 유지하며 높이가 부족하면 내부 스크롤을 제공한다.
  모바일에서는 여러 줄의 칩이 읽기 영역을 가리지 않도록
  제목 헤더는 고정하고 필터는 본문과 함께 스크롤된다. 필터 변경 시 결과 시작점으로 이동하며 고정 여부에 맞춰
  위치를 계산한다. 백그라운드 갱신은 읽던 위치를 유지한다.
- 피드 카드 패딩은 모바일 12px / 데스크톱 16px이며 하단은 8px, 내부 간격은 10px이다. 하단 지표·공유·원문 행은 구분선 바로 아래에 배치하고 버튼 높이는 32px, 모바일·터치 환경은 44px을 유지한다. 날짜 구분선은 카드 바깥에서 날짜·달력 아이콘·가는 가로선으로 표시한다. 본문, 첨부 미디어, 원문 보기·공유·지표는 기존 카드 동작을 유지한다.
- 헤더 제목과 업데이트 시각은 가로 배치하고 실제 콘텐츠 너비가 부족할 때만 줄바꿈한다. 1024px 기준으로 강제 세로 배치하지 않는다.
- 업데이트 표시는 `피드 업데이트 · YYYY. MM. DD. HH:mm:ss KST`다. 응답 생성용 `updatedAt`을
  사용하지 않는다. API의 `feedUpdatedAt`은 접근 가능한 반환 게시글 ID에 한정해 저장된
  `x_posts.fetched_at`과 `naver_cafe_posts.fetched_at`의 최댓값을 읽는다. 원문 작성 시각이나
  실패한 조회 시각을 대신 쓰지 않으며, 메타데이터가 없거나 읽기 실패 시 null을 반환한다.
  UI도 이를 현재 시각으로 대체하지 않는다. compact 응답에도 동일한 값을 제공한다.
- 시간 조회는 ID당 인덱스 조회와 최대 80개 바인딩 단위로 제한한다. 수집·DB 쓰기·권한 변경은
  없으며 기존 응답 시각과 관리자 진단 계약은 유지한다.
- 공개 피드에는 출처 업데이트 지연·이전 데이터·수집 실패 상세 안내를 표시하지 않는다.
  기존 글은 계속 읽을 수 있다. 최초 조회 자체가 실패해 글을 전혀 표시할 수 없는 경우만
  간단한 실패 문구와 재시도를 제공한다. 상세 진단은 관리자 화면의 기존 기능을 유지한다.
- 멤버는 단일 선택이며 접근 가능한 출처의 게시글만 표시한다. 이미 받은 게시글에 필터를
  적용한 뒤 날짜별로 묶으므로 필터 전환으로 재조회하지 않는다. 답글과 인용은 항상 포함하고
  답글 전환이나 이전 브라우저 숨김 설정은 사용하지 않는다.
- 본문은 15px/24px, X 5줄·카페 요약 3줄을 기본으로 실제 넘칠 때 더보기/접기를 제공한다.
  카드와 피드 모두 답글은 회신 화살표·옅은 배경·테두리가 있는 별도 영역에 표시한다. 답글 대상은 진한 문구로 구분하고 저장된 원문의 작성자·본문·미디어는 구분선 아래에 바로 표시하며 접기 전환은 두지 않는다.
  원문이 없으면 대화 보기 링크만 제공한다. 이름과 계정이 같은 경우 중복 계정을 생략한다.
- 빈 공간·본문은 외부 이동을 하지 않는다. 사진과 카페 썸네일은 원본 비율을 유지하며
  이미지 Dialog에서 방향키·순번·Escape·포커스 복원을 지원한다. 영상·GIF는 원문 링크로
  제공하고, 공유·원문 이동·읽기 전용 반응 지표를 유지한다. 확인된 URL과 게시글 ID로
  중복 링크를 제거하며 연결을 확인할 수 없는 링크는 보존한다. 피드 하단 공유·원문 보기
  버튼은 데스크톱 높이 32px, 원문 글자 11px, 공유 아이콘 14px·외부 링크 아이콘 12px로
  작게 표시한다. 모바일은 작은 콘텐츠 크기를 유지하면서 터치 높이는 44px로 확보한다.
- 팬 운영 안내·문의·권리 링크는 피드 끝에 한 번 표시한다. 로그인·로딩·비공개에서도
  푸터를 유지하고 공지·VOD의 공용 셸 기본 동작은 유지한다.

### 멀티뷰

- `/multiview`는 진입 시 iframe 없이 멤버 선택 화면을 보여준다. 공유 URL과 새로고침도
  `c=` 채널 선택만 복원하며 시청 방식을 직접 고르게 한다. 최대 8명 제한과 선택 순서를 유지한다.
- 멤버는 방송 중·방송 중이 아닌 멤버로 중복 없이 나누고, 조회 중·실패·누락 상태는 별도
  상태 확인 영역으로 구분한다. 오프라인과 상태 미상 멤버도 선택할 수 있다.
  재조회 실패 시 캐시된 OPEN/CLOSE도 상태 확인 불가로 표시하며 LIVE·방송 제목·시청자 수를 숨긴다.
  재시도 성공 후 상태를 복원하고, 실패나 복원으로 선택값·편집 중 선택·iframe URL을 바꾸지 않는다.
- 멤버 카드는 프로필·이름·선택 체크를 제공하며 LIVE에는 방송 제목과 시청자 수를 표시한다.
  선택은 `aria-pressed`로 전달한다. 목록에 없는 유효한 공유 채널도 별도 해제 항목으로 유지한다.
- 목록만 스크롤하고 하단 선택 인원·초기화·시청 버튼은 별도 고정 영역에 둬 목록을 가리지 않는다.
  `Mul.Live에서 보기`는 추천 주 버튼으로 먼저 표시하고, `이 화면에서 보기`는 outline 버튼으로 제공한다.
  미선택 시 두 버튼을 비활성화한다. 외부 열기는 채널 조합을 유지한 새 탭 링크이며 선택 화면은 유지한다.
- 로그인·채팅은 Mul.Live Plus 확장을 설치한 브라우저에서 Mul.Live를 직접 열어 이용하도록 안내한다.
- 내부 시청을 선택한 경우에만 iframe을 생성한다. 멤버 변경은 포커스를 가두는 대화상자에서
  임시 선택하고 `적용` 시에만 URL과 iframe 구성을 갱신한다. 취소·닫기·Escape는 변경을 버린다.
  0명은 적용할 수 없으며 `선택 화면으로`는 재생을 종료하고 마지막 적용 선택을 유지한다.
- 라이브 상태 자동 갱신과 편집 패널 개폐는 iframe을 재마운트하지 않는다. 방송 종료 시에도 선택을 유지한다.
- Mul.Live와 내부 CHZZK의 교차 출처 DOM·인증 상태는 조작하지 않는다. iframe의 fullscreen 권한은
  사용자 직접 조작용이며 자체 확장·로그인·쿠키 브리지·키보드 주입은 포함하지 않는다.

### OTW Play

- 곡 검색 결과는 shadcn 스타일 `ButtonGroup`에서 카드·표 리스트·그리드로 전환한다. 아이콘 전용 버튼과 툴팁으로 보기 이름을 표시하고 선택한 보기는 채움 스타일과 `aria-pressed`로 구분한다. 버튼은 접근성 이름을 제공하며 모바일에서 44px 터치 영역을 확보한다. 곡 개수·페이지 문구를 생략하고 버튼 그룹과 목록을 8px 간격으로 묶는다. 목록 끝에 가까워지면 다음 페이지를 자동으로 불러오며 중복 요청과 오류 후 자동 재시도를 막는다. 추가 요청 실패 시 기존 곡을 유지하고 재시도를 제공한다. 기본은 기존 카드이며 브라우저에 마지막 선택을 기억한다. 필터 초기화와 보기 전환은 서로 독립적이며, 전환 시 검색 결과·추가 페이지·재생 상태를 유지한다.
- 표 리스트는 작은 16:9 썸네일, 곡명·원곡 가수, 참여자, 곡 구분과 항상 보이는 재생·대기열·상세 버튼을 사용한다. 좁은 화면에서는 참여자·곡 구분을 곡명 아래로 모으고, 480px 미만 결과 영역에서는 작업 버튼도 다음 행으로 배치한다.
- 그리드는 원본 비율을 보존하는 16:9 이미지 영역과 간소화된 곡 정보·작업 버튼을 사용한다. 결과 영역 452/684/916px부터 2/3/4열이며, 각 타일은 최소 220px을 기준으로 한다. 표·그리드는 태그와 상세 메타데이터를 생략하고 재생 불가 상태는 표시한다. 아이콘 작업은 44px 조작 영역과 곡명을 포함한 접근성 이름을 갖는다. 멤버 선택은 member UID로 필터한 곡 검색으로 이동하며 발견 화면의 배치는 유지한다.

- 현재 Play는 복원된 발견·곡 검색·player 동선에 3색 글래스 스타일과 플레이리스트를
  적용한다. 폐기된 3개 화면 전면 개편은 [아카이브](docs/archive/otw-play-three-screen-design.md)다.
  구현·운영 검증 상태는 [개발 상태](docs/development-status.md)를 따른다.
- 스타일은 `src/features/otw-play/ui/play-glass.css`의 `.otw-play-glass` 안에서만 적용한다.
  공개·곡 상세·곡 제안·내 제안에 공통 표면과 입력 스타일을 제공하고 사이트 공용 사이드바와
  관리자 화면은 유지한다. API·DTO·DB·권한·검색 조건·재생 모델과 의존성은 변경하지 않는다.
- 기존 `--otw-1` 코랄 `#F66479`는 주요 재생, `--otw-2` 틸 `#31A4A9`는 선택·포커스,
  `--otw-3` 앰버 `#FFB14E`는 발견 장식에 사용한다. 다크는 `#101116` 배경과 `#1B1D25` 카드,
  라이트는 `#F6F7F9` 배경과 흰 카드로 읽기 쉬운 중립 표면을 구성한다. 사용자의 테마 선택을 존중한다.
  작은 틸 글씨는 테마별 진한/밝은 별도 잉크색으로, 코랄 버튼은 어두운 글씨로 대비를 확보한다.
- 헤더·탭·검색 조작부·플레이어 조작부는 얇은 테두리와 반투명 재질을 사용한다.
  헤더·검색·재생 조작부의 `backdrop-filter: blur(16px)`는 지원할 때만 켜고 기본값은 불투명 표면이다.
  낮은 농도의 그라디언트는 발견 상단과 플레이어 배경에 제한한다. 모바일의 전체 화면 위치 지정용
  플레이어 바깥 컨테이너에는 배경을 칠하지 않으며, 실제 표시되는 플레이어 표면에만 적용한다.
- 공통 Poppins·Pretendard와 시스템 fallback 글꼴 정책을 따른다. 제목 굵기는 700, 발견 제목은 40px/모바일 28px,
  섹션 제목은 22px, 카드 곡명은 18px이다. 큰 패널은 24px, 곡 카드는 18px 모서리를 사용한다.
  한국어 발견 제목은 단어를 보존해 줄바꿈하고, 긴 곡명은 카드 안에서 줄바꿈·말줄임한다.

- `/play/*`는 로그인 회원의 catalog·player·playlist 경험이다. config 이외 catalog API는
  인증이 필요하며 일반 회원 열람은 `public_read_enabled`를 따른다. 관리자는 권한을 확인한
  preview와 노래 클립 탐색을 사용한다. 회원 제안은 공개 flag와 분리된 인증 경로다.
- `/play/*`는 `PublicAppShell` 안에서 남은 viewport를 사용하고 사이트 푸터를
  숨긴다. 제품 내부에 두 번째 고정 사이드바를 만들지 않는다.
- 상단 탐색은 `발견`, `곡 검색`, `플레이리스트`와 관리자용 `노래 클립`을 제공한다.
  오리지널과 커버는 곡 검색의 관계 필터로 구분하며 모바일에서는 역할별 항목을
  가로 스크롤 가능한 sticky tab으로 제공한다. 탭은 shadcn 스타일의 muted 트랙, 작은 모서리 반경, 선택 탭의 중립 표면과 얕은 그림자로 구분한다.
- 발견 상단은 `오버더월 NOW PLAY ON OTW PLAY` 제목과 첨부 음표 `public/images/otw-play/glass-note.png`로 구성한다.
  원본 RGBA 파일을 그대로 사용하며 높이는 데스크톱 144px/모바일 88px, 자동 너비와 투명 배경을 유지한다.
  음표는 브랜드 장식이며 곡 썸네일을 대신하지 않는다. 소개 배너는 우측 음표를 유지하고 현재 테마의 card 배경·card-foreground 글자색·border 테두리와 공통 카드 그림자를 사용한다. 유리 효과, 블러, 반사광, 배경 그라디언트는 사용하지 않는다.
- 발견은 첫 페이지 최대 8곡을 썸네일·곡 정보가 나란한 패널로 표시한다. 실제 콘텐츠 너비
  720px 미만에서는 썸네일 아래로 정보를 쌓는다. 추천 이미지는 가로폭 100%와 자동 높이로
  원본 비율 전체를 표시해 CSS로 생기는 좌우 여백과 크롭을 없앤다. 이미지에 포함된 여백은 수정하지 않는다. 정보 영역이 더 높을 때 같은 썸네일의 흐린 배경으로 카드 높이를 채우고 원본은 비율을 유지해 중앙에 표시한다.
  이전/다음·직접 선택·키보드·터치·재생·대기열·상세 진입을 유지한다. 2026-09-07 후속 요청에 따라
  7초 자동 전환을 추가했다. 후속 요청에 따라 시작/일시정지 버튼을 제거했다. hover·키보드 포커스·숨겨진 탭에서는 정지하고 조작을 마치면 남은 체류 시간부터 재개한다.
  SUL-25: [Dia Browser 캐러셀 공개 예시](https://skiper-ui.com/v1/skiper78)를 참고해 비활성 점은 8px, 활성 표시는 40px 캡슐로 구성하고, 표시 사이의 간격은 8px로 둔다. 활성 캡슐의 흰색 채움은 실제 자동 넘김 타이머와 같은 시작점·4초 주기로 진행한다. 점↔캡슐과 이웃 점의 위치는 240ms로 이어지고, 호버·포커스·탭 숨김에서는 진행 표시도 함께 멈춘다. 호버·포커스·탭 숨김을 해제하면 멈춘 채움 위치와 남은 체류 시간부터 이어지며, 다른 곡이나 현재 점을 직접 선택할 때만 체류 시간을 4초로 초기화한다. 키보드·애니메이션 끄기·OS 동작 줄이기에서는 위치 모션을 생략하며, 자동 전환을 끈 경우 선택 캡슐은 고정된 흰색으로 표시한다.
  동작 줄이기 설정에서는 수동 이전/다음·순서 선택만 사용한다. 하단은 순서 점과 좌우 버튼을 한 줄로 두고 모바일에서 중복 숫자는 생략한다.
  하단 패딩은 데스크톱 8px·모바일 6px, 이전/다음 버튼은 데스크톱 36px·모바일 44px 정사각형으로 유지한다.
  개인화 추천은 추가하지 않는다. 그 아래에는 모든 현재 멤버의 메인 보컬·피처링 곡 진입점과 최근 공개곡 음악 카드를 둔다.
  최근곡 카드는 큰 16:9 썸네일·곡 상세 링크·참여자·분류·공개일·항상 보이는 재생/큐 추가 버튼을 제공한다.
  최근 곡은 24곡 단위로 내부 스크롤에서 이어서 불러오며, 로딩 오류나 자동 감지
  미지원 시에도 명시적인 더 불러오기·다시 시도 버튼을 제공한다.
- 곡 검색은 검색창·필터 토글·펼친 조건·적용 조건을 하나의 영역으로 묶되 바깥 카드 배경·테두리·중첩 패딩을 제거한다.
  펼친 필터는 별도 카드 대신 상단 구분선으로 나눈다.
  상세 필터는 모든 화면에서 기본적으로 접어두고, 적용된 조건은 필터를
  펼치지 않아도 칩으로 확인·해제할 수 있게 한다. 멤버를 모두 해제하면 멤버 조건도
  함께 해제한다.
- 곡 목록은 곡명 → 원곡 가수·참여자 → 분류·공개일 → 재생 작업 순서로 표시한다.
  재생·대기열 버튼은 hover 없이 항상 보인다. 상세의 실제 공식 버전 비교와 source 선택은 유지한다.
  공식 버전 개수 칩은
  표시하지 않는다. 썸네일 영역은 데스크톱에서 카드 높이에 맞추고 이미지는 `object-contain`으로
  원본 전체를 보존한다. 640px 미만에서는 카드 상단에 16:9 영역으로 배치한다.
  저장 playlist처럼 보이는 library affordance는 만들지 않는다.
- 데스크톱은 플레이큐가 있을 때 우측 380px 플레이어를 표시하고 빈 큐에서는 본문에 전체 폭을 돌려준다. 숨긴 패널은 `inert`와 `aria-hidden`으로 제외하며 큐 알림은 별도로 유지한다.
- Apple Music 스타일의 영상·곡명·실제 가창자·타임라인·셔플/이전/재생/다음/반복 컨트롤을 배치한다. 원곡 가수, 게시 채널, 분류 배지와 참여자 아바타는 플레이어에 표시하지 않는다. 곡 상세와 YouTube 링크 버튼은 표시하지 않는다. 볼륨은 재생 컨트롤과 같은 줄에서 호버·클릭·키보드로 여는 가로 슬라이더 팝오버로 제공한다.
- 반복은 꺼짐 → 전체 → 한 곡 순서다. 아이콘과 `aria-label`로 상태를 알린다. 다음 재생 목록은 중립 배경과 `aria-current`로 현재 항목을 표시하고 항목별 삭제 버튼과 드래그 핸들을 제공한다. 핸들의 위·아래 방향키로도 순서를 바꾼다.
- 단일 영상 호스트는 최소 200px 높이를 유지한다. 일반 높이에서는 대기열이 독립 스크롤하고, 낮은 화면에서는 플레이어 내부 스크롤로 전체 조작에 접근한다. 대기열은 항상 표시하고 열기·닫기 기능은 두지 않는다. 순서 변경은 현재 항목 ID와 영상 호스트를 유지한다.
- 1280px 미만에서는 명시적 첫 재생 직후 전체 화면으로 전환한다. 카탈로그로 돌아가면 화면 폭에 관계없이 제목·가창자·파형·재생/일시정지 버튼을 가진 소형 바로 접는다. 별도의 영상 포함 중간 크기 플레이어는 제공하지 않는다. 제목으로 전체 화면을 열며 동일한 영상 호스트와 재생 상태를 유지한다.
- OTW Play 바깥 frame은 `overflow: clip`으로 두어 하단 조작부로 포커스가 이동해도
  frame 자체가 스크롤되지 않게 한다. 제목·탭은 유지하고 콘텐츠의 내부 스크롤만 이동한다.
- 현재 멤버는 오시마크와 작은 member accent를 사용하고 외부·전 소속은 중립
  칩, 그룹은 보조 라벨을 사용한다. 색상만으로 종류를 구분하지 않는다.
- config가 꺼진 준비 중 상태와 catalog 동기화 `503`을 빈 결과와 구분한다.
- 버튼 피드백은 150ms, 탭·필터는 200ms, 추천 이미지는 600ms 페이드, 내용은 600ms 페이드·4px 이동으로 전환한다.
  추천 이미지 최대 8개를 미리 읽어 같은 DOM에 유지하고 교차 페이드한다. 전환마다 이미지 요소를 재생성하지 않으며, 원본 비율과 기존 이미지 실패 대체 처리는 유지한다.
  추천 카드의 이미지와 정보는 최대 8곡이 동일한 grid 영역을 공유해 전체 높이와 재생·순서 버튼 위치를 유지한다. 정보 묶음은 텍스트 패널 상단에 정렬하며 상단 여백은 데스크톱 24px·모바일 20px로 둔다. 곡명·참여자와 태그·가창 분류를 각각 6px 간격으로 묶고 그룹 간격은 12px, 정보 영역과 재생 작업 사이 최소 간격은 20px로 둔다. 재생 작업은 패널 하단의 순서 조작부 바로 위로 정렬하고, 조작부와의 간격은 데스크톱 16px·모바일 12px로 유지한다. 짧은 내용의 높이 차이는 정보 영역 아래에 남겨 제목 시작점과 재생 작업 위치를 유지한다. 순서 조작부는 하단에 정렬한다. 곡명은 텍스트 패널 자체를 query container로 삼아 `clamp(1.25rem, calc(0.75rem + 3.5cqi), 2rem)`으로 조절한다. 기본 글꼴 기준 20–32px·굵기 700·줄높이 1.3이며 긴 곡명은 줄바꿈한다. 참여자는 14px, 영문 안내는 10px로 구분한다. 비활성 정보는 시각·접근성 트리에서 숨긴다.
  추천 조작부와 플레이어는 전환 애니메이션의 재마운트 대상에서 제외한다.
  Ripple은 제거한다. 버튼에는 180ms 색상 전환·140ms 누름 축소와 Anime.js 기반 SVG 아이콘의 240ms 애니메이션을 적용한다. 방향 아이콘은 해당 방향으로 3px 이동, 반복·섞기는 20도 회전, 나머지는 1.12배 확대 후 복귀한다. hover·클릭·키보드 활성화에 반응하며 동작 줄이기·비활성 버튼에서는 생략한다.
  `data-size`로 icon 버튼을 구분하고 내부 패딩은 0으로 둔다. 텍스트+아이콘 버튼의 기존 좌우 패딩과 고정 위치의 모바일 플레이어 열기 버튼을 유지한다.
  검색 카드에는 항상 보이는 곡 상세 링크 버튼을 둔다. 발견·검색에서 일시정지한 현재 곡을 재생하면 기존 iframe과 진행 위치를 유지한 채 재개하며, 다른 공식 소스를 선택하면 해당 소스를 새로 불러온다.
  검색 카드 썸네일은 이미지 열 너비 100%와 원본 비율의 자동 높이를 사용한다. 텍스트 영역 높이에 이미지를 맞추지 않아 좌우 빈 공간과 크롭을 방지한다. 가로 카드에서는 이미지를 세로 중앙에 두고 모바일에서는 상단에 배치한다. 이미지가 없거나 로드 실패 시에는 16:9 대체 영역을 유지한다.
  곡 상세의 검색 복귀는 상단 왼쪽에 내용 너비의 ghost 링크로 배치한다. 화살표를 상세 카드의 왼쪽 경계에 맞추고, 높이 44px·카드와 간격 12px를 유지한다. 페이지 폭으로 늘리거나 중앙에 정렬하지 않는다.
  카드와 멤버 hover는 최대 2px 이동과 테두리 강조에 한정하고 썸네일 확대·크롭은 하지 않는다.
  음표는 처음 등장할 때만 짧게 나타나며 지속 애니메이션은 없다. iframe에는 효과를 적용하지 않는다.
  프로필 메뉴에서 애니메이션을 끄면 이동·전환을 제거하며 키보드 focus는 2px 틸 outline으로 표시한다.
  모바일 주요 버튼은 최소 44px 높이로 유지한다. 회원 폼의 입력·제출·검수 흐름은 동일하다.
- 재질과 모션 참고: [Apple Materials](https://developer.apple.com/design/human-interface-guidelines/materials),
  [MDN prefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion).
  검토 결과와 전후 화면은 저장소 루트 `design-qa.md`에서 확인한다.

### 프로필과 스냅샷

- 프로필과 스냅샷은 앱 크롬을 숨기는 특수 화면이다.
- 프로필은 이미지와 멤버 accent를 적극적으로 사용한다.
- 스냅샷은 공유 이미지 생성용 비상호작용 화면이므로 고정 폭과 안정적 렌더링을
  유지하되, 조작을 위한 여백과 hover 전용 공간은 두지 않는다.
- 그리드 스냅샷 헤더는 로고, `오늘의 편성표` 제목과 날짜, 우측의 최종 편집
  일시를 한 행에 배치한다. 별도의 `OTW Schedule` 브랜드 문구는 표시하지 않는다.
- The timeline snapshot is a 720px-wide editorial poster, exported at 2x.
  It is a static publication, not an application UI: no cards, chips, rounded
  enclosures, gradients, shadows, motion, or interactive spacing conventions.
- OTW teal anchors thin rules, coral carries the 88px date, and amber underlines
  the weekday. Place the original transparent logo directly on the paper with
  no enclosing box. Use a darker coral date ink on light paper for contrast.
  Light and dark editions retain
  the same brand and member inks on warm paper or deep teal backgrounds.
- Each scheduled broadcast occupies a full-width band in its member's main
  color, with a secondary-color edge. Choose black or white text using measured
  color contrast. Set time at 36px, member name at 34px, and title at 28px.
  Keep full titles; grow the poster rather than shrinking or clipping content.
- Use embedded Poppins for Latin text and numerals, and Pretendard for Korean text.
  Preserve the existing verified font-loading and system-font fallback contract.
- 최종 편집 일시는 조회 시각이 아니라 사용자에게 공개되는 일정 데이터가 실제로
  생성, 수정, 삭제된 시각이다. 수동 편집, 승인 반영, 자동 라이브 반영은 포함하고
  승인 대기 후보 수집과 거부처럼 공개 일정이 바뀌지 않은 처리는 제외한다.
- 그리드 스냅샷의 멤버 카드는 프로필, 멤버명, 그룹명을 한 줄 헤더에 배치하고
  고정 최소 높이와 프로필 오버랩을 피한다. 일정이 적은 카드도 불필요하게 늘리지
  않고 각 카드의 콘텐츠 높이에 맞춘다.
- 그리드 스냅샷의 일정 카드는 실제 오늘의 스케쥴 카드와 상태, 시간, 제목 표현을
  공유한다. 휴방과 게릴라는 시간을 표시하지 않고, 방송 일정만 지정 시간 또는
  `미정`을 표시한다.
- The poster orders confirmed broadcasts by time, then presents guerrilla,
  undecided, off-day and unregistered members as compact editorial rosters.
  Place each status heading above its roster, with 26px names and 24px headings
  so the shared image remains readable at 390px wide. Use circular portraits
  (96px for broadcasts, 56px for rosters) with member-color outlines; preserve
  the original image assets and their faces.
  Member profiles and color marks identify each person without repeating status
  labels. Preserve actual notes and any explicit times in these rosters.
  각 명단은 2열이며 상태명만 반복하는 제목은 생략하되 실제 입력한 사유와 제목은 보존한다.
- 방송 제목은 18px, 긴 제목도 최소 16px로 줄바꿈하여 전체 내용을 보존한다.
  멤버명은 16px로 표시하고 소속 그룹명은 칩 배경 없이 보조 텍스트로 표시한다.
- 일정 없는 멤버는 2열의 프로필·이름 목록으로 모으고 공통 안내는 한 번만 표시한다.
  하단에는 최종 편집 일시, 한국시간 기준, 일정 변경 가능성, 사이트 주소를 표시한다.

### 관리자

- 관리자 화면은 기본적으로 `AdminLayout`과 `AdminSectionHeader`를 기준으로 하되,
  화면 높이가 중요한 프리뷰형 화면은 패널 헤더가 섹션 헤더 역할을 대신할 수 있다.
- 필터는 form grid, 결과는 table/card 조합, 수정은 dialog를 사용한다.
- 위험 액션은 confirm dialog와 toast를 함께 사용한다.
- 운영 대시보드는 `지금 확인할 것 → 수집 상태 → 자원 및 한도 → 정기 작업 상태 →
  보존·정리` 순서를 사용한다. 현재 조치가 필요한 이슈와 Queue를 시간순 로그보다
  먼저 두고, 정상 상태에서는 짧은 상태 바로 축약한다.
- Cloudflare D1의 실제 rows read/written과 scheduler의 보수적 admission 예상치는
  별도 카드, 제목, 단위로 표시한다. 실제 사용량 공급자 조회 실패는 운영 장애나
  scheduler 차단으로 승격하지 않는다.
- 정기 작업은 작업 종류별 최신 판정을 기본 탭으로 사용하고 전체 시간순 이력은
  보조 탭에서 필요할 때만 조회한다. `대상 없음`, `동시 실행 병합`, `아직 실행
  시각 아님`은 회색 중립 상태로 표현한다.
- 파괴적인 데이터 정리는 기본 화면에서 감추고 `고급 작업 · 데이터 정리` disclosure
  안에 배치하며 기존 재확인 절차를 유지한다.
- 스냅샷 프리뷰처럼 상태 확인이 주목적인 화면은 좌측의 좁은 옵션 패널과
  우측의 넓은 미리보기 패널로 나누고, 보조 설명보다 렌더 결과를 우선한다.
- 관리자 스냅샷 프리뷰는 실제 `/snapshot` 출력 폭과 내용을 바꾸지 않고,
  프리뷰 패널 안에서만 축소 비율을 적용해 화면에 맞춰 보여준다.

## 색상과 토큰

- 배경/전경/카드/경계는 `src/index.css` semantic token을 우선한다.
- OTW brand accents는 `--otw-1`, `--otw-2`, `--otw-3`를 사용한다.
- 사이드바 활성 상태:
  - light: `--sidebar-primary` 기반의 어두운 선택면.
  - dark: 흰색 선택면과 어두운 텍스트.
- 멤버 `main_color`는 카드 헤더, 아바타 border, 작은 상태 accent에 사용한다.
- 멤버 `sub_color`는 낮은 opacity 배경 tint에만 사용한다.
- 멤버 색상 위 텍스트는 반드시 대비 계산을 거친다.

상태 색상 기준:

- LIVE: red 계열, 흰 텍스트, 작은 pulse dot.
- 방송: 멤버색 또는 teal 계열.
- 휴방: rose/zinc 계열.
- 게릴라: amber 계열.
- 미정: slate/zinc 계열.
- 공지: neutral surface + 아이콘/배지 강조.
- 이벤트: purple 계열은 작은 배지에만 제한.
- YouTube: red 계열.
- CHZZK: emerald/cyan 계열.

## 타이포그래피

- 영문·숫자 기본 폰트는 `Poppins`, 한글은 `Pretendard Variable`을 사용한다.
- Poppins Latin의 100–900 정적 WOFF2와 Pretendard 1.3.9의 가변 WOFF2를 자체 제공하며,
  400–900 굵기를 지원한다. `font-display: swap`과 시스템 폰트 대체를 유지한다.
- 스냅샷은 최대 3초 동안 폰트를 준비하고, 준비 완료 후 글자 크기를 다시
  계산한다. 폰트 준비 또는 포함 실패 시 시스템 폰트로 저장·복사를 계속
  제공하고 완료 메시지에 대체 사용 사실을 표시한다.
- 페이지 제목: `text-xl`-`text-2xl`, `font-semibold` 또는 `font-bold`.
- 카드 제목: `text-sm`-`text-base`, 중요 멤버명은 더 크게 허용.
- 메타 텍스트: `text-xs`-`text-sm text-muted-foreground`.
- 시간/숫자는 `tabular-nums`를 사용한다.
- display급 큰 글자는 프로필, 스냅샷, 홈 멤버명처럼 명확한 이유가 있을 때만
  쓴다.

## 컴포넌트 규칙

- 버튼은 `src/shared/ui/button.tsx` variant를 우선한다.
- 아이콘 버튼에는 `aria-label`을 반드시 둔다.
- 일반 카드는 `rounded-lg`/`rounded-xl`, `border`, `shadow-sm`를 기본으로 한다.
- 카드 안에 카드 중첩은 피하고, repeated item 또는 modal처럼 실제로 필요한
  경우에만 card surface를 쓴다.
- Hover는 색상, border, 작은 shadow 정도로 제한한다.
- 필터 chip은 짧은 라벨과 명확한 selected state를 유지한다.
- Empty/Loading/Error 상태는 모든 데이터 화면에 준비한다.

## 접근성

- 일반 텍스트 대비는 WCAG AA 4.5:1 이상을 목표로 한다.
- 큰 텍스트와 아이콘+텍스트 chip도 배경 대비를 확인한다.
- focus-visible ring은 제거하지 않는다.
- `aria-current`, `aria-expanded`, `aria-controls`, `aria-pressed`를 상태에 맞게
  사용한다.
- 클릭 가능한 `div` 대신 `button` 또는 `a`를 우선한다.
- 모바일 버튼과 chip은 터치하기 쉬운 높이를 유지하고 텍스트가 겹치지 않게 한다.

## 구현 체크리스트

- 기존 feature에 같은 패턴이 있는가?
- `PublicAppShell` 또는 `ContentPageShell`을 재사용할 수 있는가?
- 새 색상은 semantic token, 상태색, 멤버색, 플랫폼색 중 하나인가?
- light/dark mode에서 선택 상태와 focus 상태가 명확한가?
- 모바일에서 긴 문구가 버튼이나 카드 밖으로 넘치지 않는가?
- 로딩, 빈 상태, 에러 상태가 있는가?
- 접근성 label과 keyboard 동작이 있는가?
- 변경된 UI 규칙을 이 문서에 반영했는가?


## 통합 관리자 콘솔 (2026-09)

관리자가 상태를 읽고, 근거를 확인한 뒤, 같은 업무에서 처리하는 순서로 구성한다. 기능 조합과 주소 상태는 `src/app/admin/console-screen.tsx`가 담당하고, 승인·게시·수집 의미는 각 기능의 공개 인터페이스를 재사용한다.

| 메뉴 | 주소 | 주요 업무 |
| --- | --- | --- |
| 대시보드 | `/admin/operations` | 즉시 조치, 사람의 검토, 자동 처리 대기, 수집 상태 |
| 자동 수집 스케쥴 검토 | `/admin/review` | 일정 승인·거부 제외 |
| 수집·소스 | `/admin/collection` | X·네이버·일정·YouTube·키리누키 채널 |
| OTW Play | `/admin/otw-play` | 카탈로그·공개 관리, 자동 후보·사용자 제안·가져오기, 감시·승인 채널·재생 상태 |
| 콘텐츠 | `/admin/content` | 공지, D-Day, 스냅샷 |
| 자원·보존 | `/admin/resources` | X 포함 예산, YouTube 쿼터, D1 실사용·보호, 보존·이미지 |
| 실행·변경 이력 | `/admin/history` | 실행 근거, 일정 변경, 관리자 감사 |

- 업무 탭은 `tab`, 수집 공급자는 `source`를 사용한다. 검색 `q`, 결과 `state`, 분류 `category`, 페이지 `page`, 선택 `selected`, 정렬 `sort`를 URL에 보존한다. 기존 주소는 해당 업무로 연결하며 스냅샷 날짜·모드·테마와 기존 주요 해시를 유지한다.
- 데스크톱 메뉴 폭은 216px, 본문은 14px를 기준으로 한다. 단순 행은 약 36~40px, 여러 결과를 비교하는 행은 내용만큼 확장한다. 긴 설명은 근거 상세에 두고 제목·조작을 가로 스크롤 밖에 두지 않는다. 모바일 조작 영역은 44px 이상을 확보한다.
- 대시보드는 즉시 조치와 검토 대상을 먼저 표시한다. 다수의 조치가 검토 영역을 밀어내지 않도록 조치 목록은 독립 스크롤을 제공한다. 색상만으로 의미를 전달하지 않고 상태 라벨·원인·영향·다음 행동을 함께 제공한다.
- `확인 중`, `미확인`, `조회 실패`, `이전 정보`, `기록 없음`을 정상·0건과 구분한다. 마지막 조회 시각을 표시하고 5분 이상 지난 조회는 오래된 정보로 표시한다. 저장된 과거 오류는 현재 장애로 추측하지 않는다.
- 조회 버튼은 ‘상태 새로고침’, 외부 처리를 수행하는 버튼은 ‘지금 수집’ 또는 ‘전체 채널 갱신 실행’으로 표시한다. 새 화면을 위해 상시 API 폴링을 추가하지 않는다. 실행 중인 기존 작업 감시는 유지한다.
- X 전체 예산 안에 원문·작성자 보강 한도가 포함된다. 두 금액을 더하지 않는다. X 기준일은 UTC, YouTube 쿼터는 Pacific이며 D1 행 수나 금액과 합산하지 않는다. 실행 작업 묶음, 현재 원문 대기, 작성자 대기는 각각 다른 단위다.
- Play 검색과 게시 상태·분류 필터는 전체 카탈로그를 대상으로 적용한 뒤 페이지를 나눈다. 곡, 가창, 재생 가능 여부, 게시 상태를 별도로 표시한다. 검토 완료·임시 저장·게시·철회는 서로 다른 동작을 유지한다.
- 공지 관리는 미리보기 없이 전체 폭 목록으로 표시한다. 데스크톱은 내용·관련 멤버·첨부 요약 / 게시 상태·기간 / 작업 버튼을 같은 열에 정렬하고, 모바일은 세로로 배치한다. 대표 선택은 즉시 반영됨을 표시한다. 공개 공지 확인 및 이미지 상태·정리 링크는 공지 관리에 표시하지 않는다. 이미지 정리는 자원 화면에서 기존 대상 확인 절차를 유지한다.
- D-Day 생일의 저장 연도는 목록에서 노출하지 않고 ‘매년 M월 D일’과 다음 도래일을 보여준다. `9999` 저장값과 공개 계산 정책을 유지한다. 윤일의 평년 계산은 기존 `setYear` 달력 정책(3월 1일)을 따른다.
- 스냅샷 날짜·모드·테마는 상단 도구 모음으로 모으며, 출력 픽셀 크기와 화면 축척을 구분한다. 실제 스냅샷 iframe 렌더링을 유지한다.
- 상세 선택과 페이지 이동은 브라우저 이력에 남긴다. 변경 입력이 있는 편집에서는 계속 편집/변경 버리기 확인을 제공한다. 저장 실패 시 입력을 유지하고 폼 주변에 결과를 표시한다. 저장 뒤 관련 목록·홈 요약·이력을 무효화해 서버 결과를 다시 확인한다.

- 가져오기와 승인 채널은 기존 작업·등록 목록을 입력 폼보다 먼저 표시한다. 신규 등록은 해당 폼으로 이동하는 링크를 제공한다. 후보의 변경 예정 정보·판단 근거는 접어서 제공하고, 현재 상태와 다음 행동은 목록에 남긴다.
- 감사 로그는 저장된 `changes`의 변경 전·후 값을 비교한다. 값이 없었던 기록은 ‘기록 없음’, 명시적 null은 ‘미설정’, 빈 문자열은 ‘빈 값’으로 구분한다. 내부 이벤트·사용자 ID·IP·원문은 상세에서 확인한다.
- 모바일 모달도 포털 위치와 관계없이 44px 조작 영역을 적용한다. 실행 이력은 좁은 화면에서 카드로 전환해 상세를 읽는 동안 제목과 상태가 가려지지 않게 한다.

- OTW Play의 모든 관리 탭은 별도 메뉴 안에서 이동한다. 기존 통합 화면의 Play 주소는 검색·페이지·선택·해시를 유지해 해당 Play 탭으로 연결한다.
- 자동 처리 대기는 제목과 지표 영역을 분리하고 항목명·건수를 같은 기준선에 정렬한다. 확인 시각은 지표 아래에 둔다. 수집 상태 카드는 12px 내부 여백, 아이콘·제목·상세 링크 한 줄로 구성하며 중첩 패딩을 두지 않는다.

- 카탈로그 분류 옵션은 현재 검색 결과나 페이지와 무관하게 전체 활성 곡에서 구성한다. 선택한 분류는 결과가 없어도 유지한다.
- 별도 API로 조회한 수집 상태는 해당 조회의 실패 여부와 마지막 확인 시각을 함께 표시한다. 재조회 실패 시 이전 판정임을 명시하고, 성공한 빈 응답은 확인 중이 아닌 기록 없음으로 표시한다.

- 2026-09-08: 발견 카드 숫자 페이지 표시를 제거하고 전환 간격을 7초로 조정했다. 참여자·멤버·그룹 및 이름 검색의 기본 역할은 메인 보컬·피처링이며 명시적 코러스·기타 역할 검색은 유지한다. 헤더 검색은 입력칸 대신 검색 영역 전체에 포커스를 표시한다.

- 2026-09-08 후속: 검색 카드는 콘텐츠 영역 480px 이상에서는 썸네일 왼쪽·정보와 버튼 오른쪽의 좌우 구성을 유지한다. 480–720px 미만에서는 썸네일 34%·정보 66%와 정보 padding 12px를 사용하고 버튼은 오른쪽 영역 안에서 줄바꿈한다. 480px 미만에서만 모바일 세로 카드로 전환한다. 정보 padding 16px·간격 8px로 이미지 원본 비율을 보존하면서 상하 빈 띠를 제거한다. Play 내부 스크롤바는 얇은 회색 손잡이·투명 트랙을 사용하며 hover에서 밝기를 높인다. 라이트/다크 color-scheme과 강제 색상 모드를 지원한다. 시각적 참고: [Spotify의 다크 UI와 디자인 역사](https://newsroom.spotify.com/2026-04-23/spotify-design-history/). 스크롤바 세부 색상은 OTW Play 표면에 맞춰 자체 조정했다.

- 검색 카드의 정보 묶음은 상단, 재생·대기열·상세 버튼은 하단에 정렬한다. 정보 패널 상하 padding은 각각 16px이며 남는 높이는 정보와 버튼 사이에 둔다.

- 검색 카드 제목은 정보 패널 자체를 query container로 삼아 `clamp(1.25rem, calc(0.875rem + 2.5cqi), 2rem)`을 적용한다. 기존 글꼴을 유지하고 굵기 900·자간 0·줄높이 1.25로 강조한다. 긴 제목은 기존 2줄 표시를 유지하며 정보 상단·버튼 하단 정렬을 보존한다.

- 폐기한 중간 전폭 정보/버튼 배치는 사용하지 않는다. 모든 좌우 카드에서 정보는 상단, 작업은 오른쪽 하단에 둔다. 썸네일 영역은 카드 높이 전체를 채우며, 원본 이미지는 확대나 크롭 없이 비율을 보존해 가운데 배치한다. 높이 차이는 같은 이미지의 흐린 배경(blur 12px·opacity 0.6)으로 채워 상하 빈 공간을 제거한다.

## OTW Play 플레이리스트 (2026-09-10)

상단 발견/곡 검색/플레이리스트에서 접근한다. 기본 모음은 평면 벤토 카드(콘텐츠 폭 960/560px 경계의 4/2/1열), 실제 이미지와 곡/가창 수를 표시한다. 오리지널·커버 다음 현재 멤버, 내 목록 순서다. 카드 클릭은 목록 보기로 이동하며 개인 사본 편집은 상세 화면에서 제공한다. 대기열 추가는 상세 화면 버튼에서 실행한다.

개인 편집은 데스크톱 좌측 검색/우측 목록, 모바일 곡 찾기/편집 목록 전환이다. 순서는 드래그와 키보드 위아래 버튼으로 바꾼다. 편집 중에는 플레이어와 재생 큐를 숨기고 재생을 일시정지한다. 기존 큐는 보존한다. 명시적 저장 후 서버 재조회, 미저장 보호와 충돌 입력 보존을 적용한다. 상세 계약은 `docs/otw-play-implementation-guide.md` 28절을 따른다.

개인 목록의 대표곡은 각 행의 작은 별 아이콘으로 지정하며 선택된 행에 대표곡 표식을 표시한다. 제목·설명 아래에 작은 썸네일·곡명·가창자 미리보기와 자동 이미지 전환을 배치한다. 대표곡은 목록 순서와 재생 상태를 바꾸지 않는다. 카드와 상세의 이미지는 서버가 계산한 동일한 대표이미지를 사용한다.

관리자 OTW Play의 기본 플레이리스트 탭은 자동 목록 선택, 이름·설명 편집, 카드 미리보기와 해당 목록의 대표곡 검색으로 구성한다. 넓은 화면에서는 설정/검색 2열, 좁은 화면에서는 1열로 배치한다. 기본 복원은 초안에 반영한 뒤 저장하며 사용자 지정 이름·설명은 카드에서 임의 축약하지 않는다.

기본·개인 플레이리스트 상세 헤더는 208px 정사각 커버 옆에 제목·설명·곡/가창 수와 작업 버튼을 함께 배치한다. 콘텐츠 폭 639px 이하에서는 커버를 160px로 줄이고, 479px 이하에서는 192px 커버와 정보를 중앙 정렬로 쌓는다. 제목과 설명은 줄바꿈하며 버튼은 최소 44px 높이를 유지한다. 로딩 뼈대도 같은 커버 크기와 배치를 사용한다. 헤더 내부 하단 여백 24px(소형 20px)과 다음 영역 사이 12px로 곡 목록을 구분한다. 진행/결과가 없는 상태 안내 영역은 렌더링하지 않아 빈 여백을 만들지 않는다.

기본·개인 플레이리스트 일괄 추가 중에는 프로그레스바와 퍼센트 대신 로딩 아이콘, ‘재생 목록에 담고 있어요’ 안내, 현재 목록 이름과 취소 버튼을 한 영역에 표시한다. 중립 배경과 테두리로 목록과 구분하며 긴 이름은 줄바꿈한다. 모션 감소 설정에서는 아이콘을 회전시키지 않고 상태 문구를 유지한다. 완료·오류·취소 결과는 같은 상태 영역에 정보 아이콘과 친근한 해요체 문장으로 바로 표시하며 ‘플레이리스트 추가 결과’ 같은 별도 제목은 두지 않는다. 실제 추가된 가창 수를 알리고 중복·재생 불가 수는 0보다 클 때만 안내한다. 중복만 있거나 빈 목록이면 추가 성공으로 표현하지 않는다. 플레이큐의 ‘현재 선택’·‘재생 중’ 표시는 데스크톱과 모바일 모두 곡 제목 위에 배치하고 제목과 가창자 정보는 이어서 표시한다.

기본·개인 플레이리스트 상세 상단에는 왼쪽 화살표와 `플레이리스트로 돌아가기` 링크 버튼을 제공한다. 브라우저 방문 기록과 무관하게 `/play/playlists`로 이동한다.

플레이리스트 편집 상단의 뒤로 가기는 저장 목록 상세(신규는 플레이리스트 홈)로 이동하며 기존 미저장 보호를 적용한다. 뒤로 가기·제목·저장·상태·모바일 전환은 고정하고 카탈로그/현재 목록은 각각 독립 스크롤한다. 두 영역에 키보드 초점으로 접근할 수 있다.

편집 헤더는 44px 단일 행에 뒤로 가기 아이콘·18px 제목·비공개 표식·저장을 배치한다. 중복 목록 보기와 상시 안내 문구는 제거하고, 변경/저장 결과가 있을 때만 작은 상태 행을 표시한다. 모바일 전환은 바로 아래에 유지한다. 편집 화면 상하 패딩과 본문 간격은 12px다.

마우스/키보드 환경의 플레이리스트 상세 목록·편집 버튼은 창 너비와 무관하게 높이 32px로 통일한다. 상세 헤더의 주요 작업 버튼은 최소 44px다. 아이콘 버튼은 32px 정사각형이며 터치 환경은 기존 터치 영역 정책을 유지한다.

현재 플레이리스트는 Motion Reorder의 세로 드래그 정렬을 사용한다. 손잡이를 이동하면 인접 항목이 스프링 애니메이션으로 자리를 내주며 순서가 실시간 반영된다. 독립 스크롤 위치를 보정하고 모션 감소 설정에서는 전환 시간을 제거한다. 위/아래 이동 버튼과 명시적 저장을 유지하며 드래그 중 저장은 비활성화한다.

현재 플레이리스트 항목은 손잡이·번호·2줄 곡 정보·이동/삭제 버튼을 가로로 배치한다. 상하 여백은 8px이며 긴 제목/참여자는 한 줄 말줄임과 전체 텍스트 title을 제공한다. 드래그 정렬과 키보드 조작을 유지한다.

편집 컬럼 내부 그리드는 `minmax(0, 1fr)`로 구성해 긴 곡명이나 입력 필드의 최소 너비가 컬럼을 밀어내지 않게 한다. 설명은 세로로만 크기 조절하며, 가로 스크롤 없이 각 컬럼의 세로 스크롤을 유지한다.

곡 검색 그리드는 공식 커버 등 관계 문구를 생략한다. 기존 대표 가창자 표시 기준을 유지하며 각 이름 앞에 24px 원형 멤버 프로필을 표시하고, 외부 참여자나 이미지 로드 실패는 기본 인물/그룹 아이콘으로 표시한다. 여러 가창자는 줄바꿈해 카드 너비를 넘지 않게 한다.

플레이리스트 카드는 풀브리드 이미지와 흰색 굵은 타이포를 겹치는 포스터형으로 구성한다. 상단 모음은 최대 64px, 멤버 이름은 최대 48px·굵기 900을 사용하며 상단 분류와 하단 실제 곡/가창 수를 분리한다. 멤버 프로필·실제 영상 썸네일을 사용하고 이미지가 없으면 기존 OTW Play 음표 그래픽을 사용한다. 어두운 오버레이로 대비를 확보하며 이미지 확대는 reduced-motion 설정을 따른다. 개인 목록과 발견 요약에도 동일한 표면을 적용하며 카드 클릭은 목록 보기로 이동한다.

곡 검색 그리드는 전체 카드 배경에 실제 썸네일을 채우고 하단 오버레이에 제목·원곡 가수·멤버 아바타를 배치한다. 제목 링크의 클릭 영역을 카드 전체로 확장하되 재생/추가 버튼은 별도 계층으로 분리한다. 별도 곡 상세 아이콘은 생략한다. 이미지 확대와 윤곽으로 호버·키보드 포커스를 표시하며 reduced-motion을 따른다. 원형 아이콘 버튼은 마우스 환경 32px, 터치 환경 44px이다.

곡 검색 그리드의 정보는 카드 하단 14px 여백에 밀착해 제목·원곡 가수·가창자 순으로 묶고, 재생/추가 원형 버튼은 우측에 6px 간격으로 가로 배치한다. 이미지 상부는 밝게 노출해 썸네일을 강조한다. 가창자는 재생 플레이어와 동일하게 최대 3개의 겹친 아바타 그룹과 우측의 전체 이름 텍스트로 표현하며 긴 이름은 말줄임과 title로 제공한다.

플레이리스트 편집 카탈로그는 결과를 간격 없는 목록으로 묶고 약 53px 행으로 압축한다. 56×32px 썸네일 옆에 한 줄 제목과 가창자·분류·날짜 메타데이터를 두 줄로 표시한다. 긴 문구는 말줄임과 title로 제공하고 재생 불가 상태와 추가 버튼은 유지한다. 기본/개인 목록 상세 행에는 이 밀도를 적용하지 않는다.

낮은 데스크톱 화면(1280px 이상·높이 639px 이하)의 현재 재생/플레이큐 전환은 shared/ui/tabs의 shadcn 스타일 Radix Tabs를 사용한다. 선택 상태와 방향키 이동을 제공하고 기존 영상 호스트와 재생 상태를 유지한다.

발견의 메인 추천곡 카드는 좌우 분할을 풀브리드 배경 이미지와 오버레이로 변경한다. 제목은 32–64px·굵기 900·줄높이 1.08로 표시하며 이미지와 텍스트는 기존 추천곡 순서대로 함께 전환한다. 어두운 방향성 오버레이로 대비를 확보하고 재생·대기열 추가·곡 상세와 하단 순서 조작은 유지한다. 중복 흐림 이미지는 제거하고 실제 썸네일 한 장을 카드 전체에 채운다.

발견 메인 추천곡 카드에서는 별도 곡 상세 버튼을 생략하고 재생·추가만 제공한다. 가창자 표시는 곡 검색 그리드와 같은 공통 아바타 그룹 컴포넌트를 사용해 최대 3개 프로필을 겹치고 우측에 이름을 표시한다.

발견 메인 추천곡 카드의 곡/가창 태그와 공식 커버·공개 형태·참여 형태 등의 분류 표시는 모두 생략한다. 제목과 아바타 그룹·가창자 이름을 중심으로 표시한다.

발견 메인 카드 가창자는 아바타 32–44px·이름 16–22px로 제목 크기에 비례해 표시한다. 재생/추가 버튼과 캐러셀은 하단 footer에 고정 정렬한다. 캐러셀은 카드 중앙에서 기존 기준보다 6px 아래에 배치하며, 원형 이전/다음 버튼을 인디케이터 좌우에 4px 간격으로 붙인다. 좁은 카드에서는 이 조작 묶음 전체를 두 번째 줄 중앙에 둬 재생/추가 버튼과 겹치지 않게 한다.

발견의 최근 공개된 곡은 곡 검색과 동일한 OtwPlaySongGrid를 재사용한다. 풀브리드 썸네일·하단 제목/원곡 가수/아바타 그룹·우측 원형 재생/추가 버튼·카드 상세 이동을 통일하며 기존 최근 공개순과 추가 페이지 로딩을 유지한다.

발견 멤버로 찾기의 원형 아바타 테두리는 기존 멤버 API의 main_color를 사용한다. 공통 active members Query 캐시를 재사용하고 색상이 없거나 조회에 실패하면 기본 border 토큰을 유지한다.

우측 플레이어의 게시 채널과 태그/가창 분류는 6px 간격의 한 정보 그룹으로 묶고 구분선은 가창자 및 상세 동작 아래, 정보 그룹 위에 배치한다.

우측 플레이어의 구분선·게시 채널·태그 묶음은 진행바와 재생 조작 아래로 배치한다. 반복 토글은 아이콘만 표시하고 현재 반복 상태와 다음 동작은 접근성 이름 및 툴팁으로 제공한다.

우측 플레이어의 게시 채널·태그·관련 구분선은 제거한다. 랜덤(대기열 섞기)은 데스크톱 및 모바일 플레이큐 제목 우측의 아이콘 버튼으로 이동하며 기존 중복/현재곡 보존과 비활성 조건을 유지한다.

우측 플레이어는 Apple Music의 단색 채움형 재생 아이콘과 절제된 간격을 기준으로 구성한다. 영상 아래 곡명과 실제 가창자를 두고, 진행 시간·탐색 바·남은 시간을 한 줄에 배치한다. 별도 앨범 썸네일과 원곡 가수는 추가하지 않는다. 기존 Poppins·Pretendard를 유지하며 중립 표면과 분홍 활성 표시로 통일한다.

컨트롤 순서는 셔플·이전·재생/일시정지·다음·반복이다. 이전/다음은 이중 삼각형, 재생은 원형 배경 없는 채움형 기호다. 일반 조작 영역은 44px, 주 재생은 56px이다. 곡 상세와 YouTube 링크 버튼은 제거하고 볼륨을 재생 컨트롤과 같은 줄에 배치한다. 다음 재생 목록은 항상 표시하며 왼쪽 드래그 핸들·썸네일·제목·가창자·삭제 버튼으로 구성한다. 팝업 메뉴와 위아래 이동 버튼은 사용하지 않는다. 포인터 드래그와 핸들의 방향키로 순서를 바꾸며 현재 재생 곡과 영상 호스트를 유지한다.

볼륨 버튼은 정밀 포인터 호버, 터치 클릭, 키보드 입력으로 가로 native range와 음소거를 담은 팝오버를 연다. 별도 제목과 퍼센트 장식은 생략하고 값은 접근성 텍스트로 제공한다. 0~100%, 1% 단위로 즉시 반응하며 음소거 해제 시 저장된 볼륨을 복원한다. 탐색 역시 native range로 즉시 반영한다. 모바일 전체 화면·태블릿 미니 영상·접힌 실행 버튼은 동일한 재생 엔진과 연결한다. 낮은 화면은 내부 스크롤로 모든 조작에 접근하며 영상의 최소 200px 높이를 유지한다.


플레이어 조작부 압축에 맞춰 낮은 데스크톱 탭 모드는 화면 높이 559px 이하로 조정한다. 560px 이상에서는 영상·조작·플레이큐를 함께 표시해 유휴 공간을 활용한다. 낮은 화면의 큐 최소 높이는 96px이며 탭 모드 재생 영역은 화면 높이를 넘으면 내부 스크롤을 제공한다. 실제 634px 화면(rail 570px)에서 큐 높이 195px, 540px 화면에서 큐 탭 높이 214px를 확인했다.

데스크톱 피드는 648px 열을 사용해 헤더 아이콘 왼쪽부터 헤더 오른쪽까지 정렬한다. 데스크톱 멤버 필터는 피드 왼쪽에 16px 간격으로 붙이며 우측은 대칭 여유 공간으로 남긴다. 고정 헤더에도 스크롤바 공간을 예약해 본문 스크롤바 유무로 정렬이 어긋나지 않게 한다.

관리자 OTW Play 카탈로그는 전체 조회 결과를 25곡씩 무한 스크롤로 이어서 표시한다. 검색·게시 상태·분류 변경 시 표시 범위를 초기화하며, 키보드 조작과 자동 감지 미지원 환경을 위한 더 보기 버튼 및 마지막 결과 안내를 제공한다.

멤버 게시글의 X·카페 카드 하단 기능 영역은 상하 8px 패딩을 동일하게 유지한다. 구분선은 카드 안쪽 좌우 끝까지 이어지고, 기능 내용의 좌우 패딩은 본문과 맞춘다. 공유·원문 버튼은 마우스 환경 28px, 터치 환경 44px로 표시한다.

X·카페 카드의 좌측 강조선을 고려해 우측 내부 패딩을 좌측보다 4px 작게 둔다. 본문과 하단 기능 영역 모두 좌측 12px·우측 8px, sm 이상에서는 좌측 16px·우측 12px를 사용한다. 구분선과 피드 전체 위치는 유지한다.

사용자 제안 검토는 상단 영상·제안 정보, 전체 너비의 승인 내용 편집, 최종 확인 및 승인 영역 순으로 배치한다. 영상·채널 확인 버튼은 최종 승인 영역에서 제공하고, 승인 버튼 바로 위에 미충족 조건을 표시한다. 사전 확인 실패는 폼 내부에서 알리며 재확인 중에는 이전 검증 결과로 승인할 수 없다.

회원 곡 제안의 가창 참여자 영역은 legend와 본문 간격을 분리하고 내부 패딩 16px, 그룹 간격 16px를 사용한다. 가창 역할은 전체 너비의 목록에서 이름과 고정 너비의 선택 입력을 정렬하고, 각 선택 입력은 셀 너비를 채운다.

회원 곡 제안의 곡 정보는 연결 방식 선택 대신 곡명 검색으로 시작한다. 검색 결과에서 기존 곡을 연결하거나 원하는 곡이 없을 때 새 곡 추가로 진행한다. 검색 실패는 빈 결과로 취급하지 않는다. 기존 곡은 곡명과 원곡 가수를 요약하고, 새 곡에만 곡명·장르·원곡 가수 편집을 제공한다. 곡 변경은 다른 곡 검색으로 돌아가며 가창 참여자 입력을 유지한다. 곡 정보 fieldset은 가창 참여자와 같은 패딩과 그룹 간격을 사용한다.

유튜브 다시보기 탭은 공식 유튜브 다음에 배치하며 등록된 youtube_vod 전용 채널의 일반 영상을 최신순으로 표시한다. 다섯 탭은 한 줄로 배치하고 좁은 화면에서는 가로 스크롤하며, 키보드 또는 클릭으로 선택한 탭을 보이는 영역으로 이동한다. 멤버 필터 변경 시 20개부터 다시 조회하고 더 보기로 저장된 영상을 추가한다. 미등록·초기 수집·수집 중지·부분 실패·조회 실패를 구분해 안내한다. 다시보기 채널은 기존 공식 채널 수집 소스를 공유하되 전용 채널만의 과거 Shorts 탐색은 수행하지 않는다.

노래 클립은 곡 검색과 동일한 제목·검색행·접이식 필터·적용 조건 배지·카드/표/그리드 보기 선택기를 사용한다. 보기 설정과 결과 컴포넌트를 공유하고 목록 끝에서 다음 페이지를 불러온다. 필터는 가창 멤버 한 명과 방송일 범위 또는 방송일 미확인으로 구성한다. 같은 곡의 여러 클립은 가창 ID별로 유지하며 상세 링크와 재생·대기열은 해당 클립을 선택한다. 원곡 가수는 기존 카탈로그 조회값을 표시하고 방송 근거는 카드와 클립 상세에서 확인한다.

노래 클립의 모든 결과 보기(카드·표·그리드)는 클리퍼(현재 표시·재생되는 클립 source의 업로드 채널명)와 방송일을 항상 표시한다. 가창 멤버나 원곡 가수를 클리퍼로 사용하지 않으며, 방송일은 검수된 performedOn만 사용하고 업로드일·카탈로그 게시일로 대체하지 않는다. 누락 시 클리퍼 미확인·방송일 미확인으로 표시한다. 카드와 그리드의 클리퍼명은 썸네일 좌측 상단의 대비 높은 칩으로 표시하고 본문에서는 중복 표시하지 않는다. 긴 칩 이름은 말줄임하며 전체 이름을 title로 제공한다. 카드의 가창일은 상단 칩 그룹에서 크기·굵기·배경색으로 강조하고 게시일 칩과 본문 날짜의 중복 표시는 제거한다. 검수된 performedOn이 없으면 가창일 미확인으로 표시하며 업로드일로 대체하지 않는다. 그리드의 방송일은 본문에 유지한다. 표는 클리퍼·방송일 별도 컬럼에 표시하며 좁은 화면에서도 숨기지 않고 가로 스크롤로 확인할 수 있게 하며 긴 채널명은 줄바꿈한다.

곡 검색과 클립 표의 썸네일은 112px 너비·16:9 비율로 확대한다. 클립 표는 클리퍼 150px, 방송일 112px의 별도 컬럼과 최소 960px 표 너비를 사용한다. 좁은 화면에서는 표 영역만 키보드 및 터치 가로 스크롤을 제공하며 데이터 컬럼을 카드형으로 합치지 않는다.

카탈로그의 가창·곡 영구 삭제 확인창은 승인 제안과 작업 이력이 보존됨을 안내한다. 회원의 승인 제안 상세에서는 연결된 가창 삭제 여부를 표시하고 삭제된 항목의 카탈로그 링크를 숨긴다.

Play 채널 목록은 각 행에 수정과 삭제 작업을 함께 표시한다. 삭제 확인창은 대상 채널명과 Play 등록 정보 삭제 범위를 명시하며, 연결된 영상·수집 설정의 보호와 YouTube 원본 채널 유지 여부를 안내한다.

신규 곡 등록의 AI 제안은 실행·구간 선택·분석 상태를 한 행에 모으고 안내와 항목별 상세는 기본 접힘으로 제공한다. 일괄 적용·적용 상태·되돌리기를 함께 배치하며 경고는 항상 표시한다. 상세 항목은 넓은 화면에서 두 열, 좁은 화면에서 한 열로 표시하고 제안값 요약을 제공한다.

검수 목록은 미완료 항목을 먼저, 등록 준비 완료 항목을 맨 뒤에 표시한다. 각 그룹 안에서는 발견 시각·ID 내림차순을 유지하고 다음 페이지도 같은 순서를 따른다. 공식 영상·노래 클립의 AI 자동 채우기·일괄 적용·개별 적용은 사용자가 ‘구간 선택’을 켰을 때만 시작·종료 위치를 반영한다. ‘지정 구간만 분석’은 AI 입력 범위이며 재생 구간 반영을 허용하는 선택과 구분한다. 구간 선택을 끄면 전체 영상으로 돌아가고 AI가 선택을 자동으로 켜지 않는다.

검수 페이지의 목록 복귀·저장 버튼은 본문 길이와 스크롤 위치에 관계없이 관리자 콘텐츠 영역의 화면 하단에 고정한다. 사이드바를 침범하지 않으며 모바일 안전 영역을 확보한다. 푸터 높이만큼 본문 여백을 예약해 마지막 입력란과 키보드 초점이 가려지지 않게 한다.

공식 영상·노래 클립 등록은 AI 제안 앞에 업로드 채널의 상태와 해결 행동을 표시한다. 같은 탭의 대상 채널 전용 화면으로 이동하며 미등록 채널 ID와 용도를 전달하고 서버에서 표시명을 조회한다. 등록은 검수 대기·비활성으로 완료하고 해당 채널의 승인 편집으로 이어진다. 영상 사용 허용과 승인은 명시적으로 저장하며 자동 수집은 별도 접이식 설정으로 유지한다. 업로더와 가창자는 자동 연결하지 않는다.

채널 관리 왕복은 신규 등록 작업을 종료하지 않는다. 등록 단계·입력·AI 분석 범위·작업·선택·적용 및 되돌리기 이력을 메모리에 보존하고 원래 목록 검색 조건으로 복귀한다. 버튼과 브라우저 뒤로/앞으로 이동에 같은 규칙을 적용한다. 복귀 시 카탈로그와 영상 조건을 다시 조회하며 실패하면 재확인 전까지 진행·저장을 차단한다. 정확한 대상 채널 왕복만 곡 초안 이탈 경고에서 제외하고 채널 폼 자체의 변경은 보호한다. 새로고침 복구는 지원하지 않으며 초안 없는 복귀 URL은 일반 채널 관리로 표시한다.

### 편성표 디자인 병행 제공

시간순 보기의 기본 다운로드·복사는 확정된 720px 포스터를 유지한다. 구분선 아래의 ‘기존 편성표 다운로드 / 기존 편성표 복사’는 별도 레거시 렌더러를 사용한다. 그리드에는 이 메뉴를 표시하지 않는다. 공개 디자인 선택은 저장하지 않으며, 출력 시작 시점의 날짜·보기·실제 테마를 사용한다. 이미지 생성 중에는 출력 메뉴 전체를 비활성화한다.

기존 편성표는 콘텐츠 520px와 좌우 12px 여백, 둥근 헤더와 민트 상단선, 투명 로고, 전체 날짜·요일·최종 편집 시각, 시간/멤버·일정 2열 표로 구성한다. 멤버 아바타는 원형이며 시그니처 색 테두리를 사용한다. 게릴라·미정·휴방·일정 없음은 하단 독립 패널로 표시하며 실제 제목과 사유·명시된 시간을 보존한다. 다크는 검정/짙은 회색/민트, 라이트는 밝은 회색/흰색/짙은 청록을 사용한다. 두 편성표 모두 임베딩된 Poppins·Pretendard와 기존 시스템 폰트 대체·이미지 준비·PNG 생성 경로를 공유한다. 레거시 CSS는 `.snapshot-legacy` 아래로 제한한다.

스냅샷의 `design=poster|legacy`는 생략하거나 잘못되면 poster로 처리하고, grid에서는 무시한다. 관리자 편성표 미리보기의 포스터/기존 편성표 선택은 URL 상태로 날짜·테마·새 탭 및 이전 관리자 주소 리다이렉트에 보존한다. 출력 폭과 여백은 실제 PNG와 미리보기가 같은 정의를 사용한다. 레거시 파일명은 `오버더월 스케쥴-기존편성표-YYYY-MM-DD.png`이며 API·DB·사용자 설정 저장·자동 제거 기능은 추가하지 않는다.

### 최종 문구 및 검토 정리 (2026-09-16)

- 공개 메인 버튼은 ‘이미지 다운로드’를 유지하고 기본 다운로드 메뉴 항목만 ‘이미지 다운로드 (Beta)’로 표시한다.
- 포스터의 시간 확정 건수·상태별 건수·일정 없음 인원 표시는 제거한다. 레거시의 집계 표시는 유지한다.

## 애니메이션 설정

우측 플레이어의 버튼은 누르는 즉시 0.9배 피드백을 주고 160ms ease-out으로 복귀한다. 재생 아이콘과 슬라이더 값에는 전환 지연을 두지 않는다. 재생 표시는 playing 상태에서만 움직이고 일시정지 시 멈춘다. 볼륨 팝오버는 트리거를 기준으로 열리고 Escape 시 트리거로 포커스를 돌려준다. OS 동작 줄이기와 사이트 애니메이션 끄기를 따르며 호버는 정밀 포인터에만 적용한다. 화면 전환과 곡 변경에서 동일한 영상 호스트를 유지한다.

프로필(게스트 포함) 메뉴의 `애니메이션 활성화` 스위치로 사이트 전체의 애니메이션을 제어한다. 기본값은 켜짐이며 사이트 스위치가 켜져 있고 OS의 동작 줄이기가 꺼져 있을 때만 애니메이션을 허용한다. OS 설정 변경은 즉시 반영하되 저장된 사이트 선택과 스위치 표시는 유지한다. 선택은 현재 브라우저에 저장하고 다른 탭에도 반영한다. 꺼짐은 CSS 전환·키프레임, Motion 효과, 버튼 효과와 추천 캐러셀 자동 넘김에 적용하며 입력·탐색 기능은 유지한다. 외부 임베드 내부의 효과는 제어하지 않는다.


### 플레이어 영상·파형·정렬 (2026-09-17)

영상과 조작부는 별도 카드 배경·둥근 하단 모서리를 사용하지 않고 하나의 중립 표면으로 이어진다. 데스크톱 좌우 여백은 8px이며 영상 아래 곡 정보·타임라인·재생 및 볼륨 조작을 순서대로 배치한다.

재생 조작부의 실제 사용 가능 폭이 340px 이하이면 버튼 간격을 2px, 주 재생 버튼을 48px로 줄이고 이전·다음 아이콘도 함께 축소한다. 모든 버튼의 터치 영역은 최소 44px로 유지한다. 287px 이하에서는 이전·재생·다음을 첫 줄, 랜덤·반복·볼륨을 둘째 줄로 배치해 우측 잘림을 방지한다.

헤더의 텍스트 탭은 모바일과 같은 Discover·노래 클립·플레이리스트 아이콘을 이름 앞에 표시한다. 넓은 화면에서는 18px 아이콘과 6px 간격, 좁은 화면에서는 기존 20px 아이콘 전용 버튼을 사용해 화면 크기가 바뀌어도 같은 목적지를 인식할 수 있게 한다.

[Skiper2 공개 음악 미리보기](https://skiper-ui.com/v1/skiper2)를 참고해 제목 오른쪽에 파란색(#60a5fa) 3px 막대 6개, 2px 간격, 18px 높이의 파형을 자체 구현한다. 실제 YouTube playing 상태에만 반응하고 일시정지·버퍼링·오류에서는 점 형태로 멈춘다. 오디오 스펙트럼 분석 결과가 아닌 재생 상태 표시다. OS 동작 줄이기와 사이트 애니메이션 설정을 따른다. 볼륨 동작 참고는 [Apple Music 웹 사용자 가이드](https://support.apple.com/en-gb/guide/music-web/apdmfdca8f52/web)이며 볼륨과 음소거는 기존 YouTube IFrame API에 직접 전달한다.


랜덤 재생은 표시 목록의 순서를 바꾸지 않는 토글이다. 현재 곡과 이번 회차에서 재생한 곡, 재생 불가 곡을 제외하고 다음 곡을 무작위로 고른다. 전체 반복은 새 회차를 시작하고 반복 꺼짐은 회차 종료 시 멈춘다. 자동 종료의 한 곡 반복은 유지하며 랜덤 끄기는 목록 순서 재생으로 돌아간다. 현재 곡은 강조색 제목·왼쪽 표시선·aria-current로 표시하고 긴 목록에서는 목록 안에서 현재 항목으로 스크롤한다. 이전 버튼은 기존 목록상 이전 곡 탐색을 유지한다.

좁은 화면의 카탈로그 복귀·Escape·소형 플레이어 접기는 표시 상태만 변경한다. 동일한 YouTube 영상과 재생 상태를 유지하며, 다시 펼쳐도 일시정지 상태를 임의로 재개하지 않는다. 재생 중지는 명시적인 일시정지·큐 비우기·편집 화면 진입 등 재생 제어에 한정한다.

OTW Play 헤더는 모바일에서도 한 줄을 유지한다. 실제 헤더 폭 767px 이하에서는 왼쪽 OTW Play 제목을 유지하고 Discover·노래 클립(관리자)·플레이리스트를 우측 44px 아이콘 내비게이션으로 전환한다. 검색 버튼도 같은 크기로 이어서 배치한다. 각 아이콘은 접근성 이름과 호버·키보드 초점 툴팁을 제공하며 현재 페이지를 표면·색으로 구분한다. 검색 버튼은 페이지 이동 없이 바로 아래에 검색 입력을 펼치고 초점을 옮긴다. Escape·외부 누름·초점 이탈로 닫으며 Escape는 검색 버튼으로 초점을 복귀한다. 모바일에서는 곡 제안 메뉴와 입력·내 제안 UI를 제공하지 않으며 직접 주소 접근은 PC 이용 안내와 OTW Play 복귀 링크만 제공한다.

접힌 휴대폰 플레이어는 제목·가창자를 누르는 펼치기 버튼과 독립된 재생/일시정지 버튼을 제공한다. 전체·미니·접힌 플레이어는 동일한 재생 상태 기반 파형 및 재생 버튼 컴포넌트를 사용한다. 처음 재생할 때 전체 플레이어를 열되, 한 번 소형으로 접은 뒤에는 재생 재개·자동 다음 곡·카탈로그의 다른 곡 선택에서도 탐색 화면을 유지한다. 제목을 명시적으로 눌러 전체 플레이어를 열 수 있다. 큐가 비면 이 표시 선택을 초기화한다. 곡 전환은 같은 YouTube 호스트를 사용하며 화면 펼치기와 독립적이다.

1280px 미만에서 소형↔전체 플레이어 전환은 Anime.js WAAPI로 280ms 동안 표면의 클립 영역과 제목·가창자 위치 및 균일 배율을 연결한다. 내부 영상·헤더·진행바·컨트롤·큐는 최대 90ms 시차와 160ms 등장 효과를 사용한다. 닫기는 같은 소형 위치로 돌아가며 즉시 입력·접근성 대상을 소형 플레이어로 넘긴다. 빠른 반대 입력은 현재 프레임에서 이어지고, 화면 크기 변경·편집 진입·큐 비움·곡 변경·언마운트는 효과를 정리한다. 사이트 애니메이션 끄기, OS 동작 줄이기와 키보드 입력에서는 즉시 전환한다. 전환 중에도 단일 YouTube 호스트와 재생 상태를 유지한다.

데스크톱 OTW Play 헤더는 제목과 탐색 탭을 16px 간격으로 왼쪽에 묶고, 검색 입력과 곡 제안을 오른쪽에 배치한다. 검색은 가용 폭에 따라 최대 360px, 탭은 높이 36px, 검색·제안 버튼은 높이 44px로 두고 세로 중심을 정렬한다. 사이드바를 제외한 실제 헤더 폭이 960px 이하이면 검색 입력을 44px 버튼으로 접어 줄바꿈을 방지한다. 검색 입력에는 장식용 돋보기 하나와 검색어가 있을 때만 초기화 버튼을 표시한다. 초기화는 페이지 이동 없이 입력·빠른 검색 결과를 비우고 입력 초점을 유지한다. Enter와 전체 결과 보기는 기존 검색 화면으로 이동한다. 아이콘 내비게이션에서는 좌우 8px 여백과 버튼 사이 4px 간격을 사용한다.

플레이어는 첫 재생 전 현재 영상 썸네일을 표시하고 playing 상태에서 제거한다. 이후 일시정지·버퍼링에서는 영상 화면을 유지하며 썸네일 전환으로 YouTube 호스트를 재생성하지 않는다.


### OTW Play 탐색 탭과 상호작용 (2026-09-17)

공개 Play 헤더는 넓은 화면에서 64px 높이와 좌우 20px 여백을 사용한다. 탭은 36px 높이, 좌우 12px 여백, 2px 간격의 중립 트랙으로 묶는다. 선택 배경은 Anime.js WAAPI로 200ms ease-out 이동하며 URL의 현재 링크를 따른다. 최초 표시·검색 등 탭 밖의 화면에서 복귀·반응형 크기 변경은 즉시 정렬하고, 연속 탐색은 진행 중인 위치에서 새 선택으로 이어진다. 탭 이동 자체를 애니메이션 완료까지 지연시키지 않는다.

실제 헤더 폭 767px 이하에서 아이콘 전용 너비 44px·높이 36px 메뉴와 툴팁을 유지한다. 359px 이하에서는 워드마크의 장식 음표를 숨기고 글자 15px·좌우 4px 여백·탭 사이 0px로 줄여 280px 화면에서도 검색과 탐색을 한 줄에 제공한다. 탭 높이는 좁은 화면과 터치 환경에서도 요청한 36px을 적용하며, 검색 버튼은 44px을 유지한다.

Play의 공통 버튼 아이콘 피드백도 Anime.js를 사용한다. 240ms로 반응하고 반복 입력·언마운트에서 이전 효과를 정리한다. 독자적인 피드백을 가진 플레이어에는 중복 적용하지 않는다. 기존 사이트 설정 계약에 따라 프로필의 애니메이션 활성화 스위치가 효과를 제어하며, 끄면 선택 표시와 입력·탐색은 즉시 반영된다.


### 플레이리스트 히어로 전환 (2026-09-17)

[Skiper80 공개 미리보기](https://skiper-ui.com/v1/skiper80)의 이미지 연결 방식을 참고해 자체 구현한다. Discover·플레이리스트 목록에서 기본/개인 카드를 열면 해당 이미지가 상세 헤더의 표지로 이어지고, 목록 복귀 버튼·상단 메뉴·브라우저 뒤로 가기에서는 같은 카드로 돌아간다. Anime.js WAAPI의 360ms ease-out으로 위치·크기·모서리·이미지 크롭과 카드의 어두운 표면을 함께 전환한다. 제목도 이미지와 동일한 360ms 타이밍으로 위치와 글자 크기가 이어진다. 출발·도착 제목의 글꼴·두께·색상·정렬·줄바꿈을 각각 보존한 텍스트를 균일 확대/축소하며 교차 전환해 긴 제목이 도중에 갑자기 재배치되지 않게 한다. 원래 제목은 레이아웃과 접근성 트리에 유지하고 전환 종료·중단 시 시각 표시를 복구한다. 본문·URL·버튼은 전환 완료를 기다리지 않고 정상 탐색 흐름을 유지한다.

전환 레이어는 Play 콘텐츠 영역 안에만 표시하고 포인터·보조기술 입력 대상에서 제외한다. 플레이어와 YouTube 호스트는 유지한다. 목록에서의 세로 스크롤 및 Discover의 가로 스크롤 위치를 기억하며, 복귀 시 선택 카드가 보이도록 복원한다. 개인 목록의 늦은 로딩도 실제 이미지가 나타날 때 연결하되 1.2초 내 도착하지 않으면 임시 레이어를 정리하고 기존 로딩/오류 화면을 유지한다. 삭제되어 돌아갈 카드가 없거나 다른 화면으로 이동하면 효과를 정리한다.

직접 상세 주소로 진입하거나 키보드로 활성화한 링크, 애니메이션 비활성 설정에서는 이동 효과 없이 탐색한다. 효과를 끄거나 연속 이동·스크롤·화면 크기 변경·언마운트가 발생하면 숨긴 이미지를 복구하고 임시 레이어를 제거한다. 원본 소스나 Pro 라이선스 자산은 사용하지 않는다.
