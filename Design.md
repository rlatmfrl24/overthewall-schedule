# OTW Schedule Design Guide

이 문서는 현재 구현된 OTW Schedule의 화면, 컴포넌트, 색상, 접근성 기준을
정리한 디자인 가이드입니다. 새 화면을 만들거나 기존 화면을 개선할 때는 이
문서를 먼저 확인하고, 변경 후 문서도 함께 갱신합니다.

## 기준 파일

| 영역 | 파일 |
| --- | --- |
| 전역 토큰 | `src/index.css` |
| 공개 앱 프레임 | `src/app/layout/app-shell.tsx` |
| 공개 내비게이션 | `src/app/layout/app-navigation.ts` |
| 콘텐츠 페이지 헤더 | `src/shared/ui/content-page-shell.tsx` |
| 사이트 푸터 | `src/app/layout/footer.tsx` |
| UI primitive | `src/shared/ui/*` |
| 일정 입력 | `src/features/schedules/ui/schedule-dialog.tsx` |
| 공지 배너 | `src/features/notices/ui/notice-banner.tsx` |
| 관리자 레이아웃 | `src/app/admin/admin-layout.tsx` |

## 제품 톤

- 공개 화면은 팬 사이트다운 친근함과 빠른 스캔성을 함께 가져간다.
- 관리자 화면은 장식보다 밀도, 정렬, 상태 확인을 우선한다.
- 멤버 색상은 감성 요소가 아니라 정보 accent로 쓴다.
- 플랫폼 고유색은 아이콘, 작은 배지, 상태 강조에만 제한한다.
- 다크모드에서는 강한 블루 선택색을 쓰지 않는다. 사이드바 활성 항목은 흰색
  배경과 어두운 텍스트를 사용한다.

## 앱 프레임

- 일반 공개 화면은 `PublicAppShell`을 사용한다.
- `lg`에서는 접힌 64px 사이드바, `xl`에서는 256px 사이드바를 사용한다.
- `/multiview`는 iframe 폭이 breakpoint에서 역으로 줄어들지 않도록 `lg` 이상에서도
  64px 접힌 사이드바를 유지한다.
- 모바일은 56px 상단 헤더와 `Sheet` 메뉴를 사용한다.
- 사이드바 상단 brand 영역과 콘텐츠 헤더는 데스크톱에서 64px 높이로 라인을
  맞춘다.
- 사이드바 푸터는 펼친 상태 56px, 접힌 상태 88px를 기준으로 하며, 접힌
  상태의 테마와 사용자 컨트롤 사이에 8px 간격을 유지한다.
- 사이트 푸터는 56px 기준의 보조 정보 영역이다.

## 콘텐츠 페이지

공지사항, VOD & 클립, 멤버 게시글처럼 콘텐츠를 탐색하는 화면은
`ContentPageShell`을 사용한다.

- 헤더 divider는 좌우 여백 없이 전체 폭을 채운다.
- 헤더는 스크롤 컨테이너 밖에 고정되어 스크롤 위치에 흔들리지 않는다.
- 헤더 내부 콘텐츠는 `max-w-screen-2xl`과 동일한 좌우 패딩 체계를 따른다.
- 페이지 본문은 `px-3 sm:px-5 lg:px-7 xl:px-8`, `gap-5`, `pb-10` 흐름을
  기본으로 한다.

## 화면별 패턴

### 관리자 X 수집 운영

- 멤버 게시글 관리의 X 탭은 게시물 수집과 답글·인용 원문 보강 상태를 상단의
  두 카드로 비교한다. 넓은 화면에서는 나란히, 좁은 화면에서는 세로로 배치한다.
- 실행 이력은 설정보다 먼저 표시하고 전체 결과, 게시물 수집, 원문 보강을
  구분한다. 행을 펼치면 계정 묶음별 저장 결과와 보강 오류·재시도 시각을 보여준다.
  작업 묶음 진행률은 원문 보강 완료율로 해석하지 않는다.
- 현재 전체 대기량은 특정 실행의 처리량과 구분한다. 원문·작성자 및 검토·연결·
  이월 건수는 중복되거나 집계 단위가 다르므로 합산 비율을 만들지 않는다.
- 전체 X 예산과 그 안에 포함되는 원문 보강 한도를 사용·예약·잔여 금액 및
  UTC 기준일과 함께 표시한다. 예산 대기는 게시물 수집 실패와 구별한다.
- 공개·피드, 게시물 수집, 원문 보강 설정은 하단의 접이식 영역으로 제공한다.
  카드의 설정 버튼은 해당 영역을 펼치고 summary에 키보드 포커스를 이동한다.
- 조회 실패·오래된 결과·과거 기록 누락은 명시한다. 미확인 값을 0이나 정상으로
  표시하지 않으며, 다음 처리 가능 시각을 실행 예약 시각으로 표현하지 않는다.

### 오늘의 스케쥴

- 멤버 카드가 홈 화면의 핵심이다. 아바타, 멤버명, 그룹칩, 라이브 상태가 먼저
  읽혀야 한다.
- 그룹명 칩은 카드 우측 상단에 배치해 카드 높이를 절약한다.
- 라이브 표시는 아바타와 통합하고, hover 시 시청자 수를 보여준다.
- schedule card는 좌측 flag divider 없이 상태칩, 시간, 제목만으로 읽히게 한다.
- 카드 텍스트는 WCAG 대비와 모바일 줄바꿈을 기준으로 조정한다.

### 주간 스케쥴표

- 주간 화면은 테이블형 정보 탐색이 우선이다.
- sticky 요일 헤더와 멤버 열을 유지한다.
- schedule item은 시간, 상태, 제목 순서로 읽히게 하고 과한 장식은 피한다.
- 다크모드에서는 미묘한 실선이나 겹친 배경이 보이지 않도록 투명도 중첩을
  줄인다.

### VOD & 클립

- 플랫폼별 콘텐츠는 같은 섹션 헤더, 날짜 그룹, 카드 밀도를 공유한다.
- 미디어 카드는 썸네일 비중을 크게 두고 제목은 2줄 안에서 끊는다.
- 더보기/접기 버튼은 outline surface, 충분한 높이, 명확한 텍스트 대비를
  유지한다.

### 멤버 게시글

- VOD와 같은 콘텐츠 페이지 간격과 헤더 체계를 사용한다.
- 게시글 카드는 읽기 중심의 넓은 리스트로 유지한다.
- X와 Naver Cafe는 같은 골격을 쓰고 플랫폼 차이는 아이콘/작은 상태 요소에만
  드러낸다.
- X 게시글 카드 내부는 아바타, 이름·핸들·시각, 본문, 문맥 프리뷰, 미디어,
  반응 푸터 순서를 유지하되 `p-3 sm:p-4` 패딩과 10px 전후의 간격으로 밀도 있게
  구성한다. 멤버 퍼스널 컬러는 카드 좌측 accent 막대로 표시한다.
- 답글은 대상 게시글을 작은 문맥 카드로 먼저 보여주고 실제 답글을 주 콘텐츠로
  표시한다. 본문 앞의 답글 대상 멘션은 반복하지 않는다.
- 인용 게시글은 별도 상태 배지 없이 항상 중첩 프리뷰로 표시하며 긴 본문은
  3줄로 제한한다.
- X 반응 푸터는 Naver Cafe 카드처럼 좌측부터 답글, 재게시, 좋아요, 공유를
  작은 아이콘과 짧은 간격으로 배치한다. 재게시 수에는 일반 재게시와 인용을
  합산하고, 실제로 수행할 수 없는 반응 지표는 클릭 가능한 버튼처럼 표현하지
  않는다.

### 멀티뷰

- `/multiview`는 화면 높이가 우선인 특수 화면으로, 사이트 푸터를 숨기고 남은
  높이를 iframe에 배정한다.
- 상단에는 멀티뷰 제목과 초기화, 멤버 목록, 외부 `Mul.Live` 링크를 간결하게
  배치하고, 멤버 선택은 영상 위에 뜨는 overlay 패널에서 처리한다.
- 선택한 CHZZK 채널 조합은 반복 `c=` URL 상태로 보존하고, 같은 순서의 채널 ID를
  `Mul.Live` 경로로 만들어 하단 iframe에 표시한다. 채널은 최대 8개로 제한하고,
  URL 입력과 UI 선택 양쪽에서 같은 제한을 적용한다.
- 라이브 상태 자동 갱신, 멤버 목록 열기와 닫기 같은 호스트 UI 변경은 재생 중인
  `Mul.Live` iframe DOM을 재마운트하지 않는다. 채널 조합이 실제로 바뀐 경우에만
  iframe `src`를 갱신한다.
- `Mul.Live`와 그 안의 CHZZK 플레이어는 교차 출처이므로 호스트에서 내부 DOM이나
  넓은 화면 상태를 직접 조작하지 않는다. 불필요한 iframe 교체를 막아 내부 상태가
  초기화되는 경우를 최소화한다.
- iframe의 `fullscreen` 권한은 사용자의 직접 조작을 위한 것이며, 확장 프로그램,
  쿠키 브리지, 키보드 이벤트 주입으로 화면 모드를 자동 변경하지 않는다.

### OTW Play

- 현재 `/play/*` UI는 운영 공개 전 관리자 preview다. 비로그인·비관리자는
  config·catalog 요청 없이 로그인 또는 권한 안내만 보고, 익명 public GET 계약은
  장래 공개 전환을 위해 유지한다.
- `/play/*`는 `PublicAppShell` 안에서 남은 viewport를 사용하고 사이트 푸터를
  숨긴다. 제품 내부에 두 번째 고정 사이드바를 만들지 않는다.
- 상단 탐색은 `발견`, `곡 검색` 두 항목으로 제한한다. 오리지널과 커버 구분은
  곡 검색 화면의 관계 필터에서 제공하며 모바일에서는 같은 두 항목을 가로
  스크롤 가능한 sticky tab으로 제공한다.
- 발견은 첫 페이지의 최대 8곡을 16:9 대표 배너로 표시한다. 그 아래에는 모든
  현재 멤버의 메인 보컬 곡 진입점과 최근 공개곡 compact table을 순서대로 둔다.
  최근 곡은 24곡 단위로 내부 스크롤에서 이어서 불러오며, 로딩 오류나 자동 감지
  미지원 시에도 명시적인 더 불러오기·다시 시도 버튼을 제공한다.
- 곡 검색의 상세 필터는 모든 화면에서 기본적으로 접어두고, 적용된 조건은 필터를
  펼치지 않아도 칩으로 확인·해제할 수 있게 한다. 멤버를 모두 해제하면 멤버 조건도
  함께 해제한다.
- 곡 목록은 썸네일 16:9, 곡명, 원곡 가수, 대표 가창과 버전 수를 읽기 쉬운
  행으로 표시한다. 저장 playlist처럼 보이는 library affordance는 만들지 않는다.
- 데스크톱은 우측 380px `PlayerQueuePanel` 하나를 사용한다. 단일 iframe은 panel
  상단에 356×200px로 항상 보인다. iframe 아래에는 곡명과 현재 멤버 profile·외부 person·group
  icon 및 참여자 이름을 먼저 두고 YouTube·곡 상세 action을 같은 row에 배치한다. 음악/가창
  분류는 그 아래 보조 metadata로 내린다. 실제 IFrame 위치를 반영하는 seekable progress와
  진행/남은 시간 다음에 상태 문구 없는 previous/play/next와 반복·셔플 control row를
  둔다. 볼륨 버튼은 세로 슬라이더와 음소거 버튼을 담은 접근 가능한 popover를 연다.
  가창 관계·공개 유형·참여 형태·게시일은 작은 배지로 표시하고, 게시 채널은 transport
  아래의 작은 YouTube source attribution으로 분리한다.
  플레이큐는 같은 panel의 남은 높이를 사용해
  player 아래에서 독립 스크롤한다. 하단 재생바, player 접기·펼치기와 overlay
  상세 panel은 만들지 않는다.
- 데스크톱 viewport 높이가 720px 미만이면 게시 채널 출처와 여백을
  먼저 줄이되 참여자 identity·이름과 YouTube·곡 상세 action은 한 줄로 유지한다.
  iframe 200px과 플레이큐 최소 144px을 보존한다. 높이 640px 미만에서는 단일 356×200px iframe을
  계속 보인 채 `현재 재생`과 `플레이큐` 상세 영역을 명시적으로 전환한다. 전환은
  재생을 멈추거나 iframe을 재생성하지 않으며 rail 바깥 document scroll 대신
  queue 내부 scroll을 사용한다.
- 1280px 미만에서는 재생 의도 직후 별도 전체 화면 `Now Playing`으로 전환한다.
  16:9 iframe, 곡·참여자 정보, 재생 조작, 볼륨과 세션 플레이큐를 한 화면에서
  제공한다. 640–1279px에서 카탈로그로 돌아가면 같은 iframe을 200×200px로 유지하는
  우측 하단 216px miniplayer로 축소하며 pause하지 않는다. miniplayer는 곡명,
  play/pause와 전체 화면 확장만 제공하고 queue 이동에도 전체 화면을 강제로 열지 않는다.
  640px 미만에서만 복귀 전에 pause하고 launcher에서 player를 다시 열어 재개한다.
  iframe을 복제하거나 숨긴 상태로 재생하지 않는다.
  중앙과 queue는 document 대신 각자 내부 스크롤한다.
- 현재 멤버는 오시마크와 작은 member accent를 사용하고 외부·전 소속은 중립
  칩, 그룹은 보조 라벨을 사용한다. 색상만으로 종류를 구분하지 않는다.
- config가 꺼진 준비 중 상태와 catalog 동기화 `503`을 빈 결과와 구분한다.

### 프로필과 스냅샷

- 프로필과 스냅샷은 앱 크롬을 숨기는 특수 화면이다.
- 프로필은 이미지와 멤버 accent를 적극적으로 사용한다.
- 스냅샷은 공유 이미지 생성용 비상호작용 화면이므로 고정 폭과 안정적 렌더링을
  유지하되, 조작을 위한 여백과 hover 전용 공간은 두지 않는다.
- 스냅샷 헤더는 로고, `오늘의 편성표` 제목과 날짜, 우측의 최종 편집 일시를
  한 행에 배치한다. 별도의 `OTW Schedule` 브랜드 문구는 표시하지 않는다.
- 최종 편집 일시는 조회 시각이 아니라 사용자에게 공개되는 일정 데이터가 실제로
  생성, 수정, 삭제된 시각이다. 수동 편집, 승인 반영, 자동 라이브 반영은 포함하고
  승인 대기 후보 수집과 거부처럼 공개 일정이 바뀌지 않은 처리는 제외한다.
- 그리드 스냅샷의 멤버 카드는 프로필, 멤버명, 그룹명을 한 줄 헤더에 배치하고
  고정 최소 높이와 프로필 오버랩을 피한다. 일정이 적은 카드도 불필요하게 늘리지
  않고 각 카드의 콘텐츠 높이에 맞춘다.
- 그리드 스냅샷의 일정 카드는 실제 오늘의 스케쥴 카드와 상태, 시간, 제목 표현을
  공유한다. 휴방과 게릴라는 시간을 표시하지 않고, 방송 일정만 지정 시간 또는
  `미정`을 표시한다.
- 타임라인 스냅샷은 방송 일정을 시간순 표로 보여주고 휴방, 게릴라, 미정,
  일정 없음은 구분된 보조 그룹으로 표시한다.

### 관리자

- 관리자 화면은 기본적으로 `AdminLayout`과 `AdminSectionHeader`를 기준으로 하되,
  화면 높이가 중요한 프리뷰형 화면은 패널 헤더가 섹션 헤더 역할을 대신할 수 있다.
- 필터는 form grid, 결과는 table/card 조합, 수정은 dialog를 사용한다.
- 위험 액션은 confirm dialog와 toast를 함께 사용한다.
- 운영 대시보드는 `지금 확인할 것 → 수집 상태 → 자원 및 한도 → 정기 작업 상태 →
  보존·정리` 순서를 사용한다. 현재 조치가 필요한 이슈와 Queue를 시간순 로그보다
  먼저 두고, 정상 상태에서는 짧은 상태 바로 축약한다.
- Cloudflare D1의 실제 rows read/written과 scheduler의 보수적 admission 예상치는
  별도 카드, 제목, 단위로 표시한다. 실제 사용량 공급자 조회 실패는 운영 장애나
  scheduler 차단으로 승격하지 않는다.
- 정기 작업은 작업 종류별 최신 판정을 기본 탭으로 사용하고 전체 시간순 이력은
  보조 탭에서 필요할 때만 조회한다. `대상 없음`, `동시 실행 병합`, `아직 실행
  시각 아님`은 회색 중립 상태로 표현한다.
- 파괴적인 데이터 정리는 기본 화면에서 감추고 `고급 작업 · 데이터 정리` disclosure
  안에 배치하며 기존 재확인 절차를 유지한다.
- 스냅샷 프리뷰처럼 상태 확인이 주목적인 화면은 좌측의 좁은 옵션 패널과
  우측의 넓은 미리보기 패널로 나누고, 보조 설명보다 렌더 결과를 우선한다.
- 관리자 스냅샷 프리뷰는 실제 `/snapshot` 출력 폭과 내용을 바꾸지 않고,
  프리뷰 패널 안에서만 축소 비율을 적용해 화면에 맞춰 보여준다.

## 색상과 토큰

- 배경/전경/카드/경계는 `src/index.css` semantic token을 우선한다.
- OTW brand accents는 `--otw-1`, `--otw-2`, `--otw-3`를 사용한다.
- 사이드바 활성 상태:
  - light: `--sidebar-primary` 기반의 어두운 선택면.
  - dark: 흰색 선택면과 어두운 텍스트.
- 멤버 `main_color`는 카드 헤더, 아바타 border, 작은 상태 accent에 사용한다.
- 멤버 `sub_color`는 낮은 opacity 배경 tint에만 사용한다.
- 멤버 색상 위 텍스트는 반드시 대비 계산을 거친다.

상태 색상 기준:

- LIVE: red 계열, 흰 텍스트, 작은 pulse dot.
- 방송: 멤버색 또는 teal 계열.
- 휴방: rose/zinc 계열.
- 게릴라: amber 계열.
- 미정: slate/zinc 계열.
- 공지: neutral surface + 아이콘/배지 강조.
- 이벤트: purple 계열은 작은 배지에만 제한.
- YouTube: red 계열.
- CHZZK: emerald/cyan 계열.

## 타이포그래피

- 기본 폰트는 `Inter`.
- 페이지 제목: `text-xl`-`text-2xl`, `font-semibold` 또는 `font-bold`.
- 카드 제목: `text-sm`-`text-base`, 중요 멤버명은 더 크게 허용.
- 메타 텍스트: `text-xs`-`text-sm text-muted-foreground`.
- 시간/숫자는 `tabular-nums`를 사용한다.
- display급 큰 글자는 프로필, 스냅샷, 홈 멤버명처럼 명확한 이유가 있을 때만
  쓴다.

## 컴포넌트 규칙

- 버튼은 `src/shared/ui/button.tsx` variant를 우선한다.
- 아이콘 버튼에는 `aria-label`을 반드시 둔다.
- 일반 카드는 `rounded-lg`/`rounded-xl`, `border`, `shadow-sm`를 기본으로 한다.
- 카드 안에 카드 중첩은 피하고, repeated item 또는 modal처럼 실제로 필요한
  경우에만 card surface를 쓴다.
- Hover는 색상, border, 작은 shadow 정도로 제한한다.
- 필터 chip은 짧은 라벨과 명확한 selected state를 유지한다.
- Empty/Loading/Error 상태는 모든 데이터 화면에 준비한다.

## 접근성

- 일반 텍스트 대비는 WCAG AA 4.5:1 이상을 목표로 한다.
- 큰 텍스트와 아이콘+텍스트 chip도 배경 대비를 확인한다.
- focus-visible ring은 제거하지 않는다.
- `aria-current`, `aria-expanded`, `aria-controls`, `aria-pressed`를 상태에 맞게
  사용한다.
- 클릭 가능한 `div` 대신 `button` 또는 `a`를 우선한다.
- 모바일 버튼과 chip은 터치하기 쉬운 높이를 유지하고 텍스트가 겹치지 않게 한다.

## 구현 체크리스트

- 기존 feature에 같은 패턴이 있는가?
- `PublicAppShell` 또는 `ContentPageShell`을 재사용할 수 있는가?
- 새 색상은 semantic token, 상태색, 멤버색, 플랫폼색 중 하나인가?
- light/dark mode에서 선택 상태와 focus 상태가 명확한가?
- 모바일에서 긴 문구가 버튼이나 카드 밖으로 넘치지 않는가?
- 로딩, 빈 상태, 에러 상태가 있는가?
- 접근성 label과 keyboard 동작이 있는가?
- 변경된 UI 규칙을 이 문서에 반영했는가?


## 통합 관리자 콘솔 (2026-09)

관리자가 상태를 읽고, 근거를 확인한 뒤, 같은 업무에서 처리하는 순서로 구성한다. 기능 조합과 주소 상태는 `src/app/admin/console-screen.tsx`가 담당하고, 승인·게시·수집 의미는 각 기능의 공개 인터페이스를 재사용한다.

| 메뉴 | 주소 | 주요 업무 |
| --- | --- | --- |
| 대시보드 | `/admin/operations` | 즉시 조치, 사람의 검토, 자동 처리 대기, 수집 상태 |
| 자동 수집 스케쥴 검토 | `/admin/review` | 일정 승인·거부 제외 |
| 수집·소스 | `/admin/collection` | X·네이버·일정·YouTube·키리누키 채널 |
| OTW Play | `/admin/otw-play` | 카탈로그·공개 관리, 자동 후보·사용자 제안·가져오기, 감시·승인 채널·재생 상태 |
| 콘텐츠 | `/admin/content` | 공지, D-Day, 스냅샷 |
| 자원·보존 | `/admin/resources` | X 포함 예산, YouTube 쿼터, D1 실사용·보호, 보존·이미지 |
| 실행·변경 이력 | `/admin/history` | 실행 근거, 일정 변경, 관리자 감사 |

- 업무 탭은 `tab`, 수집 공급자는 `source`를 사용한다. 검색 `q`, 결과 `state`, 분류 `category`, 페이지 `page`, 선택 `selected`, 정렬 `sort`를 URL에 보존한다. 기존 주소는 해당 업무로 연결하며 스냅샷 날짜·모드·테마와 기존 주요 해시를 유지한다.
- 데스크톱 메뉴 폭은 216px, 본문은 14px를 기준으로 한다. 단순 행은 약 36~40px, 여러 결과를 비교하는 행은 내용만큼 확장한다. 긴 설명은 근거 상세에 두고 제목·조작을 가로 스크롤 밖에 두지 않는다. 모바일 조작 영역은 44px 이상을 확보한다.
- 대시보드는 즉시 조치와 검토 대상을 먼저 표시한다. 다수의 조치가 검토 영역을 밀어내지 않도록 조치 목록은 독립 스크롤을 제공한다. 색상만으로 의미를 전달하지 않고 상태 라벨·원인·영향·다음 행동을 함께 제공한다.
- `확인 중`, `미확인`, `조회 실패`, `이전 정보`, `기록 없음`을 정상·0건과 구분한다. 마지막 조회 시각을 표시하고 5분 이상 지난 조회는 오래된 정보로 표시한다. 저장된 과거 오류는 현재 장애로 추측하지 않는다.
- 조회 버튼은 ‘상태 새로고침’, 외부 처리를 수행하는 버튼은 ‘지금 수집’ 또는 ‘전체 채널 갱신 실행’으로 표시한다. 새 화면을 위해 상시 API 폴링을 추가하지 않는다. 실행 중인 기존 작업 감시는 유지한다.
- X 전체 예산 안에 원문·작성자 보강 한도가 포함된다. 두 금액을 더하지 않는다. X 기준일은 UTC, YouTube 쿼터는 Pacific이며 D1 행 수나 금액과 합산하지 않는다. 실행 작업 묶음, 현재 원문 대기, 작성자 대기는 각각 다른 단위다.
- Play 검색과 게시 상태·분류 필터는 전체 카탈로그를 대상으로 적용한 뒤 페이지를 나눈다. 곡, 가창, 재생 가능 여부, 게시 상태를 별도로 표시한다. 검토 완료·임시 저장·게시·철회는 서로 다른 동작을 유지한다.
- 공지 관리는 미리보기 없이 전체 폭 목록으로 표시한다. 데스크톱은 내용·관련 멤버·첨부 요약 / 게시 상태·기간 / 작업 버튼을 같은 열에 정렬하고, 모바일은 세로로 배치한다. 대표 선택은 즉시 반영됨을 표시한다. 공개 공지 확인 및 이미지 상태·정리 링크는 공지 관리에 표시하지 않는다. 이미지 정리는 자원 화면에서 기존 대상 확인 절차를 유지한다.
- D-Day 생일의 저장 연도는 목록에서 노출하지 않고 ‘매년 M월 D일’과 다음 도래일을 보여준다. `9999` 저장값과 공개 계산 정책을 유지한다. 윤일의 평년 계산은 기존 `setYear` 달력 정책(3월 1일)을 따른다.
- 스냅샷 날짜·모드·테마는 상단 도구 모음으로 모으며, 출력 픽셀 크기와 화면 축척을 구분한다. 실제 스냅샷 iframe 렌더링을 유지한다.
- 상세 선택과 페이지 이동은 브라우저 이력에 남긴다. 변경 입력이 있는 편집에서는 계속 편집/변경 버리기 확인을 제공한다. 저장 실패 시 입력을 유지하고 폼 주변에 결과를 표시한다. 저장 뒤 관련 목록·홈 요약·이력을 무효화해 서버 결과를 다시 확인한다.

- 가져오기와 승인 채널은 기존 작업·등록 목록을 입력 폼보다 먼저 표시한다. 신규 등록은 해당 폼으로 이동하는 링크를 제공한다. 후보의 변경 예정 정보·판단 근거는 접어서 제공하고, 현재 상태와 다음 행동은 목록에 남긴다.
- 감사 로그는 저장된 `changes`의 변경 전·후 값을 비교한다. 값이 없었던 기록은 ‘기록 없음’, 명시적 null은 ‘미설정’, 빈 문자열은 ‘빈 값’으로 구분한다. 내부 이벤트·사용자 ID·IP·원문은 상세에서 확인한다.
- 모바일 모달도 포털 위치와 관계없이 44px 조작 영역을 적용한다. 실행 이력은 좁은 화면에서 카드로 전환해 상세를 읽는 동안 제목과 상태가 가려지지 않게 한다.

- OTW Play의 모든 관리 탭은 별도 메뉴 안에서 이동한다. 기존 통합 화면의 Play 주소는 검색·페이지·선택·해시를 유지해 해당 Play 탭으로 연결한다.
- 자동 처리 대기는 제목과 지표 영역을 분리하고 항목명·건수를 같은 기준선에 정렬한다. 확인 시각은 지표 아래에 둔다. 수집 상태 카드는 12px 내부 여백, 아이콘·제목·상세 링크 한 줄로 구성하며 중첩 패딩을 두지 않는다.

- 카탈로그 분류 옵션은 현재 검색 결과나 페이지와 무관하게 전체 활성 곡에서 구성한다. 선택한 분류는 결과가 없어도 유지한다.
- 별도 API로 조회한 수집 상태는 해당 조회의 실패 여부와 마지막 확인 시각을 함께 표시한다. 재조회 실패 시 이전 판정임을 명시하고, 성공한 빈 응답은 확인 중이 아닌 기록 없음으로 표시한다.
