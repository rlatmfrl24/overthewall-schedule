# 2026-09-10 후속: 멤버 필터 축소와 카드 내부 색상 선

final result: passed

- PC 필터를 280px에서 200px로 줄이고 초기화 버튼을 제거했다. 전체 멤버와 선택 칩 재클릭으로 선택 해제가 가능하다.
- 카드 내부 가상 요소로 3px 색상 선을 표시한다. 실제 `/feed`에서 left 4px, top/bottom 8px, 멤버 색상을 확인했다. 가상 요소이므로 이전 space-y 장식 요소의 여백 문제를 재현하지 않는다.
- 로컬 1280px 화면에서 필터 너비 200px, 초기화 버튼 0개, 긴 멤버 이름 표시와 카드 내부 선을 확인했다.
- 관련 테스트 30개 통과. 커밋·푸시는 하지 않았다.

# 2026-09-10 후속: 카드 출처·색상 보더·날짜·헤더

final result: passed

- 출처 탭과 상태를 제거하고 멤버 필터만 유지했다. 실제 로컬 피드에서 X 80개·네이버 카페 10개의 카드 출처 아이콘과 접근성 이름, 출처 탭 0개를 확인했다. 목록은 탭 패널 대신 이름 있는 region으로 제공한다.
- 내부 space-y 규칙의 영향을 받던 색상 장식 span을 제거하고 카드 자체 4px 왼쪽 보더로 바꿨다. 서로 다른 콘텐츠 높이의 카드에서 4px 보더와 멤버 색상이 적용되고 위아래 끝이 카드에 맞는 것을 확인했다. 기존 span을 기대하던 테스트도 실제 보더 검증으로 변경했다.
- 날짜는 달력 아이콘, 14px/semibold 글자, 구분 배경과 상하 10px 여백을 사용한다. 1280px 밝은/어두운 테마에서 확인했다.
- 헤더는 공간에 따라 줄바꿈한다. 실제 768px에서 제목과 업데이트 시각이 한 줄, 헤더 높이 64px였으며 360px에서는 자연스럽게 두 줄이 됐다. 두 화면 모두 가로 넘침이 없다.
- 관련 Vitest 4개 파일 40개 테스트, lint, typecheck:test, architecture:check, build 통과. 전체 테스트 스위트는 이번 변경에서 반복하지 않았다. 커밋·푸시는 하지 않았다.

# 2026-09-10 후속: PC 좌측 필터와 확장 피드

final result: passed

- 최신 요청에 따라 800px 최대 너비를 제거했다. PC는 왼쪽 280px 필터와 16px 간격의 유동 너비 피드, 모바일은 위쪽 필터와 단일 열 피드다. 헤더·콘텐츠의 PC 바깥 여백은 16px로 통일했다.
- 출처 탭은 필터 전체 너비를 채우는 shadcn 스타일로 세 칸을 균등 배치한다. 화면의 출처/멤버 제목은 제거하고 접근성 그룹 이름을 유지했다. 멤버 칩은 PC에서 세로로 배치하며 오시마크·대표색·선택 상태를 보존했다.
- 실제 `/feed`의 1440px 화면에서 필터 x=272px, 너비 280px, 피드 x=568px, 너비 841px를 확인했다. 탭은 각각 88px다. 1024px에서 피드 너비 617px, 문서 너비 1024px로 가로 넘침이 없었다. 360px에서는 필터와 피드 너비가 모두 321px이며 출처 탭 터치 높이는 44px다.
- X + 빙하유 선택 시 게시글 10개, 초기화 후 전체 복원을 확인했다. 키보드 방향키로 X 탭 선택과 포커스 유지, 결과 시작점 이동 후 좌측 필터 top=12px 고정을 확인했다.
- 캡처: `C:/Users/rlatm/.codex/visualizations/2026/09/09/01a083f8-5c9b-7ae3-ad40-73dca142b72c/member-feed-wide/`의 `1440-dark.png`, `1440-light.png`, `1024-dark.png`, `360-dark.png`.
- 관련 테스트 15개, lint, typecheck:test, build 통과. 마지막 모바일 탭 높이 CSS 보정은 실제 브라우저에서 44px로 재확인했다. 공용 페이지 셸의 구현은 변경하지 않았으며 커밋·푸시는 하지 않았다.

# 2026-09-10 후속: 통합 필터 내부 그룹 구분

final result: passed

- 통합 영역을 유지하면서 출처 탭과 멤버 칩을 레이블과 얇은 구분선으로 분리했다. 출처 행 오른쪽 초기화 버튼은 결과 유무와 관계없이 두 조건을 해제하며 기본 상태에서는 비활성화된다.
- 로컬 `/feed` 실제 데이터로 X + 빙하유 선택 시 게시글 10개, 초기화 후 전체 출처/전체 멤버 복원을 확인했다. 방향키로 X 탭 선택 시 선택 상태와 키보드 포커스를 유지했고 모바일 포커스 상단은 141.5px였다.
- 1280px 밝은/어두운 테마와 360px 어두운 테마를 확인했다. 360px에서 모든 멤버 칩은 3줄로 노출되고 터치 높이 44px, 문서 너비 360px로 가로 넘침이 없다. 작은 화면의 멤버 레이블을 별도 줄로 배치해 칩 너비를 확보했다. 필터 높이는 235px이며 모바일에서는 기존처럼 스크롤되어 읽기 영역을 가리지 않는다.
- 캡처: `C:/Users/rlatm/.codex/visualizations/2026/09/09/01a083f8-5c9b-7ae3-ad40-73dca142b72c/member-feed-filter-groups/`의 `1280-light.png`, `1280-dark.png`, `360-dark.png`.
- 관련 Vitest 2개 파일 15개 테스트, lint, typecheck:test, architecture:check, build, diff --check 통과. 이번 필터 변경에서는 전체 테스트 스위트를 반복하지 않았다. 실행 환경의 기존 Node 25 엔진 범위 경고는 남아 있다.
- 피드 업데이트 시각·답글·멤버 색상 보더와 다른 작업 변경사항을 유지했다. 커밋·푸시는 하지 않았다.

# 2026-09-10 후속: 통합 칩 필터·피드 데이터 업데이트 시각

final result: passed

최신 사용자 요청에 따라 이전 검토의 좌측 멤버 목록·출처 지연 안내·목록 확인 시각을 대체했다. 아래의 이전 기록은 당시 구현 이력이다.

## 결과와 실제 데이터 근거

- 제목은 유지하고 출처 아이콘 탭과 오시마크 멤버 칩을 하나의 줄바꿈 영역에 통합했다. 데스크톱 1280px에서 필터 높이는 94px다. 멤버 전원과 전체 선택을 표시한다.
- 모바일은 통합 필터 전체가 스크롤되므로 읽는 동안 큰 고정 영역을 점유하지 않는다. 390px에서 멤버 선택 후 결과 시작점 y=55.5px, 필터 하단 y=55.5px를 확인했다. 키보드로 조작하는 경우에는 초점을 화면 밖으로 밀지 않으며, 360px에서 방향키로 X 탭 선택 후 포커스 y=145.5px를 확인했다.
- 각 글의 왼쪽에 멤버 대표색 4px 보더를 표시했다. 피드의 상하 패딩 8px·내부 간격 6px·모바일 좌우 12px·데스크톱 좌우 16px로 줄였다. 별도 멤버 제목과 프로필 목록, 수집 지연/이전 데이터 안내는 제거했다. 초기 조회 실패로 아무 글도 표시할 수 없는 경우에만 재시도를 유지한다.
- 실제 `/feed` 화면에 **2026. 09. 05. 10:09:25 KST**가 표시됐다. 로컬 `/api/member-posts?compact=1`의 `feedUpdatedAt`과 readonly SQLite 조회를 비교해 둘 다 `2026-09-05T01:09:25.560Z`임을 확인했다. 반환 글 90개의 ID에 한정해 `x_posts`와 `naver_cafe_posts`의 `fetched_at` 최댓값을 읽었다. 응답 생성 시각은 9월 10일로 다르며 UI에는 사용하지 않는다.
- 날짜 필드는 하위 호환 가능한 nullable/optional 응답 필드다. 메타데이터 누락·실패는 null이며 현재 시각을 만들어 넣지 않는다. 비공개 글은 시간 조회 대상에도 포함하지 않는다. DB 읽기만 추가했고 수집 실행·DB 쓰기·권한·검색 조건은 변경하지 않았다.

## 시각 비교

원본 `C:/Users/rlatm/AppData/Local/Temp/codex-clipboard-e6cf6742-7a1a-4563-a3d2-4279d0ac6788.png` (1024×768px 시안 보드)와 최종 `1280-light.png`, `390-dark-reading.png`를 같은 이미지 입력에 함께 열어 검토했다. 원본은 연속 피드의 구조 참고이며 최신 사용자 지시에 따른 멤버 칩·시그니처 보더·한국어 콘텐츠·기존 전역 메뉴는 의도적 차이다. 원본 보드의 세 화면을 한 viewport로 비교하지 않았다.

캡처 폴더: `C:/Users/rlatm/.codex/visualizations/2026/09/09/01a083f8-5c9b-7ae3-ad40-73dca142b72c/member-feed-compact/`

- `1280-dark.png`, `1280-light.png`: CSS 1280×720px, 캡처도 동일. 통합 탭/칩, 색상 보더, 실제 데이터 갱신 시각 확인.
- `390-dark.png`, `390-dark-reading.png`: CSS/캡처 390×844px. 진입 시 전원 칩 표시와 스크롤 후 넓어진 읽기 영역 확인.
- `360-dark.png`: CSS/캡처 360×800px. 칩 9개 모두 터치 높이 44px, 문서 scrollWidth 360px, 가로 넘치는 게시글 0개.

모든 캡처 DPR 1이며 별도 이미지 확대·축소는 하지 않았다. 글꼴은 기존 Inter/sans-serif와 한국어 대체 글꼴, 본문 15px/24px를 유지했다. 작은 필터 12px·오시마크는 실제 멤버 데이터이며 색상 칩은 명도에 따라 선택 글자색을 바꾼다. 게시글 프로필·이미지 원본 비율·원문/공유 44px 동작 영역은 보존했다. 필터·시간·보더와 모바일 본문을 확대 가능한 캡처로 읽었으며 추가적인 P0/P1/P2 시각 문제는 발견하지 않았다.

## 반복 검토에서 보완한 점

모바일의 여러 줄 통합 필터를 고정하면 읽기 공간을 계속 점유하는 문제를 확인해 모바일에서만 일반 스크롤로 바꿨다. 변경 후 `390-dark-reading.png`와 DOM 좌표로 필터가 읽기 영역 밖으로 이동함을 확인했다. 키보드 조작 시에는 필터 초점을 화면에 유지하는 예외를 실제 방향키 흐름으로 확인했다.

## 검증 결과와 한계

- 변경 관련 UI/쿼리 테스트 11개 파일/61개, 피드 API 테스트 12개 통과. 마지막 키보드 처리 보완 후 overview 테스트 12개 재통과. API 검증은 저장 시각과 응답 생성 시각 구분, compact 보존, 반환 ID 범위, 비공개 범위 제외, 메타데이터 오류에도 글 유지·시각 null을 포함한다.
- `pnpm lint`, `pnpm typecheck:test`, `pnpm architecture:check`, `pnpm sync:agent-cursor:check`, `pnpm build` 통과. 마지막 UI 조정 파일은 ESLint와 build를 재실행했다.
- 기본 `pnpm test`의 단위 단계: 240파일 중 237파일 통과, 1668개 중 1653개 통과/15개 실패. OTW Play catalog-page 13개는 `window.localStorage.clear is not a function`, 관리자 테스트 2개는 5초 timeout이었다. 피드 변경 파일의 실패는 없다.
- 실패 파일을 분리해 실행한 결과 관리자 64개는 모두 통과했다. catalog-page 13개는 독립 실행에서도 동일한 저장소 오류가 났으며, 해당 실행에만 `NODE_OPTIONS=--no-experimental-webstorage`를 적용하면 13개 모두 통과했다. Node 25 Web Storage와 jsdom 환경의 충돌로 분리했다. 테스트 코드·제한 시간·프로젝트 실행 설정은 바꾸지 않았다. 기본 명령 전체를 통과했다고 주장하지 않는다.
- 기본 스크립트의 단위 실패로 건너뛴 Worker 검사는 별도 실행해 **27파일/293개 통과**를 확인했다.
- 로컬 5173의 실제 멤버/출처 조합(빙하유+카페 2개), 선택 해제, 업데이트 시각 유지, 모바일 스크롤과 키보드 포커스를 확인했다. 운영 배포·외부 수집은 실행하지 않았다. 기존 다른 변경과 브랜치를 보존했으며 커밋·푸시는 하지 않았다.

---

# 2026-09-10 멤버 게시글 레퍼런스 기반 개편

final result: passed

`/feed`의 최상단 출처 탭, 전체 멤버 선택 목록, 연속 피드와 답글 미리보기를 실제 로컬 화면에서 확인했다. 기존 OTW Play·멀티뷰 변경과 브랜치 `codex/otw-play-multiview-ui`는 보존했다. 커밋·푸시·배포는 수행하지 않았다.

## 비교 대상과 적용 범위

- 원본 시각 자료: `C:/Users/rlatm/AppData/Local/Temp/codex-clipboard-e6cf6742-7a1a-4563-a3d2-4279d0ac6788.png` (1024×768px, 모바일 시안 세 장을 담은 보드).
- 실제 구현: `http://localhost:5173/feed`, 로그인된 로컬 데이터, 기존 밝은/어두운 테마.
- 구현 캡처 폴더: `C:/Users/rlatm/.codex/visualizations/2026/09/09/01a083f8-5c9b-7ae3-ad40-73dca142b72c/member-feed-reference-redesign/`.
- 원본과 `1440-light.png`, `390-light-cafe.png`, `390-dark-parent-after.png`를 같은 이미지 비교 입력으로 열어 전체 구성과 답글 영역을 비교했다. 이전 답글 캡처도 원본과 같은 입력으로 비교했다.
- 원본은 제품의 정확한 화면 복제가 아닌 구조 참고 자료다. 한국어 실데이터·기존 전역 메뉴·실제 멤버 프로필·출처 아이콘을 사용한다. 원본의 영문 서비스 이름, 샘플 도시 사진, 작성 버튼·작성 패널은 요청 범위에 없으므로 추가하지 않았다.
- 원본 보드의 세 시안을 하나의 viewport로 간주하지 않는다. 모바일 단일 목록의 위계·밀도·구분선·작은 맥락 미리보기를 비교했다. 구현은 DPR 1, CSS viewport와 캡처 픽셀 크기가 같으며 별도 이미지 확대나 축소 편집은 하지 않았다.

## 수정과 재비교

1. 전체 화면: 큰 카드 테두리·그림자·멤버 색상 막대를 제거하고 하단 구분선으로 연결했다. 프로필 32px, 이름·출처·시각의 작은 헤더, 본문, 미리보기, 반응·원문·공유 순서를 유지한다.
2. [P2, 해결] 답글 미리보기의 표시 이름이 없는 경우 계정명이 두 번 표시됐다. `390-dark-parent-before.png`의 마지막 답글에서 확인했다. 이름과 계정이 같으면 보조 계정을 생략하도록 수정했고, 같은 390×844px·다크·테리 눈나·X 선택·목록 끝 상태의 `390-dark-parent-after.png`에서 단일 계정 표시를 확인했다. 미확보 작성자는 별도 안내를 유지한다.
3. 최종 비교: 원본과 수정 후 캡처를 함께 확인했다. 추가 P0/P1/P2 차이는 발견하지 않았다. 원본보다 큰 44px 동작 영역과 전체 멤버의 모바일 상시 노출은 사용자의 접근성·멤버 목록 요구를 따른 의도적 차이다.

## 필수 시각 검토

| 항목 | 확인 결과 |
| --- | --- |
| 글꼴·위계 | 기존 Inter/sans-serif와 한국어 시스템 대체 글꼴을 유지한다. 실제 본문 15px/24px, 이름 14px, 보조 정보 11px. 긴 본문 줄바꿈과 실제 넘칠 때 더보기를 확인했다. 원본의 영문 타이포그래피를 한국어에 그대로 복제하지 않았다. |
| 간격·레이아웃 | 데스크톱 왼쪽 멤버 220px, 열 간격 20px, 게시글 최대 720px. 피드는 얇은 구분선으로 연결한다. 모바일 멤버 전원 2열, 출처 탭 44px 한 줄만 고정. 게시글과 페이지 가로 넘침 없음. |
| 색·상태 | 기존 밝은/어두운 테마의 background/foreground/muted 토큰을 사용한다. 선택 탭은 밑줄·진한 글자, 멤버는 체크·배경으로 구분한다. 출처 지연은 작은 중립색 안내이며 상세는 접혀 있다. |
| 이미지·아이콘 | 기존 실제 멤버 프로필과 게시글 미디어, X·카페 SVG 자산, Lucide 기본 아이콘을 사용한다. 사진을 자르지 않고 비율을 유지하며 모바일 240px/데스크톱 320px 높이를 유지한다. 샘플 사진·가짜 프로필을 추가하지 않았다. |
| 문구·콘텐츠 | 전체/X/네이버 카페, 답글 맥락·원문 링크, 정확한 목록 확인 시각을 표시한다. 내부 구현 설명은 본문에 넣지 않는다. 답글 전환 및 이전 숨김 설정은 더 이상 사용하지 않는다. |

## 실제 화면 근거

| 캡처 | CSS viewport / 픽셀 | 상태 및 확인 |
| --- | --- | --- |
| `1440-light.png`, `1440-dark.png` | 1440×900 / 동일 | 전체 멤버·전체 출처, 왼쪽 220px, 피드 720px, 탭 높이 48px |
| `1024-dark.png` | 1024×768 / 동일 | 왼쪽 220px, 피드 649px, 모든 멤버 노출 |
| `390-light.png` | 390×844 / 동일 | 전체 멤버 전원 노출, 정확한 시각, 상단 아이콘 탭 |
| `390-light-cafe.png` | 390×844 / 동일 | 카페 선택 후 결과 시작점으로 이동, 44px 탭만 고정, 텍스트 목록 |
| `390-dark-replies.png` | 390×844 / 동일 | 미확보 답글의 회신 화살표·관계·직접 원문 링크 |
| `390-dark-parent-after.png` | 390×844 / 동일 | 확보된 원문이 즉시 노출, 중복 계정 제거, 푸터 1회 표시 |
| `360-dark.png` | 360×800 / 동일 | 전원 선택 항목 9개(전체+멤버 8), 탭 44px, 문서 scrollWidth 360px, 넘치는 게시글 0개 |
| `notice-regression.png`, `vod-regression.png` | 1280×720 / 동일 | 공지·VOD의 기존 고정 헤더·필터·카드·푸터 유지 |

390·1024·1440px에서도 문서 너비가 viewport를 넘지 않았으며 확인한 게시글의 scrollWidth가 clientWidth를 넘지 않았다. 비교 후 임시 viewport를 해제하고 전체 멤버·전체 출처·밝은 테마로 되돌렸다.

## 실제 동작과 시각 정보의 계약

- 실제 카페 탭 전환으로 카페 게시글만 표시되고, X/테리 눈나 선택으로 해당 멤버 글 10개와 다른 멤버 글 0개를 확인했다. 방향키로 전체→X 선택 및 포커스 이동을 확인했다.
- 모바일 필터 전환 후 결과 시작점으로 이동했으며 상단 탭은 y=56px, 높이 44px로 유지됐다. 멤버 목록을 Dialog 없이 직접 선택했다.
- 실제 첨부 사진의 Dialog 열기, Escape 닫기, 원래 이미지 확대 버튼으로 포커스 복원을 확인했다. 답글 원문이 있는 경우 바로 미리보기를, 없는 경우 대상 링크를 확인했다.
- 접힌 출처 지연 안내를 펼쳐 상세 사유를 확인하고 Space로 접었다. 강제 X 수집은 검증 목적으로 실행하지 않았다. 재시도 연결과 기존 글 보존은 컴포넌트 테스트로 확인했다.
- `worker/features/member-posts/application/get-member-posts.ts`의 최상위 `updatedAt`은 응답 생성 시각이다. 따라서 UI는 **목록 확인**이라는 이름으로 날짜·초·KST를 표시하며 원문 작성 시각·수집 완료 시각과 구분한다. 데이터 수집 완료 시각은 현재 공개 API에서 제공하지 않는다. 임의 현재 시각이나 마지막 글 작성 시각을 수집 시각으로 사용하지 않았다. 서버 API·DB·수집 동작 변경은 없다.

## 자동 검증

- 관련 Vitest: **21개 파일 / 122개 테스트 통과**. 피드·X·카페·공용 카드/탭/셸·공지 20개 파일/118개와 VOD overview 1개 파일/4개를 실행했다.
- 답글 상시 표시·이전 저장값 무시·멤버/출처 필터·순서·필터 전환 시 API 입력 불변·정확한 KST 시각·접힌 오류와 재시도·접근 정책·백그라운드 스크롤 유지·이미지 뷰어·공유·링크/인용 보존을 포함한다.
- `pnpm lint`, `pnpm typecheck:test`, `pnpm architecture:check`, `pnpm build`, `pnpm sync:agent-cursor:check`: 통과. 마지막 답글 수정 후 영향 파일 ESLint·관련 테스트·타입 검사·빌드를 다시 통과했다.
- 새 테스트에 잘못 추가한 Testing Library `exact` 옵션 때문에 발생한 테스트 타입 오류는 옵션을 제거하여 해결했다. 제품 타입과 최종 테스트 타입 검사 모두 통과했다.
- Node v25.2.1이 저장소 권장 범위 `>=24.20.0 <25`와 달라 engine 경고가 있었으나 위 검증은 완료됐다. 런타임 버전이나 서버 포트는 변경하지 않았다.
- `Design.md`의 멤버 게시글 규칙을 최종 구조로 갱신했다. 이전 답글 숨김·모바일 멤버 Dialog 규칙은 최신 사용자 요청으로 대체했다.

## 검증 한계 및 마무리

- 조회 실패·로그인/비공개 조합·공유 취소/복사 실패 등은 자동 테스트로 확인했다. 실제 계정의 로그아웃이나 접근 정책 변경은 하지 않았다.
- 현재 로컬 게시글의 출처 지연은 기존 데이터 상태다. 새 수집을 강제하거나 실제 수집 시각을 추정하지 않았다.
- 브라우저 콘솔 로그의 별도 수집과 운영 배포는 수행하지 않았다. 실제 UI 흐름과 DOM readback, 타입·린트·관련 회귀 테스트 범위를 구분한다.
- 체크 완료: 레퍼런스 동시 비교, 최종 답글 재캡처, 반응형·테마·키보드·이미지 검증, 공용 레이아웃 회귀, 디자인 문서 갱신.

---

# 2026-09-08 플레이어 태그 줄바꿈

플레이어 분류 영역의 가로 스크롤과 singleLine 옵션을 제거하고 개별 태그 칩을 너비에 맞춰 다음 줄로 배치했다. 스크롤용 tabIndex도 제거했다. 실제 Chrome 1366×768에서 347px 태그 영역의 clientWidth와 scrollWidth가 같고, `Have Yourself A Merry Little Christmas`의 공개일 칩이 두 번째 줄에 표시됐다. 재생 조작과 193.5px 높이의 큐 영역이 정상 표시됨을 확인했다. 태그가 적은 `팬서비스`는 한 줄을 유지한다. 검증용 큐를 비우고 viewport를 복원했다. 플레이어 UI 회귀 테스트 13개, 린트 통과.

---

# 2026-09-08 플레이큐에 따른 우측 플레이어 표시

큐 길이를 기준으로 데스크톱 rail을 표시하고, 빈 큐에서는 너비를 0으로 접는다. 등장 320ms·퇴장 280ms의 너비 전환과 opacity를 적용했다. 내부 section 너비를 고정해 iframe에 확대·축소 효과를 주지 않는다. 빈 패널은 inert/aria-hidden 처리하고 마지막 삭제 안내는 패널 밖의 live region에서 유지한다. 기존 reduced-motion 규칙은 전환을 제거한다.

실제 Chrome 로컬 `/play` → 첫 곡 추가 → 재생 → `/play/songs` → 마지막 큐 삭제 흐름을 확인했다. 1920×1080에서 큐 없음은 rail 0px/목록 1664px, 큐 있음은 rail 380px/목록 1284px였다. 재생 중 경로 이동 전후 단일 iframe(widget2, 356×200px)과 일시정지 조작이 유지됐고, 마지막 삭제 후 iframe 0개·visibility hidden·inert를 확인했다. 390×844에서 빈 큐 카탈로그와 전체 플레이어 너비는 390px로 가로 넘침이 없었으며 카탈로그 복귀 후 열기 버튼이 표시됐다. 1366×600에서는 플레이큐 전환 후 큐 높이 288px·단일 200px 높이 iframe·삭제 버튼 접근을 확인했다. 테스트 곡을 제거하고 viewport를 복원했다.

플레이어 UI·컨텍스트 회귀 테스트 26개, `pnpm lint`, `pnpm typecheck:test`, `pnpm exec tsc -b --pretty false` 통과. 실제 브라우저는 동작 줄이기 꺼짐 상태로 검증했으며 켜짐 상태는 기존 CSS 규칙으로만 확인했다. 이번 변경에 전체 테스트·운영 배포는 수행하지 않았다.

---

# 2026-09-08 좌우 카드 썸네일 높이 채움

이미지 원본 비율과 좌우 배치를 유지하면서 정보 높이가 큰 카드의 하단 빈 공간을 제거했다. 미디어 컨테이너를 카드 높이로 늘리고 앞쪽 원본 이미지는 contain으로 온전히 보여준다. 뒤에는 같은 썸네일을 장식용 cover/blur 배경으로 배치해 남는 상하 공간을 채운다. 장식 이미지는 접근성 트리에서 숨기며 기존 썸네일 오류 대체 동작을 재사용한다.

실제 1366px과 500px 화면에서 8곡 모두 카드 내부와 미디어 영역 높이 차이 0px, 앞쪽 원본 비율 16:9를 확인했다. 390px 모바일에서는 미디어와 원본 이미지 높이가 모두 199.125px로 기존 세로 배치를 유지했고 가로 넘침이 없었다. 임시 viewport를 복원했다. 카드/썸네일 회귀 테스트 9개와 린트·diff 검사 통과.

---

# 2026-09-08 중간 배치 폐기·좌우 카드 유지

사용자 피드백에 따라 참여 정보와 버튼을 전폭 하단으로 이동하던 중간 배치와 관련 마크업을 제거했다. 콘텐츠 폭 480px 이상에서는 썸네일 왼쪽·정보/작업 오른쪽을 유지하며 480px 미만에서만 세로 카드로 전환한다. 480–720px 구간은 이미지 34%·정보 66%, padding 12px로 조절하고 버튼은 오른쪽 영역 안에서 줄바꿈한다. 썸네일은 원본 비율을 유지하며 정보와 함께 상단 정렬한다. 좁은 좌우 카드에서 정보가 이미지보다 높을 경우 이미지 아래 공간은 보존하되 크롭하지 않는다.

실제 1366px 화면에서 좌우 열 268px/402px, 500px 화면에서 157.75px/306.25px를 확인했다. 500px의 8곡 모두 작업 버튼은 오른쪽 영역 내에 있고 가로 넘침이 없었다. 768px 태블릿은 좌우 배치와 썸네일 상단 여백 0px, 390px은 세로 배치로 확인했다. 검토 중 viewport를 복원했다. 검색 카드·검색 회귀 테스트 13개, 린트·TypeScript 및 diff 검사 통과. 아래 3단계 검토 기록은 폐기된 구현 이력이다.

---

# 2026-09-08 검색 카드 반응형 3단계

좌측 사이드바와 우측 플레이어를 제외한 콘텐츠 폭 1152px 미만에서 바로 큰 세로 카드로 바뀌던 기준을 수정했다. 600px 미만은 세로, 600–1152px 미만은 이미지·제목 가로 + 참여 정보·작업 하단 전폭, 1152px 이상은 기존 좌우 카드로 구성한다. 제목의 굵기 900과 이미지 원본 비율을 유지하고 중간 구간의 제목은 실제 영역 너비 기준 20–26px로 조절한다.

실제 1366×768 화면의 콘텐츠 폭은 720px이며 8곡 모두 카드 높이 256.75px·제목 23.25px·썸네일 여백 0px를 확인했다. 768px 화면은 카드 277.28px·제목 23.82px, 620px 경계 화면은 8곡 모두 제목 잘림과 버튼 가로 넘침 없이 가로 배치를 유지했다. 390px은 세로 배치·제목 22.05px, 1920px은 기존 좌우 배치·제목 31.56px·8곡 썸네일 여백 0px였다. 확인한 모든 화면에서 가로 넘침이 없었으며 임시 viewport를 복원했다.

곡 카드·검색 회귀 테스트 13개, 린트, TypeScript 검사와 diff 검사 통과. 재생·검색 조건·페이지네이션 로직은 변경하지 않았다.

---

# 2026-09-08 검색 카드 반응형 제목

제목은 실제 정보 영역 너비에 따라 기본 글꼴 기준 20–32px로 조절하고 굵기 900·자간 0을 적용했다. 실제 Chrome에서 390px 화면은 약 22.05px, 1790px은 29.61px, 1920px은 31.69px로 확인했다. 기존 960px 콘텐츠 경계에서는 두 줄 제목으로 이미지 위아래에 여백이 다시 발생해 가로 배치 시작점을 1152px로 조정했다. 수정 후 1790px·1920px의 8곡 모두 이미지 높이 차이 0px와 버튼 하단 여백 16px를 유지했다. 모바일 렌더링과 가로 넘침 없음을 확인했고 임시 viewport를 복원했다. 린트·diff 검사 통과. CSS 변경으로 신규 테스트는 추가하지 않았다.

---

# 2026-09-08 검색 카드 정보·버튼 상하 정렬

검색 카드에 한정해 정보는 flex-start, 작업 버튼은 margin-top:auto로 정렬했다. 실제 1920×1080의 8곡 모두 정보 상단 16px·버튼 하단 16px, 썸네일과 카드 내부 높이 차이 0px를 확인했다. 390×844에서도 8곡의 상하 여백이 각각 16px이고 가로 넘침이 없었다. 임시 viewport는 복원했다. CSS 정렬만 변경했으며 린트와 diff 검사를 통과했다.

---

# 2026-09-08 썸네일 높이·버튼 하단 정렬·스크롤바

1920px 화면에서 검색 카드 정보 영역이 이미지보다 5.5px 높아 상하 빈 띠가 생기는 것을 확인했다. 이미지 비중을 40%로 늘리고 정보 간격을 줄였으며, 콘텐츠 960px 미만에서는 세로 배치로 원본 비율을 보존한다. 실제 1920px 및 경계 1596px에서 8곡 모두 카드 내부와 이미지의 높이 차이가 0px였다. 390px에서는 8곡 이미지 비율 16:9와 이미지 영역 높이 차이 0px를 확인했고, 1366px에서는 세로 배치 및 가로 넘침 없음이 확인됐다.

발견 재생 작업을 패널 하단으로 정렬했다. 1920px에서 8곡 전환 중 카드 높이 370.25px와 순서 조작부까지 16px 간격이 유지됐고, 모바일에서는 간격 12px·가로 넘침 없음이 확인됐다. 스크롤바는 Play 루트에 한정한 회색 thumb와 투명 track, 명시적인 color-scheme을 적용했다. 실제 다크 화면에서 rgb(98,105,120) thumb와 투명 track을 확인했다. 임시 viewport와 검토 전 라이트 화면을 복원했다.

관련 테스트 20개·린트 통과. 이미지 크롭이나 플레이어 상태 로직은 변경하지 않았다. 참고한 Spotify 디자인 자료는 Design.md에 기록했다.

---

# 2026-09-08 페이지 표시·참여자 검색·헤더 포커스

숫자 페이지 표시를 제거하고 자동 전환을 6초에서 7초로 늦췄다. 순서 점과 이전/다음 조작은 유지한다. 참여자 이름·멤버·그룹 조건의 역할 미지정은 메인 보컬·피처링으로 처리하며, 명시적인 코러스·기타 선택은 유지한다. 기존 term/gram 후보에 역할 검증을 적용하므로 데이터 재작성 없이 기존 카탈로그에도 적용된다. 캐시 경로를 v2로 변경해 이전 검색 결과 재사용을 차단했다. 발견과 참여자 칩의 멤버 링크도 같은 기본값을 사용한다.

헤더 검색에 일반 입력용 배경·높이·개별 outline이 중첩되던 스타일을 분리했다. 검색 영역 전체에 focus-within outline을 적용하고 검색 입력은 투명 배경과 영역 높이를 따른다. 실제 Chrome에서 양메이 입력 및 Enter 검색 후 입력 포커스 유지, 검색 영역 40px·입력 약 37.78px, 입력 outline 없음·영역 outline 표시를 확인했다. 실제 카탈로그는 바움쿠헨 엔드롤 1곡을 반환했다. 역할 필터의 기본 문구와 명시적 코러스 선택도 확인했다. 로컬 코러스 검색 결과는 비어 있었으며, 역할별 배제·포함은 D1 통합 테스트로 별도 검증했다.

관련 단위 테스트 41개와 D1 통합 테스트 15개 통과. D1 검증은 이름의 완전·부분 일치, 멤버/참여자 필터, 기본 코러스/기타 제외, 명시적 코러스 검색, 곡명 검색 보존 및 기존 쿼리·bind·rows-read 비용 제한을 포함한다. 린트·아키텍처·프런트엔드와 Worker TypeScript 검사 통과. 운영 배포는 수행하지 않았다.

---

# 2026-09-07 발견 정보 상단 정렬·반응형 곡명

`77a8ccf` 이후 정보 묶음을 패널 상단으로 정렬했다. 상단 여백은 모바일 20px·데스크톱 24px이며, 기존 6px 정보 그룹과 12px 그룹 간격을 유지한다. 곡명 크기는 페이지 전체가 아닌 실제 텍스트 패널의 너비를 기준으로 `clamp(1.25rem, calc(0.75rem + 3.5cqi), 2rem)`을 적용한다. 기본 글꼴 기준 범위는 20–32px이다. 하단 순서 조작부는 `margin-top: auto`로 위치를 유지한다. 변경 범위는 발견 카드 CSS이며 데이터·재생·전환 로직은 변경하지 않았다.

실제 `/play`의 로컬 8곡으로 확인했다. 너비 390·600·768·1920px에서 긴 곡명의 글자 크기는 각각 약 23.10·30.45·32·29.88px였다. 768px에서는 세로 배치로 텍스트 영역이 더 넓어 데스크톱 2열보다 글자가 크며, 이는 의도한 컨테이너 기준 동작이다. 네 크기에서 가로 넘침이 없고 긴 제목도 줄바꿈된다. 데스크톱 1920×1080에서 8곡 모두 카드 높이 368.75px와 재생·순서 버튼 위치가 동일했다. 모바일 390×844에서 8곡 모두 카드 높이 약 506.73px, 재생 작업의 카드 내 y=400.06px, 정보 상단 여백 20px로 동일했다.

발견 회귀 테스트 11개와 전체 린트 통과. 데스크톱·모바일 렌더링을 확인하고 임시 viewport를 복원했다. 최신 캡처는 `.tmp/otw-play-glass-review/fluid-discover-desktop.png`, `fluid-discover-mobile.png`이며 비교 페이지 Revision 6에 반영했다. 아래 기록은 이전 검토 이력이다.

---

# 2026-09-07 발견 상단 카드 정보 묶음 개선

`43b634f` 이후, 긴 제목에 맞춘 개별 행의 높이 때문에 문장 사이가 벌어지던 subgrid를 정보 전체의 겹친 grid로 변경했다. 곡명·참여자, 태그·가창 분류를 각각 6px 간격의 그룹으로 묶고 그룹 사이 12px, 재생 작업까지 14px를 적용했다. 태그가 없으면 빈 태그 행을 만들지 않는다. 곡명은 데스크톱 28px·모바일 22px, 참여자 14px, 영문 안내 10px로 위계를 조정했다.

실제 1920×1080과 390×844에서 8곡을 모두 전환했다. 데스크톱 카드 높이 368.75px와 재생·순서 버튼 위치가 동일했고, 모바일은 카드 높이 505px·재생 작업의 카드 내 y=398px로 동일했다. 모바일 가로 넘침은 없었다. 짧은 곡명의 남는 높이는 정보 위쪽에 모아 곡명과 참여자가 붙어 읽히도록 했다. 원본 이미지와 기존 전환·재생 기능은 유지하며 임시 viewport는 복원했다.

발견 회귀 테스트 11개와 전체 린트 통과. 최신 캡처: `.tmp/otw-play-glass-review/grouped-discover-desktop.png`, `grouped-discover-mobile.png`. 아래 개별 행 높이 고정 기록은 이전 버전의 검토 이력이다.

---

# 2026-09-07 검색 썸네일 좌우 여백 수정

`8558468` 이후, 검색 카드의 썸네일이 텍스트 영역 높이에 종속되던 absolute/contain 배치를 원본 비율의 일반 흐름 이미지로 변경했다. 가로폭 100%·높이 auto를 사용하며, 이미지 열은 세로 중앙 정렬한다. 원본 전체를 보존하고 이미지 없음·실패 시 16:9 대체 영역을 유지한다.

실제 Chrome 로컬 카탈로그에서 2560px 화면의 이미지와 영역 너비가 모두 465px, 이미지 높이는 261.5625px였다. 1920px에서 약 365.69×205.69, 768px에서 약 210.89×118.63, 390px에서 349×196.3125로 원본 1280×720 비율을 유지했다. 각 크기에서 이미지와 영역의 가로폭이 같고 문서 가로 넘침이 없었다. 임시 viewport는 복원했다.

기존 검색 카드·썸네일 테스트 9개와 전체 린트 통과. 새 기능·패키지·API 변경은 없으며 기존 hero 모드는 유지했다. 캡처는 `.tmp/otw-play-glass-review/width-catalog-wide.png`, `width-catalog-desktop.png`, `width-catalog-mobile.png`에 저장했다.

---

# 2026-09-07 상세 검색 복귀 링크 정렬

`ed0e8f6` 이후, 전체 너비로 늘어나 가운데에 놓이던 검색 복귀 버튼을 상세 카드 상단 왼쪽의 내용 너비 링크로 변경했다. 실제 데스크톱·390×844 모바일에서 너비 84.375px, 높이 44px, 카드와 간격 12px를 확인했다. 모바일 가로 넘침이 없으며 클릭 후 `/play/songs`와 곡 검색 제목을 확인했다. 임시 화면 크기는 복원했다.

기존 곡 상세 테스트와 전체 린트를 통과했다. 이번 변경은 링크 정렬·간격이며 별도 상태 로직이나 새 테스트는 추가하지 않았다. 캡처: `.tmp/otw-play-glass-review/detail-navigation-desktop.png`, `detail-navigation-mobile.png`.

---

# 2026-09-07 카드 높이·재생 재개·SVG 피드백 검증

`ae72298` 이후 후속 변경이다. 현재 사양은 이 절과 `Design.md`를 따르며, 아래 Ripple 구현 기록은 이전 버전의 검증 이력이다.

- 발견의 제목 길이·참여자·태그 유무에 따라 높이가 바뀌던 구조를 변경했다. 최대 8곡이 이미지 grid와 정보 subgrid의 5개 행을 공유하며, 비활성 정보는 보이지 않고 접근성 트리에서도 제외된다. 임의의 고정 높이나 곡명 자르기를 추가하지 않았다.
- 실제 Chrome에서 8곡을 모두 수동 전환했다. 1920×1080에서 카드 높이 368.75px, 내부 행 높이 `[16.5, 90, 20, 22, 16.5]`, 하단 버튼 y=553.75가 모든 곡에서 같았다. 390×844에서는 카드 높이 525.3125px, 행 높이 `[16.5, 60, 20, 22, 16.5]`로 동일했고 문서 너비는 390px였다.
- 같은 곡의 재생 요청은 이미 로드한 소스라는 이유로 load를 건너뛰었지만 play 명령을 보내지 않았다. 재생 재개 회귀 테스트에서 실제로 play 호출 0회로 실패하는 것을 확인한 뒤, 현재 로드한 항목·소스가 같을 때 play를 명시적으로 호출하도록 수정했다. 소스가 다르면 기존 load 경로를 사용한다.
- 실제 발견에서 재생 → 우측 일시정지 → 발견의 현재 곡 재생 → 진행 시간 1:42와 재생 중 상태를 확인했다. 다시 1:43에 일시정지 → 곡 검색 이동 → 첫 카드 재생 → 2:15 진행 및 재생 중 상태를 확인했다. 양쪽 모두 iframe ID는 `widget2`, 큐는 1곡으로 유지됐으며 검증 후 해당 곡을 삭제했다.
- 검색 카드의 재생·큐 추가 옆에 곡 상세 링크 버튼을 추가했다. 첫 카드 버튼에서 팬서비스 상세 URL과 h1을 확인했다. 데스크톱·모바일에서 세 작업을 모두 읽고 누를 수 있었다.
- Ripple의 pseudo-element·좌표 계산을 제거했다. hover·포인터·키보드 입력으로 버튼 안의 SVG에만 360ms 방향 이동·회전·확대 후 복귀를 적용했다. 실제 이전 추천곡 버튼에서 SVG transform의 x=-2.00838 중간 프레임과 `::before content: none`을 확인했다. 동작 줄이기·비활성·키 반복은 기존 테스트에서 보호한다.
- `architecture:check`, `typecheck:test`, `lint`, `test`, `build` 모두 통과했다. 단위 233파일/1,597개 + Worker 23파일/220개 = 1,817개다. 현재 곡 재개 시 재로드·seek·큐 중복 없음, 다른 소스 선택 시 해당 소스 load, 상세 링크 목적지와 SVG 대상 입력 전달을 확인한다.
- 최신 캡처는 `.tmp/otw-play-glass-review/stable-discover-desktop.png`, `stable-discover-mobile.png`, `stable-catalog-desktop.png`, `stable-catalog-mobile.png`다. 실제 동작은 `http://localhost:5173/play`에서 확인한다. 다크 테마와 현재 일반 모션 설정을 유지했다. 이번 재검증은 위 화면·동작 범위이며 과거의 다른 해상도·라이트 상태 검증은 아래 이력이다.

운영 배포·API·DB·권한·패키지 변경은 없다.

---

# 2026-09-07 카드 전환·버튼 피드백 개선 검증

`codex/otw-play-glass-refresh`의 `e21b2cd` 이후 후속 변경이다. 아래 이전 검토의 시작·일시정지 버튼 및 360ms 전환 설명은 당시 기록이며, 현재 동작은 이 절과 `Design.md`를 따른다.

- 자동 전환 시작·일시정지 버튼을 제거했다. 6초 자동 전환은 유지하며 hover·포커스·숨겨진 탭에서 멈춘다. hover와 포커스가 모두 벗어나면 새 6초 간격으로 재개한다. 동작 줄이기에서는 수동 전환만 허용한다.
- 추천곡 정보와 재생 작업을 하나의 그룹으로 정렬했다. 하단 순서 표시·좌우 이동은 독립적으로 유지한다. 하단 패딩은 데스크톱 8px, 모바일 6px이며 실측 테두리 포함 거리는 각각 9px, 7px였다.
- 이미지와 내용은 600ms로 전환하고 내용 이동은 4px로 제한했다. 실제 전환 중 이미지 재생성으로 잠깐 비는 현상을 발견해 최대 8개 이미지를 미리 읽고 같은 요소를 유지하는 교차 페이드로 수정했다. 실제 8개 이미지의 1280px 원본 로드와 전환 전후 opacity 1→0 / 0→1을 확인했다. 최초 네트워크 로딩과 실패 처리는 기존 썸네일 컴포넌트를 따른다.
- 기존 버튼에는 배경색 전환만 있었다. [Material Web 버튼](https://github.com/material-components/material-web/blob/main/button/internal/button.ts)의 별도 Ripple 구조를 참고해 OTW Play 내부에 포인터 위치 기반 520ms Ripple과 140ms 누름 피드백을 적용했다. 실제 Chrome 클릭에서 시작 x=21px, Ripple 지름 약 64px, 표시 중 opacity 약 0.153을 확인했다. 키보드는 중심에서 시작하고 비활성·동작 줄이기는 생략한다.
- IconButton은 `data-size`로 명시적으로 구분한다. 텍스트+SVG 버튼의 패딩은 유지한다. 이전·다음은 데스크톱 36×36, 모바일 44×44, 내부 패딩 0이며 모바일 16px 아이콘 주변 여유는 각 14px였다.
- 실제 `/play`의 다크 테마에서 390×844와 1920×1080을 확인했다. 문서 가로 넘침이 없고 모바일 8개 순서 표시와 좌우 버튼은 한 줄이었다. 이번 Chrome의 동작 줄이기는 꺼져 있었으며 일반 모드 자동 전환을 확인했다. OS·테마 설정은 변경하지 않았다.
- 모바일 큐 추가 → 56×56 고정 플레이어 버튼 → 전체 플레이어 진입 → 실제 영상 0:05 진행을 확인했다. 버튼의 `position: fixed`와 화면 아래 16px 간격을 유지한다. 검증 항목은 큐에서 삭제했다.
- 아키텍처 검사, 테스트 타입 검사, 린트, 전체 회귀 테스트(단위 233파일/1,595개 + Worker 23파일/220개), 빌드가 통과했다. 마지막 이미지 유지 수정 후 관련 14개 테스트·린트·빌드도 다시 통과했다. 테스트는 자동 전환·포커스·동작 줄이기·타이머 정리·이미지 요소 보존·Ripple 입력 및 기존 클릭 전달을 확인한다.
- 최신 발견 캡처: `.tmp/otw-play-glass-review/motion-discover-desktop.png`, `motion-discover-mobile.png`. 비교 문서의 검색·최근곡 캡처는 해당 레이아웃이 바뀌지 않은 이전 `e21b2cd` 기록이다. 실제 애니메이션은 `http://localhost:5173/play`에서 확인한다.

API·DTO·DB·권한·재생 모델·패키지는 변경하지 않았으며 운영 배포는 하지 않았다. 이전 전체 화면 검증은 아래 기록을 보존하며, 이번에 다시 실행한 화면·흐름은 위 범위다.

---

# 2026-09-07 후속 UI 개선 검증

사용자의 5개 후속 요청을 같은 `codex/otw-play-glass-refresh` 브랜치에 반영했다. 아래 초기 검증 기록은 `4256d90` 당시의 상태다.

- 제목: `오버더월 NOW PLAY ON OTW PLAY`.
- 추천 이미지는 실제 원본 1280×720에 대해 가로 652.02px / 높이 366.75px를 확인했다. 영역과 이미지의 가로폭이 같고 원본 비율 전체를 유지한다. 모바일은 349px, 태블릿은 703px로 양쪽 폭이 일치한다.
- 추천곡은 6초 자동 전환, 360ms 페이드·슬라이드. hover·숨겨진 탭에서 멈추고 키보드 진입 후에는 시작 버튼으로 재개한다. 조작부와 실제 재생 iframe은 교체하지 않는다.
- 이번 Chrome에서는 `prefers-reduced-motion: reduce`가 실제 활성화돼 있었다. 기본 정지·명시적 시작 후 자동 전환을 확인했으며, 계산된 이미지 animationName은 `none`이었다. 일반 모드의 이동 애니메이션 렌더링은 이번 환경에서 별도 재현하지 않았고 CSS 및 회귀 테스트로 검증했다.
- 실제 시작 후 팬서비스→모시모시→Christmas 등 선택 곡과 상세 링크가 자동 갱신되고 일시정지 버튼으로 멈추는 것을 확인했다.
- 최근곡은 큰 16:9 이미지, 곡 상세 링크, 참여자·분류·공개일, 항상 보이는 재생·큐 추가 버튼이 있는 음악 카드로 변경했다. 데스크톱 4열, 768px 태블릿 2열(각 342.5px), 390px 모바일 1열을 확인했다.
- 최근곡 QWER 카드에서 추가됨·큐 1곡을 확인하고 재생 버튼으로 실제 영상·자막·0:23 진행·재생 중 상태까지 확인했다. 검증 후 일시정지하고 테스트 큐 항목을 제거했다.
- 모바일 최근곡 썸네일에서 팬서비스 상세로 진입했다. 검색의 바깥 카드와 필터 내부 카드를 제거한 상태에서 필터 열기·빙하유 조건 3곡·모두 초기화 8곡 복귀를 확인했다.
- 390×844와 768×1024에서 document 가로 넘침 없음. 1920×1080 발견·검색 최종 캡처를 저장했다. 임시 viewport는 복원했다. 테마·OS 설정은 변경하지 않았다.
- `pnpm typecheck:test`, `pnpm lint`, `pnpm architecture:check`, `pnpm test`, `pnpm build` 통과. 단위 232파일/1,592개 + Worker 23파일/220개 = 1,812개.
- 기존 table/CSS 클래스 기대는 승인된 음악 카드와 실제 상세·재생 작업의 존재를 확인하도록 갱신했다. 자동 전환 시간, hover, 키보드 정지, 명시적 재개, 동작 줄이기 기본 정지, unmount 타이머 정리 회귀 테스트를 추가했다.

현재 비교: [.tmp/otw-play-glass-review/comparison.html](.tmp/otw-play-glass-review/comparison.html).
최신 증거 파일은 `refined-discover-desktop.png`, `refined-catalog-desktop.png`, `refined-discover-mobile.png`, `refined-recent-mobile.png`, `refined-catalog-mobile.png`, `refined-discover-tablet.png`다.
운영 배포·API·DB·재생 모델·패키지 변경은 없다.

---

# OTW Play 3색 글래스 UI 검토

검토일: 2026-09-07. 로컬 검토 범위의 시각적 P0/P1/P2 결함은 수정했다.
운영 배포와 회원 제안의 신규 제출·검수는 수행하지 않았다.

## 기준과 구현

- 기준: 사용자가 승인한 3색 글래스 UI 계획, `otw-play-v2` (`16f97a7560a49121b64276a2a3cadc7f07da5c36`).
- 구현 브랜치: `codex/otw-play-glass-refresh`.
- 실제 진입점: `http://localhost:5173/play`, `/play/songs`, 곡 상세, `/play/submit`, `/play/submissions`.
- 관리자 로그인 상태의 기존 로컬 카탈로그 8곡으로 확인했다. 공개 비활성·관리자 미리보기 권한 모델은 유지했다.
- 첨부 이미지는 전체 화면 목업이 아닌 브랜드 장식의 원본이다. 레이아웃은 승인 계획과 V2의 기존 이용 흐름을 기준으로 비교했다.
- `public/images/otw-play/glass-note.png`: 원본 1024×1536 RGBA, 사용자 첨부 파일과 SHA-256 동일
  (`35A74CD6170B53CBE89DA8EBD1681E7AB2A6FC9C1107F1CC4396D7CEF2B0D60D`).
- 증거 폴더: `.tmp/otw-play-glass-review/`. 이미지 편집·데이터 조작 없이 Chrome 브라우저에서 캡처했다.
- 전후 비교: [초기 비교](.tmp/otw-play-glass-review/comparison-initial.html).

## 전체 화면과 세부 비교

V2와 새 버전의 발견·곡 검색 이미지를 같은 비교 입력으로 열어 대조했다.
모두 1920×1080 픽셀 / 같은 CSS viewport, 같은 다크 테마, 검색 조건 없음, 큐 0곡,
동일한 첫 곡 팬서비스와 동일한 카탈로그 정렬이다. 축척 보정·크롭·리샘플링은 하지 않았다.
공용 사이드바의 마우스 hover 위치는 일부 다르며 제품 변경으로 판단하지 않았다.

| 화면 | V2 | 새 UI |
| --- | --- | --- |
| 발견 | `v2-discover-desktop.png` | `glass-discover-desktop.png` |
| 곡 검색 | `v2-catalog-desktop.png` | `glass-catalog-desktop.png` |

세부 영역은 원본 해상도의 같은 이미지에서 별도로 확인했다.
발견의 x=280–1500, y=88–574 영역에서 음표·타이포·썸네일·조작부를,
검색의 x=280–1516, y=353–790 영역에서 첫 두 카드의 순서·이미지 비율·버튼 대비를 비교했다.
모바일 390×844 원본은 정보가 충분히 크게 보여 별도 이미지 크롭 없이 초점·줄바꿈·탭을 확인했다.

| 검토 항목 | 판단 |
| --- | --- |
| 글꼴·타이포그래피 | 기존 Inter/시스템 fallback 유지, 제목 700. 발견 40/28px, 섹션 22px, 카드 곡명 18px. 긴 한·일 곡명은 카드 안에서 줄바꿈하며 모바일 발견 제목은 단어를 보존한다. |
| 간격·구조 | 발견의 이미지 위 글씨를 옆 정보 패널로 분리했다. 실제 콘텐츠 720px 미만에서 세로 배치, 패널 24px/카드 18px 모서리. 사이트 내비게이션과 우측 380px 영역 유지. |
| 색·재질 | 코랄 주요 재생, 틸 선택과 포커스, 앰버 장식. 목록은 중립 표면. 반투명 조작부는 16px blur와 불투명 fallback을 사용한다. |
| 이미지 | 첨부 음표의 원본 비율·투명도·색을 유지한다. 실측 높이 144/88px. 실제 곡 이미지는 별도 표시하며 `object-fit: contain`을 확인했다. |
| 문구·정보 | 첫 페이지 8곡을 탐색하는 기존 추천 배너이며 개인화·에디터 큐레이션을 표방하지 않는다. 곡명→아티스트·참여자→분류·공개일→작업 순서. 공식 버전 개수 칩 없음. |

## 반응형·상태 증거

| viewport | 실제 확인과 증거 |
| --- | --- |
| 390×844 | 발견·검색·긴 제목·다수 참여자 상세·빈 결과·펼친 필터·전체 플레이어·내 제안. `glass-discover-mobile.png`, `glass-catalog-mobile.png`, `glass-long-title-mobile.png`, `glass-detail-mobile.png`, `glass-empty-mobile.png`, `glass-filters-mobile.png`, `glass-player-mobile.png`, `glass-submissions-mobile-light.png`. |
| 768×1024 | 실제 콘텐츠 너비 703px의 세로 추천 패널, 전체/미니 플레이어. `glass-discover-tablet.png`, `glass-player-tablet.png`, `glass-mini-player-tablet.png`. |
| 1366×768 | 실제 추천 콘텐츠 너비 665px의 세로 패널, 380px 우측 플레이어, 가로 넘침 없음. `glass-discover-1366.png`. |
| 1920×1080 | 가로 추천 패널, 검색 카드, 다크/라이트 표면, 제안 폼. `glass-discover-desktop.png`, `glass-catalog-desktop.png`, `glass-catalog-light.png`, `glass-submit-light.png`. |
| 1366×600 | 단일 iframe 높이 200px 유지, 현재 재생/큐 전환, 8곡 큐 내부 스크롤 후 마지막 항목 도달. `glass-player-short.png`, `glass-queue-scroll-short.png`. |

전체 문서 가로 넘침은 위 주요 viewport에서 없었다. 멤버 목록은 기존 의도대로 내부 가로 스크롤을 사용한다.
플레이어·큐의 구간별 배경과 문구를 확인했으며 재생 버튼 아래 구분선을 되살리지 않았다.

## 실제 이용 흐름

- 발견 다음/이전 추천곡 전환, 긴 제목과 참여자 표시, 멤버 빙하유 진입으로 `member=3`, `participantRole=vocal` 조건과 2곡 결과를 확인했다.
- 멤버 조건 개별 해제, 모두 초기화, 검색어 입력 후 0곡 안내와 필터 초기화로 원래 8곡 복귀를 확인했다.
- QWER 상세에서 두 보컬과 공식 버전·source 목록을 확인했다. 상세의 대기열 추가 후 `추가됨`과 큐 항목을 확인했다.
- 팬서비스 재생 버튼으로 실제 YouTube 영상·자막, 진행 시간 0:11→0:43 갱신과 `일시정지`/`재생 중` 상태를 확인했다.
- 반복은 꺼짐→전체→한 곡→꺼짐 순환을 실제 버튼으로 확인했다.
- 태블릿 전체→미니 전환과 곡 상세 이동, source 선택, 낮은 화면 큐 전환에서 YouTube iframe은 계속 1개, id `widget2`를 유지했다.
- QWER의 `이 source 재생`으로 영상과 큐 현재 항목이 QWER로 바뀌었고, 추가 작업 후 큐 8곡을 확인했다.
- 640px 미만 카탈로그 복귀 시 일시정지·launcher, 태블릿 미니 화면의 재생/확장 조작을 확인했다. 기존 복귀 시 일시정지 동작은 변경하지 않았다.
- 곡 제안 첫 입력 화면, 입력 후 이탈 보호, 내 제안의 기존 항목·검수 상태를 확인했다. 검증 입력은 저장·제출하지 않았다.
- 다크/라이트 전환 후 원래 다크로 복원했다. 임시 viewport 지정도 해제했다.
- 최종 검토 탭의 브라우저 console error 조회 결과는 빈 배열이었다. 전체 YouTube 내부 네트워크 로그 감사는 수행하지 않았다.

## 접근성·모션

- 버튼 150ms, 탭·필터 200ms, 추천 내용 240ms. 썸네일 확대나 iframe 변형 없음.
- 모바일 주요 재생·큐 버튼은 실제 높이 44px. 선택·반복 상태는 문구와 아이콘을 함께 사용한다.
- 검색 포커스와 추천 조작부의 키보드 포커스를 확인했다. 바깥 frame을 `overflow: clip`으로 바꾸어 초점 이동이 헤더를 끌어올리지 않게 했다.
- `prefers-reduced-motion: reduce`의 애니메이션·전환 해제 규칙이 브라우저 CSSOM에 로드됐음을 확인했다.
  도구가 미디어 선호 에뮬레이션을 제공하지 않아 OS의 동작 줄이기를 켠 실제 렌더링은 별도 확인하지 못했다.
  [MDN 지침](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)에 맞춰 이동 효과를 제거한다.
- 중립 카드 기준 WCAG 상대 휘도 계산: 라이트 기본 글씨 15.71:1, 보조 6.10:1, 틸 글씨 6.09:1;
  다크 기본 15.32:1, 보조 8.13:1, 틸 글씨 9.85:1; 코랄 버튼 글씨 5.80:1.
  이 수치는 지정 토큰 쌍의 계산이며 모든 투명 표면·외부 iframe 텍스트를 자동 감사한 수치는 아니다.
- 이미지 실패는 기존 고해상도→카탈로그 이미지 fallback 테스트와, 두 후보가 모두 실패했을 때 `썸네일 없음`이 남는 추가 회귀 테스트로 확인했다.
  실제 카탈로그에 잘못된 이미지 URL을 주입하지 않았다.

## 발견한 문제와 수정 이력

1. **P1 / 모바일 목록이 배경에 덮임:** 우측 rail 장식 배경을 모바일의 전체 화면 위치 지정용 aside에도 적용했다.
   배경을 데스크톱 rail과 실제 표시되는 모바일 플레이어 표면으로 한정했다.
   이후 `glass-catalog-mobile.png`, `glass-discover-mobile.png`, `glass-player-mobile.png`에서 목록·전체 플레이어를 다시 확인했다.
2. **P2 / 좁은 콘텐츠의 가로 패널:** viewport 기준 컨테이너가 내부 패딩을 포함해 전환 경계가 어긋났다.
   page의 실제 콘텐츠 컨테이너를 기준으로 720px에서 전환하도록 수정했다. 1366/768px 캡처와 665/703px 실측으로 세로 배치를 확인했다.
3. **P2 / 포커스 이동 시 헤더가 32px 올라감:** `overflow: hidden`인 frame도 브라우저의 자동 초점 스크롤 대상이 됐다.
   `overflow: clip`으로 수정 후 같은 추천 버튼 동작에서 frame scrollTop=0, header y=56..120,
   main만 스크롤됨을 확인했다. `glass-mobile-focus-fixed.png`, 최종 `glass-long-title-mobile.png`에 근거를 남겼다.
   태블릿 미니 캡처는 이 수정 전 증거이며 미니 동작·단일 iframe 확인에만 사용했다.
4. **P3 / 한국어 제목 단어 중간 줄바꿈:** `word-break: keep-all` 적용 후 최종 모바일 발견 캡처에서 두 줄 단어 보존을 확인했다.
5. 테스트의 예전 분류칩 우선 배치 기대는 승인된 곡명 우선 순서로 수정했다. CSS 모서리 클래스 검증은 실제 정보 순서 검증으로 대체했다.
   frame의 `overflow-hidden` 기대는 실제 초점 스크롤 수정에 맞게 `overflow-clip`으로 갱신했다. 권한·데이터·재생 상태 기대는 완화하지 않았다.

## 코드 검증

- `pnpm architecture:check`, `pnpm lint`, `pnpm typecheck:test`, `pnpm build`: 통과.
- `pnpm test`: 단위 232파일/1,589개, Worker 23파일/220개 통과.
- `pnpm test:coverage`: 255파일/1,809개 통과. Statements 80.01%, Branches 66.95%, Functions 83.46%, Lines 81.54%.
- 이후 썸네일 후보 전체 실패 테스트 1개 추가 및 상세·frame 최종 수정에 대해 OTW Play UI 17파일/153개 전체 통과.
  예전 overflow 클래스 기대를 실제 초점 스크롤 수정에 맞게 갱신한 뒤 전체 UI 검증과 lint를 다시 통과했다.
  마지막 제품 코드로 production build도 다시 통과했다.
- `pnpm sync:agent-cursor:check`: 16개 파일, drift 0.
- API·DTO·DB·player model·공용 sidebar·관리자·package.json·lockfile 변경 없음. 신규 패키지 없음.

## 검증 한계

OS 동작 줄이기 활성화 상태, blur 미지원 실제 브라우저, 모든 이미지 실패의 실제 화면 캡처는 미확인이다.
각각 로드된 CSS 규칙·불투명 기본값·컴포넌트 회귀 테스트로 확인 범위를 제한했다.
현재 카탈로그는 8곡이므로 실제 두 번째 페이지 로딩은 이번 로컬 데이터로 재현되지 않았다. 기존 페이지네이션 회귀 검증은 통과했다.
회원 제출·관리자 검수·운영 배포는 이번 스타일 변경의 실제 쓰기 검증 범위에 포함하지 않았다.

final result: passed
