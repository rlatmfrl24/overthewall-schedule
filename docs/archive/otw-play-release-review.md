> **아카이브 (2026-09-17)**: 조사·설계·검증 당시 기록이다. 제안의 구현 승인이나 현재 운영 상태를 의미하지 않는다. 현재 계약·잔여 작업은 [문서 인덱스](../README.md)와 [개발 상태](../development-status.md)를 따른다.

# OTW Play 구현 코드 검토 — 2026-09-15

## 범위와 판단

기준 커밋 `1079637` 이후 작업 트리의 Gemini 자동 검수, 관리자 저장·게시와 구간 미리보기, 노래 클립 목록, 사용자 곡 제안·내 제안, 테스트 및 에이전트 규칙 정리를 검토했다. API 인증/소유권, 입력 검증, 카탈로그 연결, D1 원자성·버전 충돌, 큐 중복·재시도·일일 예산, 외부 URL 경계, 사용자 입력 보호와 실제 링크 동작을 추적했다.

## 발견 및 수정

- **P2 — 제목에 명시된 방송일 누락:** AI 결과 파서가 방송일의 제목 근거를 제외했다. 제공된 제목에 실제 존재하는 인용을 허용하고, 존재하지 않는 인용과 업로드 메타데이터만으로 제안한 방송일은 계속 제외한다. 정상 날짜·잘못된 날짜·출처 검증 회귀 테스트로 확인한다.
- **P2 — 승인된 가창으로 이동하지 않는 링크:** 내 제안에서 곡 slug만 전달하여 같은 곡의 다른 가창을 열 수 있었다. 승인된 performance ID를 D1 조회 DTO와 공개/관리자 미리보기 링크에 전달한다. 기존 응답과의 호환을 위해 선택 필드로 추가했다. 로컬 내 제안 → 여행 상세에서 `6588f010-fdbe-4b02-b47d-3a4ba07912cc` 가창이 ‘직접 링크로 선택됨’으로 표시되는 것을 확인했다.
- **배포 준비 누락:** 운영에는 0089~0093 마이그레이션과 AI 전용 Queue 준비가 필요했다. 기존 배포와의 호환, 복구 지점, 영향 테이블 보존을 확인한 뒤 병합 배포 전에 준비한다. 운영 Gemini 활성화와 공개 플래그 변경은 이 릴리스에 포함하지 않는다.

## 검증 근거

- 93개 마이그레이션을 임시 D1에 실제 적용하는 전체 체인 검증 통과. 기존 로컬 데이터는 보존했다.
- 제목 방송일 파서 23개 테스트, 내 제안 UI 7개 테스트, 회원 제안 D1 통합 15개 테스트 통과. API DTO 추가 후 전체 테스트 타입 검사와 변경 파일 lint 통과.
- 에이전트 동기화 스크립트 자체 테스트 5개 통과. 기존 routeTree 생성 내용 보존.
- 최근 로컬 UI 검증: 카드 목록·상세 팝업, 승인 항목 숨김/복원, 모바일 표시, 철회 취소, 초점 복귀. 이번 검토에서 수정한 승인 가창 링크는 실제 화면 이동까지 다시 확인했다.

## 한계

실제 Gemini 호출과 모델 품질 확인의 경로별 이력은 `otw-play-ai-review.md`에 기록되어 있다. 이번 코드 검토에서는 유료 분석을 추가 실행하지 않았다. 모의 분석 API 테스트를 모델 정확도 검증으로 해석하지 않는다. 운영 공개/분석 기능은 기존 비활성 상태를 유지하며, 로그인한 운영자의 실제 게시 검증은 별도 활성화 결정 없이 실행하지 않는다.

## 최종 검사 및 운영 준비

- `pnpm preflight` 통과: 295개 파일/2,196개 테스트, architecture, 타입, lint, Worker/웹 빌드, 로컬 D1 doctor, mirror check. 승인 가창 ID의 마지막 DTO 변경은 관련 UI 7개·D1 15개 테스트와 타입·lint로 추가 확인했으며 빌드는 해당 변경 이후 실행됐다.
- 운영 0089~0093 적용 완료, 미적용 0. `otw-play-ai-review` Queue 생성 완료.
- 적용 전후 주요 행 수 동일: 가창 162, 제안 1, 후보 156, 가창 참여자 302, 소스 연결 162. `foreign_key_check` 결과 0건, 임시 보존 테이블 잔여 0개. 이는 행 수·제약 검증이며 운영 전체 행의 바이트 단위 비교는 아니다.
- 운영 catalog revision 410, public_read_enabled=0, navigation_visible=0 유지. 적용 전 D1 Time Travel bookmark: `000066cc-00000000-000050e7-570ed0bd1ef0bc00a14683e70ace51be`.
