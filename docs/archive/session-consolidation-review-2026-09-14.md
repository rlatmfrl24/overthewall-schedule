> **아카이브 (2026-09-17)**: 조사·설계·검증 당시 기록이다. 제안의 구현 승인이나 현재 운영 상태를 의미하지 않는다. 현재 계약·잔여 작업은 [문서 인덱스](../README.md)와 [개발 상태](../development-status.md)를 따른다.

# 세션 변경 통합 코드 리뷰 — 2026-09-14

## 리뷰 판정

**No material findings.** 검토한 변경에서 수정이 필요한 중대한 결함을 발견하지 못했다. 통합 코드 68개 파일을 대상으로 정적 검토, 전체 테스트·커버리지·빌드, 실제 로컬 UI와 D1 재조회를 완료했다. 이 판정은 운영 배포나 외부 서비스의 모든 상황에 대한 보증을 뜻하지 않는다.

## 브랜치와 검토 대상

- 통합 브랜치: `codex/session-consolidation-2026-09-14`
- 워크트리: `C:/Develop/overthewall-schedule-integrated-review`
- 기준: `origin/master` = `9bf44786dee2397d381ebd9e2c12c01a70d4212e` (PR #135)
- 검증한 코드 HEAD: `9ca8998e28ac6d2e526fe0e06e417e703eb9e17d`
- 비교 범위: 위 기준부터 코드 HEAD까지. 이 문서는 검증 후 추가한 기록이다.

| 통합 커밋 | 출처 | 포함한 변경 |
| --- | --- | --- |
| `01612e9` | 원래 작업 폴더, 47개 파일 | 사용자 메뉴·테마, 게시글 카드, Play 승인 안내·곡 검색·카탈로그 스크롤·기본 목록 편집, 일정 저장 UX, 전용 YouTube VOD API·수집·화면 |
| `ea19d05` | `overthewall-schedule-sul-11`, 19개 파일 | Inter·Pretendard 자체 제공, 전체 앱 폰트, 스냅샷 폰트 준비·캡처·실패 처리, 라이선스와 검토 문서 |
| `53c4915` | `overthewall-schedule-sul-5`, 1개 파일 | 외부 공유 배너의 필요성·공유 방식·구현안 문서. 승인된 범위대로 구현은 추가하지 않음 |
| `e29a05e` | `overthewall-schedule-cost-optimization`, 1개 파일 | 9월 8일 운영 비용 첫 관측 기록 |
| `9ca8998` | `codex/original-playlist-artwork`의 `75434ca` | 오리지널 목록 기본 이미지 지정 및 D1 통합 테스트 |

서로 겹치는 Design.md와 daily-schedule.tsx는 양쪽 변경을 함께 보존했다. 비용 문서의 충돌은 9월 8일 관측과 9월 9일 개선 증거를 날짜 순으로 모두 유지하여 해결했다. 대표 이미지 충돌은 최신 `representativePerformanceId` 계약과 과거 기본 이미지 변경을 함께 유지했다. `playlist-service.ts`의 관리자 지정 대표곡 이미지가 기본값보다 먼저 적용되는 동작을 확인했다.

## 누락·중복 및 원본 보존 점검

통합 전 등록된 워크트리 12개, 로컬 브랜치 9개, 원격 refs와 stash 1개를 확인했다. 최근 관련 Codex 작업의 사용자 요청도 읽어 승인된 범위를 대조했다.

- 대부분의 과거 작업 HEAD는 최신 master의 조상이다. 재적용하지 않아도 통합 브랜치에 포함된다.
- X 답글 작업 `6fbca5a`는 PR #118의 정확한 HEAD이며, 병합 커밋 `bf35dd6`과 트리가 동일하다. 병합 커밋은 최신 master의 조상이다.
- WebSub 작업 폴더의 미추적 `unused-implementation-audit-2026-09-09.md`는 현재 master의 동일 문서에서 과거 기록임을 설명하는 문단만 빠진 구본이다. 최신 설명을 지우지 않고 master 문서를 유지했다.
- 원래 대표 이미지 워크트리의 `src/routeTree.gen.ts` 변경은 줄바꿈만 달라 실질적인 diff가 없다. 통합 빌드로 정상 재생성했다.
- `stash@{0}` (`e1f64cf`, 2026-05-28)은 구형 X 영구 저장·수집·비용 집계 작업이다. 후속 `82eecb3` 및 PR #32, 현행 X 기능, `0025_high_centennial.sql`·`0026_first_toro.sql`과 대조했다. 폐기된 경로, 중복 CREATE TABLE 마이그레이션과 `.tanstack/tmp`를 되살리지 않았다. stash 원본은 보존했다.
- 원본 4개 작업 폴더에서 추출한 파일 68건의 SHA-256을 통합 후 재검사했으며 변경된 원본은 0건이었다. 원래 브랜치, 인덱스와 미커밋 작업을 이동·삭제하지 않았다.
- 추출 목록·원본 해시는 원래 저장소의 `.tmp/session-consolidation-2026-09-14/manifest.json`에 보관한다. 임시 인덱스로 스냅샷을 만들어 원본 staging 상태에 영향을 주지 않았다.

## 주요 코드 검토

- **일정:** UI의 충돌 확인, 저장 요청, 저장 후 목록 무효화, 조회만 재시도하는 안내를 추적했다. 저장 실패를 성공으로 표시하지 않으며, 이전 조회 결과가 새 저장 안내를 덮지 않는 회귀 테스트가 있다.
- **YouTube VOD:** 활성 프로필 링크 → 수집 대상 등록·정리·예약 판단 → 기존 채널별 최신 50개 수집 → 저장 영상의 공개 조회 → 멤버 필터·커서·카드 흐름을 검토했다. 메인 채널·Shorts 허용 목록과 VOD 전용 채널의 과거 페이지 수집 제외를 확인했다. 공개 VOD 조회는 쓰기를 하지 않는다.
- **Play:** 승인 차단 사유는 기존 승인 조건과 일치하며 preflight 실패·이전 응답 경합을 처리한다. 회원 흐름은 요청대로 기존 곡 검색 후 새 곡 입력으로 이어진다. 카탈로그의 기존 URL 선택 항목이 스크롤 범위 밖으로 숨지 않도록 처리한다.
- **테마·공통 UI:** 관리자 테마 UI 삭제는 이전 사용자의 명시적 요청임을 확인했다. 공개 사용자 메뉴의 닉네임·테마·계정 동작과 게시글 카드 변경을 검토했다.
- **폰트:** 전체 앱의 영문·숫자 Inter 및 한글 Pretendard, 스냅샷 전용 폰트 준비와 재측정, 같은 폰트 바이트의 PNG 포함을 검토했다. 시스템 폰트 전환은 실패 시 대체 경로이며 정상 캡처의 전제가 아니다.
- **권한·데이터:** 새 API는 공개 읽기만 추가한다. 기존 관리자 권한, 저장·충돌 정책, 할당량·D1 가드, 마이그레이션 구조를 변경하지 않는다. 테스트를 맞추기 위한 계약 축소나 구형 경로 복원은 하지 않았다.

## 실행한 검증

| 검사 | 결과 |
| --- | --- |
| `pnpm lint` | 통과 |
| `pnpm architecture:check` | 통과 |
| `pnpm sync:agent-cursor:check` | 16개 mirror 일치, drift 0 |
| `pnpm typecheck:test` | 통과 |
| `pnpm test:coverage` | 281개 파일, 2,086개 테스트 통과. Worker D1 통합 프로젝트 포함 |
| 커버리지 | statements 82.91%, branches 70.17%, functions 86.00%, lines 84.41%; 설정된 기준 통과 |
| `pnpm build` | 타입 검사, Worker·클라이언트 빌드, SEO 생성 통과 |

Node 24.20.0과 pnpm 11.7.0을 사용했다. 변경 없는 lockfile에 대해 기존 node_modules를 junction으로 재사용했다. pnpm이 워크트리 경로 차이로 자동 재설치를 시도하는 것을 막기 위해 실행 환경의 `pnpm_config_verify_deps_before_run=never`를 사용했으며 검사 스크립트나 기준은 바꾸지 않았다. 빌드에 필요한 기존 ignored 환경 파일을 새 워크트리에 복사했고 출력·커밋에는 포함하지 않았다.

`git diff --check`에는 배너 Markdown의 의도적인 줄바꿈 공백 3곳과 동봉한 Pretendard 원본 라이선스 공백 1곳이 남는다. 코드 충돌이나 공백 오류는 없다. 라이선스 원문을 수정하지 않았다.

## 실제 화면과 authoritative readback

검토한 코드 HEAD를 `http://localhost:5184`에서 실행했다. 기존 로컬 D1을 SQLite backup으로 복제하여 원래 DB와 분리했다.

1. 오늘 일정표에서 멤버의 **스케쥴 추가하기** → 제목 입력 → PC 빠른 시간 **19:00** → **스케쥴 추가**를 수행했다. 팝업이 닫힌 뒤 성공 안내와 실제 일정 카드가 나타났다. 복제 D1의 `schedules` 재조회에서 ID `2652`, member `1`, 날짜 `2026-09-14`, 시각 `19:00`, 제목 `통합 리뷰 로컬 검증`, 상태 `방송`을 확인했다.
2. 같은 화면의 **이미지 다운로드**를 실행하여 정상 완료 안내를 확인했다. 생성 PNG는 2640×1306, 414,990 bytes이며 다운로드 파일을 직접 열어 한글, 시각, 제목과 카드 배치를 확인했다. 시스템 폰트 대체 안내 없이 정상 캡처됐다. 근거 이미지: `.tmp/review/schedule-capture.png`.
3. **VOD & 클립 → 유튜브 다시보기**에서 8명 필터와 최초 20개 영상을 확인했다. **다시보기 더 보기**로 40개가 됐으며, 나츠키 필터 선택 시 20개로 초기화됐다. 같은 API의 두 페이지 재조회는 각 20개, 합계 고유 영상 40개였고, 필터 응답 20개 모두 member `1`을 포함했다. 수집 상태는 `ready`였다.

이 실제 흐름과 재조회는 테스트 fixture로 대체하지 않았다. 자동 테스트는 실패·경합·D1 계약 검증을 보완한다.

## 남은 검증 한계

- 이번 리뷰에서 운영 배포·운영 DB 변경·원격 브랜치 push·PR 작성·병합은 수행하지 않았다.
- 외부 YouTube 실제 예약 수집을 다시 실행하지 않았다. 기존에 수집된 로컬 데이터로 공개 UI를 검증했고, 예약 수집은 Worker 통합 테스트와 소스 추적으로 확인했다.
- 모바일 전체 흐름, 다른 OS의 폰트 렌더링, 폰트 네트워크 실패의 실제 브라우저 재현, Play 관리자 승인·회원 제출의 실제 쓰기는 이번 리뷰에서 재수행하지 않았다. 관련 자동 테스트와 코드 검토는 포함한다.
- 중대한 미해결 사항이나 추가 제품 정책 결정은 발견하지 못했다. 리뷰용 로컬 일정은 복제 DB에만 남겨 두었다.
